var documenterSearchIndex = {"docs":
[{"category":"section","location":"trace_evaluation/#Evaluating-a-trace-against-a-model","page":"Evaluating a Trace","text":"A trajectory is just a list of (when, clock_key) pairs — a recording of which event fired, and at what time. Once you have one, a natural question is: how likely is this trajectory under my model? trace_likelihood answers it. It walks a recorded trace against a model, scores each step's contribution to the trajectory's log-likelihood, and reports whether the trace is even feasible — whether every step named an event the model had enabled at a strictly-increasing time. It never throws on a bad trace; it returns a TraceEvaluation that says what went wrong and where. The runbook entry is the terse reference; this page is the narrative.","title":"Evaluating a trace against a model"},{"category":"section","location":"trace_evaluation/#When-you-would-reach-for-it","page":"Evaluating a Trace","text":"Three situations bring a modeler here:\n\nLikelihood of an observed trajectory. You have field data — an observed sequence of events with timestamps — and a candidate model. trace_likelihood gives you log P(trajectory | model), the ingredient for inference.\nModel comparison. Score the same observed trace under two models and compare their log-likelihoods; the larger number is the trajectory the model found less surprising.\nFeasibility checking. Before you trust a likelihood, you need to know the trace is a legal path at all. A trace produced by a different model — or a hand-edited one — may name an event the model never enabled. trace_likelihood catches that as an infeasible verdict rather than silently returning a number.","title":"When you would reach for it"},{"category":"section","location":"trace_evaluation/#The-step_likelihood-requirement","page":"Evaluating a Trace","text":"Scoring a step means asking the model which clocks were enabled, with what distributions, at this instant — the competing-clock set. A plain simulation does not keep that set queryable after the fact, so the evaluation simulation must be built with step_likelihood=true, which tells its SamplingContext to record the enabled-clock likelihood as the run walks the trace. This is the one setup rule; forget it and trace_likelihood throws an ArgumentError reading \"tracelikelihood needs a simulation built with steplikelihood=true\". The flag is only needed on the evaluation sim — the run that produced the trace can be built with the default settings.","title":"The step_likelihood requirement"},{"category":"section","location":"trace_evaluation/#A-worked-example:-the-two-clock-race","page":"Evaluating a Trace","text":"The cleanest model to build intuition on is a competing exponential race, because its likelihood has a closed form you can check by hand. Two clocks, FireA and FireB, are perpetually enabled (their preconditions are trivially true); each firing bumps its own field, which re-proposes it through its derived generator, so the survivor keeps racing. FireA has rate LA = 2.0, FireB has rate LB = 3.0.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nusing Random: Xoshiro\nimport ChronoSim: precondition, generators, enable, fire!\n\nmodule Race\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nimport ChronoSim: precondition, generators, enable, fire!\n\nconst LA = 2.0\nconst LB = 3.0\n\n@keyedby RaceCell Int64 begin\n    a::Int64\n    b::Int64\nend\n\n@observedphysical RaceBoard begin\n    cell::ObservedVector{RaceCell,Member}\nend\n\nfunction RaceBoard(n::Int)\n    cells = ObservedArray{RaceCell,Member}(undef, n)\n    for i in eachindex(cells)\n        cells[i] = RaceCell(0, 0)\n    end\n    return RaceBoard(cells)\nend\n\nstruct FireA <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireA, state) = state.cell[evt.idx].a >= 0\nenable(::FireA, state, when) = (Exponential(1 / LA), when)\nfire!(evt::FireA, state, when, rng) = (state.cell[evt.idx].a += 1; nothing)\n\nstruct FireB <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireB, state) = state.cell[evt.idx].b >= 0\nenable(::FireB, state, when) = (Exponential(1 / LB), when)\nfire!(evt::FireB, state, when, rng) = (state.cell[evt.idx].b += 1; nothing)\n\nfunction init!(state, when, rng)\n    state.cell[1].a = 0\n    state.cell[1].b = 0\n    return nothing\nend\nend # module\n\nusing .Race: RaceBoard, FireA, FireB, LA, LB\n\n# 1. Record a forward trace. The observer stores (when, clock_key).\ntrace = Tuple{Float64,Tuple}[]\nobserver = (physical, when, event, changed) -> begin\n    event isa ChronoSim.InitializeEvent && return nothing\n    push!(trace, (when, clock_key(event)))\nend\nfsim = SimulationFSM(RaceBoard(1), [FireA, FireB];\n    rng=Xoshiro(424242),\n    observer=observer)\nChronoSim.run(fsim, Race.init!, (p, i, e, w) -> i > 20)\n\n# 2. Evaluate on a *fresh* sim built with step_likelihood=true so it records\n#    the enabled-clock set at each step.\nesim = SimulationFSM(RaceBoard(1), [FireA, FireB];\n    rng=Xoshiro(7), step_likelihood=true)\nev = trace_likelihood(esim, Race.init!, trace)\n\nThe initializer (Race.init!) is either an init function (physical, when, rng) -> nothing or a SimEvent whose fire! sets up the state — exactly what you would hand ChronoSim.run. The trace is any AbstractVector of (when::Float64, clock_key::Tuple) pairs.","title":"A worked example: the two-clock race"},{"category":"section","location":"trace_evaluation/#Reading-a-feasible-verdict","page":"Evaluating a Trace","text":"ev displays as:\n\nTraceEvaluation\n  feasible         : true\n  loglikelihood    : -2.295205868713304\n  steps evaluated  : 20\n  first infeasible : none\n\nfeasible : true — every one of the 20 steps named an enabled event at a strictly-increasing time. This is the precondition for trusting the number above it.\nloglikelihood : -2.295… — the trajectory's log-likelihood under the model. On its own it is only meaningful relative to another model's score for the same trace; that is what makes it a model-comparison tool.\nsteps evaluated : 20 — how many steps were scored. For a feasible trace this equals the trace length; for an infeasible one it is the count of steps that scored cleanly before evaluation stopped. It always equals length(ev.steploglik), and sum(ev.steploglik) equals ev.loglikelihood.\nfirst infeasible : none — no failing step. On an infeasible trace this line names the culprit instead.","title":"Reading a feasible verdict"},{"category":"section","location":"trace_evaluation/#Checking-the-number-by-hand","page":"Evaluating a Trace","text":"The two-clock race is worth its keep because the likelihood is analytic. In a competing exponential race where both clocks stay enabled, each inter-event gap Δt contributes the winner's density times the loser's survival — λ_win · e^{-λ_win Δt} · e^{-λ_lose Δt} = λ_win · e^{-(λA+λB) Δt} — and memorylessness means nothing carries across firings. Multiply over all steps and the survival exponents telescope to the final time tN:\n\nlog L = n_A log lambda_A + n_B log lambda_B - (lambda_A + lambda_B) t_N\n\nThis recorded trace fired FireA 10 times and FireB 10 times, ending at tN = 4.042560112198771. So:\n\nna, nb, tN = 10, 10, 4.042560112198771\nna*log(LA) + nb*log(LB) - (LA + LB)*tN   # = -2.2952058687133032\n\nwhich matches ev.loglikelihood = -2.295205868713304 to floating-point tolerance — the evaluator's per-step scoring reproduces the closed form. A tiny fixed trace makes the arithmetic fully manual:\n\nfixed = Tuple{Float64,Tuple}[(0.3, (:FireA, 1)), (0.7, (:FireB, 1)), (1.1, (:FireA, 1))]\ntrace_likelihood(esim, Race.init!, fixed).loglikelihood   # = -3.015093350212\n\nTwo FireA and one FireB ending at t = 1.1 give 2 log 2 + log 3 − 5·1.1 = -3.0150933502119996. The evaluator agrees.","title":"Checking the number by hand"},{"category":"section","location":"trace_evaluation/#Differentiating-a-trace-likelihood","page":"Evaluating a Trace","text":"Once the sequence of events is fixed, the trace log-likelihood is a smooth function of the clock distributions' rate parameters, and its gradient with respect to those parameters is the score function. Because SimulationFSM now drives CompetingClocks through the high-level SamplingContext, that gradient is available directly from ForwardDiff.jl: score for maximum-likelihood or Hamiltonian Monte Carlo, and the negative Hessian for the observed information (standard errors).\n\nThe mechanics are one keyword plus one modelling rule:\n\nlikelihood_eltype=eltype(θ) on SimulationFSM is what routes ForwardDiff.Dual numbers into the likelihood accumulator. Pass it together with step_likelihood=true: when the closure is later evaluated at a plain Float64 vector, eltype(θ) is Float64 and only the explicit step_likelihood=true keeps trace_likelihood working. The result's loglikelihood (and every entry of steploglik) then has element type eltype(θ): a Float64 under plain evaluation, a Dual under differentiation, so the same closure serves both.\nThe parameters must reach the events' enable functions Dual-generically. The derivative information rides in the distribution parameters, so the Exponential/Weibull/… each step re-proposes must be rebuilt from the vector ForwardDiff perturbs. Route θ through a Ref or a parametric field of the physical state whose element type is not pinned to Float64 — an Any-typed Ref is the simplest — so a Dual fits where a Float64 did. Event times stay Float64 throughout.\n\nUnder the hood the sampler always runs on primal Float64 values (CompetingClocks strips the Duals at the single boundary through which every enable! reaches the sampler), while the likelihood watcher keeps the Duals and accumulates the gradient. Sampling happens at a concrete parameter point; only the scoring is differentiated. This is the statistically correct split, not a compromise — see CompetingClocks' \"Differentiating the Path Likelihood\" narrative for the theory.\n\nHere is the two-clock race again, rewritten so its rates come from a module global Ref that a caller can fill with either plain numbers or Duals. The Ref is Any-typed precisely so one closure serves both:\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nusing ForwardDiff\nusing Random: Xoshiro\nimport ChronoSim: precondition, generators, enable, fire!\n\nmodule ADRace\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nimport ChronoSim: precondition, generators, enable, fire!\n\n# Rates reach enable() Dual-generically: an `Any`-typed Ref so (Float64, Float64)\n# and (Dual, Dual) tuples both fit. Exponential takes the scale, so rate λ is\n# Exponential(1/λ).\nconst RATES = Ref{NTuple{2,Any}}((2.0, 3.0))\n\n@keyedby ADCell Int64 begin\n    a::Int64\n    b::Int64\nend\n\n@observedphysical ADBoard begin\n    cell::ObservedVector{ADCell,Member}\nend\n\nfunction ADBoard(n::Int)\n    cells = ObservedArray{ADCell,Member}(undef, n)\n    for i in eachindex(cells)\n        cells[i] = ADCell(0, 0)\n    end\n    return ADBoard(cells)\nend\n\nstruct FireA <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireA, state) = state.cell[evt.idx].a >= 0\nenable(::FireA, state, when) = (Exponential(1 / RATES[][1]), when)\nfire!(evt::FireA, state, when, rng) = (state.cell[evt.idx].a += 1; nothing)\n\nstruct FireB <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireB, state) = state.cell[evt.idx].b >= 0\nenable(::FireB, state, when) = (Exponential(1 / RATES[][2]), when)\nfire!(evt::FireB, state, when, rng) = (state.cell[evt.idx].b += 1; nothing)\n\nfunction init!(state, when, rng)\n    state.cell[1].a = 0\n    state.cell[1].b = 0\n    return nothing\nend\nend # module\n\nusing .ADRace: ADBoard, RATES\n\n# A fixed trace: FireA fires at 0.3 and 1.1, FireB at 0.7.\ntrace = Tuple{Float64,Tuple}[(0.3, (:FireA, 1)), (0.7, (:FireB, 1)), (1.1, (:FireA, 1))]\n\n# The differentiable closure: push θ into RATES, build an evaluation sim whose\n# likelihood eltype matches θ, and return the trace log-likelihood.\nfunction loglik(θ)\n    RATES[] = (θ[1], θ[2])\n    sim = SimulationFSM(ADBoard(1), [ADRace.FireA, ADRace.FireB];\n        rng=Xoshiro(7), step_likelihood=true, likelihood_eltype=eltype(θ),\n        key_type=Tuple)\n    return trace_likelihood(sim, ADRace.init!, trace).loglikelihood\nend\n\nscore = ForwardDiff.gradient(loglik, [2.0, 3.0])      # the score s(θ) = ∇ log L\ninfo  = -ForwardDiff.hessian(loglik, [2.0, 3.0])      # observed information\n\nFor this always-enabled exponential race the score has a closed form, ∂ log L / ∂λ_k = n_k/λ_k − t_N with n_k the number of times clock k fired and t_N the last event time, so the answer is checkable by hand. With two FireA firings, one FireB, and t_N = 1.1:\n\nscore ≈ [2/2.0 - 1.1, 1/3.0 - 1.1]   # ≈ [-0.1, -0.7666…]\n\nThe same closure evaluated at a plain Float64 vector returns a Float64 equal to the value computed in Checking the number by hand above — eltype(θ) is then Float64, and the explicit step_likelihood=true keeps ordinary evaluation working. Non-exponential clocks need nothing special: replace the Exponential in enable with, say, Weibull(θ[1], θ[2]) and the same code differentiates a genuinely semi-Markov race. Any distribution whose logpdf/logccdf are differentiable in its parameters works.\n\nInfeasibility survives differentiation: an infeasible trace still produces feasible == false and a loglikelihood of -Inf, converted to the Dual element type when θ carries Duals.","title":"Differentiating a trace likelihood"},{"category":"section","location":"trace_evaluation/#Reading-an-infeasible-verdict","page":"Evaluating a Trace","text":"Corrupt step 17 to name a clock the model never enabled — (:FireA, 99), an index with no cell:\n\nbad = copy(trace)\nbad[17] = (bad[17][1], (:FireA, 99))\ntrace_likelihood(esim, Race.init!, bad)\n\nTraceEvaluation\n  feasible         : false\n  loglikelihood    : -Inf\n  steps evaluated  : 16\n  first infeasible : step 17, event (:FireA, 99), reason not_enabled\n\nThe verdict flips: feasible : false, the likelihood collapses to -Inf, and steps evaluated : 16 says the first sixteen steps scored cleanly before step 17 named an event that was not in the enabled set. The first infeasible line — the same data as ev.first_infeasible == (17, (:FireA, 99), :not_enabled) — points you straight at the offending step. Inside a larger structure the same value prints on one line: TraceEvaluation(feasible=false, loglikelihood=-Inf, steps_evaluated=16).","title":"Reading an infeasible verdict"},{"category":"section","location":"trace_evaluation/#Failure-forms","page":"Evaluating a Trace","text":"A trace is infeasible when a step cannot happen under the model. reason is one of two symbols:\n\n:not_enabled — the step named a clock_key that was not enabled when its turn came: the event was never enabled, or had already been disabled or consumed. This is the verdict for a trace produced by a different model, or a hand-edit like the (:FireA, 99) above.\n:time_order — the step's when was not strictly greater than the previous event's time. Trace times must strictly increase; an equal or earlier time is a :time_order failure.\n\n:not_enabled is checked first: if both fail, the missing enablement is reported (the time comparison only means something for a step that could fire at all).\n\nTwo non-failure endings are worth knowing:\n\nA trace entry with a non-finite when (e.g. Inf) or a nothing key ends evaluation early without marking the trace infeasible — feasible stays true. This is the trace's normal end-of-data sentinel, mirroring a forward run that exhausts its events.\nAn empty trace is feasible with loglikelihood = 0.0 and zero steps.\n\nAnd the one setup error: an ArgumentError reading \"tracelikelihood needs a simulation built with steplikelihood=true\" means the evaluation sim was built without that flag. Add step_likelihood=true as shown above.","title":"Failure forms"},{"category":"section","location":"trace_evaluation/#What-to-do-with-the-answer","page":"Evaluating a Trace","text":"An infeasible verdict on a trace your own model produced is a red flag: either the trace came from a different model version, or the evaluation sim was built differently from the recording sim. Line the two constructions up.\nA feasible likelihood is the number you compare across models. Score the same observed trace under each candidate; the least-negative wins.\nFeasibility is also a cheap conformance check: run a trajectory recorded from model A through model B's evaluator, and an infeasible step localizes the first place the two models disagree about what is enabled.","title":"What to do with the answer"},{"category":"section","location":"trace_evaluation/#Related","page":"Evaluating a Trace","text":"Runbook: Evaluate a recorded trace — the mechanical reference and field list.\nRecording and replaying a run — produces the richer TrajectorySkeleton when you need enable/disable history, not just (when, clock_key) pairs.\nModel checking a simulation — a discrete, exhaustive check of a recorded trace against a compiled spec, the qualitative counterpart to this quantitative one.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"effect_analysis/#Static-effect-analysis-(@fire)","page":"Static Effect Analysis","text":"@precondition derives what an event reads. @fire closes the loop by deriving what an event writes: a syntactic taint pass over the fire! body produces a static set of WriteSpecs — the event's effect footprint — which a runtime oracle then checks against what the event actually changed. The write masks feed footprint-interference lints, model-checkable effect lowering, and the whyrunning \"provably cannot stop\" verdict.\n\n@fire emits the fire! method verbatim: annotated and unannotated events run byte-for-byte identically. All the machinery is analysis metadata.","title":"Static effect analysis (@fire)"},{"category":"section","location":"effect_analysis/#Why-declare-effects","page":"Static Effect Analysis","text":"The read side already proved the pattern: @precondition finds triggers you would never list by hand (a doors_open write that must rebirth an event). Writes give you the other half:\n\nConformance. CheckEffects asserts, after every fire, that every changed address matches a declared write — catching an undeclared write the moment it happens instead of when a trajectory eventually diverges.\nReachability. can_stop_change answers whether any event can ever write what a stop predicate reads; when none can, whyrunning reports the run provably cannot stop by state change.\nDownstream analysis. The kept write masks and rhs ASTs are the input to footprint-interference lints and to effect lowering for a model checker.","title":"Why declare effects"},{"category":"section","location":"effect_analysis/#Annotating-a-fire!","page":"Static Effect Analysis","text":"Prefix @fire to the fire! definition. The signature contract is the usual four arguments (evt::EvtType, state, when, rng):\n\n@fire function fire!(evt::Infect, physical, when, rng)\n    physical.actors[evt.sink].strain = physical.actors[evt.source].strain\n    physical.actors[evt.sink].state = Infectious\nend\n\nThe walker understands, in fragment:\n\nleaf assignment x.field = rhs and container assignment c[k] = rhs;\nthe op-assign forms (+=, -=, *=, /=, %=, ^=, ÷=, |=, &=, ⊻=) — both a read and a write of the same address;\nthe ObservedState mutation API (push!, pushfirst!, append!, pop!, popfirst!, delete!, empty!, filter!, union!, intersect!, setdiff!, symdiff!, resize!, sizehint!, setindex!);\n@obswrite/@obsread;\nregistered @fragment helpers (their body is inlined, so a helper's writes are seen); and\nnested fire!(EvtType(args...), state, when, rng) calls to other @fired events (inlined, with the constructor arguments substituted for the callee's event fields).\n\nLoops and branches widen indices exactly as the read pass does: a loop-indexed write becomes a widened (tainted-index) may-write, and a write under a branch is recorded unconditionally.\n\nWhat errors at macro time:\n\na call whose name ends in ! that receives state and is not a recognized mutator (register it with @fragment, or rewrite with a recognized form);\na .= broadcast assignment to state;\na body that writes no state at all.\n\nA non-! helper that receives state (e.g. get_direction(person.location, …)) is tolerated: it is treated as opaque, a note is recorded, and any hidden write it performs is left for the runtime oracle to catch.","title":"Annotating a fire!"},{"category":"section","location":"effect_analysis/#Reading-the-WRITES-report","page":"Static Effect Analysis","text":"derivation_report(EvtType) prints a WRITES section for any @fired event:\n\nDerivation report for Infect\n  event fields: source, sink\n  triggers: none derived (hand-written generators)\n  WRITES (2 sites, 0 widened)\n    WRITE [actors, ℤ, strain]  CLEAN  binds: sink  op: assign  rhs: state_expr\n    WRITE [actors, ℤ, state]  CLEAN  binds: sink  op: assign  rhs: evt_pure\n  rhs mix: evt_pure 1, state_expr 1, stochastic 0, opaque 0\n\nEach line is one write site: its address mask (ℤ is a widened index position; a trailing .* marks a whole-element/subtree write), CLEAN vs WIDENED (with the widened-write count in the header — the over-approximation counter), the bound event fields, the operation, and the rhs classification. The rhs classes, in precedence order:\n\n:stochastic — the value depends on the rng (a rand-family call);\n:opaque — the value depends on when (time is not modeled), or reads a scalar written earlier in the same body (alias staleness), or is otherwise unanalyzable;\n:evt_pure — the value is built from event fields, literals, and module-level constants (enum instances count);\n:state_expr — the value is a pure expression over state reads and literals.\n\n:opaque should be rare — in the example corpus it is about 13% of write sites, and every occurrence has a named cause (when-dependence, an alias-staleness sequencing, or a loop-variable value). A rising opaque rate is a signal that the walker lost track of something.","title":"Reading the WRITES report"},{"category":"section","location":"effect_analysis/#What-the-oracle-catches","page":"Static Effect Analysis","text":"CheckEffects is an ExecutionPolicy that enforces the conformance contract changed ⊆ declared: after initialization and after every fire, every captured changed address must match some declared write mask (unioned with the specs of any isimmediate event types you pass in). Because widening and the may-write branch union only add coverage, the oracle never false-positives on a loop or a branch — the only throw is a genuinely undeclared write.\n\nWire it into a test the same way you construct any simulation:\n\nsim = SimulationFSM(physical, events; seed=42, policy=CheckEffects(events))\nChronoSim.run(sim, InitEvent(), stop)\n\nIt is opt-in, read-only, and consumes no randomness, so turning it on does not change the trajectory. Compose it with other diagnostics via PolicyStack.","title":"What the oracle catches"},{"category":"section","location":"effect_analysis/#A-worked-EffectCoverageError","page":"Static Effect Analysis","text":"Suppose Infect grew a helper that also bumps a location counter, but the helper is an ordinary function the walker cannot see into:\n\nsneaky(physical, i) = (physical.locations[i].cnt += 1; nothing)\n\n@fire function fire!(evt::Infect, physical, when, rng)\n    physical.actors[evt.sink].strain = physical.actors[evt.source].strain\n    physical.actors[evt.sink].state = Infectious\n    sneaky(physical, evt.sink)          # undeclared write to :locations\nend\n\n@fire records only the two actors writes (and a note that sneaky receives state). At runtime the oracle sees the extra change and throws:\n\nEffectCoverageError: event Infect wrote an address not\ncovered by any WriteSpec.\n  changed address: (locations, 3, cnt)\n  masked to      : (locations, _index, cnt)\n  classified     : missing_container — no WriteSpec names this top-level field (an undeclared effect)\n  declared writes (masked):\n    (actors, _index, strain)\n    (actors, _index, state)\nThis event performed a write its @fire analysis did not declare — either the write hides behind an opaque helper (register it with @fragment or use a recognized mutation form) or the walker misclassified the address shape.\n\nThe two-line fix is to make the write visible — either mark the helper @fragment (so its body is inlined and its write derived) or write the mutation directly in the fire! body. :missing_container means the changed field's top-level container is named by no spec; :shape_mismatch means the container is declared but at a different leaf or index shape.","title":"A worked EffectCoverageError"},{"category":"section","location":"effect_analysis/#Related","page":"Static Effect Analysis","text":"Runbook: Check that events only write what they declare.\nLinting a model's footprints — consumes the write masks @fire derives.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"invariants/#Declaring-and-checking-invariants","page":"Declaring Invariants","text":"An invariant is a safety property that must hold in every state a run passes through — \"a person is either at a floor or in an elevator, never both\", \"an elevator's floor is in range\". @invariant declares one as a named, pure boolean function of the physical state; the CheckInvariants policy evaluates every declared invariant after initialization and after every fired event, and throws a structured InvariantViolation the instant one turns false — naming the invariant, the event that broke it, and the exact address the fire wrote. Composed with a recorder, the violation even carries the replay command that reconstructs the state one step before the break. The runbook entry is the terse reference; this page is the narrative.","title":"Declaring and checking invariants"},{"category":"section","location":"invariants/#The-symptom","page":"Declaring Invariants","text":"A run finishes, or throws deep in event code, and you suspect the state went bad long before the crash surfaced — a counter went negative, two mutually exclusive flags both got set, an index escaped its range. Without invariants you find out only when something downstream trips over the corruption, far from where it happened. An invariant turns \"the state is wrong somewhere\" into \"invariant X broke at step k, and this event wrote this address to break it\".","title":"The symptom"},{"category":"section","location":"invariants/#Declaration-discipline","page":"Declaring Invariants","text":"Declare invariants at the model module's top level. Each @invariant takes a string name and an anonymous one-argument function of physical:\n\n@invariant \"a xor b\" function (physical)\n    all(c.a == 0 || c.b == 0 for c in physical.cell)\nend\n\nTwo rules make the diagnostics sharp:\n\nOne @invariant per logical clause. On violation the failing name is the first thing you read, so a narrow name localizes the bug. A single monolithic invariant that ANDs ten conditions only ever tells you \"something is wrong\".\nA pure boolean function of physical. One argument, no mutation, no randomness, returns Bool. The checker re-evaluates the invariant on the failure path to recover which addresses it read (to name the guilty ones), so a non-pure invariant gives inconsistent answers and the checker warns. A body that returns a non-Bool is a declaration error.","title":"Declaration discipline"},{"category":"section","location":"invariants/#The-port-from-validator-story","page":"Declaring Invariants","text":"This discipline usually formalizes a validator you already wrote by hand. The elevator example is the real case: it once had a check_safety_invariant function that walked the state pushing human-readable strings onto a violations list —\n\nif !found_person\n    push!(violations, \"Elevator $eidx has button $floor_button pressed but no passenger going there\")\nend\n\nThat validator became eight named @invariants, one per sub-check, each a pure boolean of physical:\n\nperson location xor elevator     person destination in range\nperson elevator exists           elevator floor in range\nelevator buttons in range        pressed button has passenger\npassenger direction consistent   no ghost calls\n\nThe push!(violations, \"...\") string check above is now @invariant \"pressed button has passenger\". The payoff over the string-list validator: instead of a batch of messages at the end of a run, you get a throw at the exact step and event that first broke a named property — and, with a recorder, a replay command to stand at that moment. (The old validator stays in the source as the regression oracle for the port.)","title":"The port-from-validator story"},{"category":"section","location":"invariants/#A-worked-violation","page":"Declaring Invariants","text":"A minimal two-flag model makes the mechanics visible. Each cell has fields a and b; the safety property is \"a xor b\" — at most one nonzero. Tick (fast) sets a=1 on a fresh cell; Corrupt (slow) sets b=1. A cell that was already Ticked and is then Corrupted breaks the invariant, and the guilty write is exactly cell[idx].b.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nusing Random: Xoshiro\nimport ChronoSim: precondition, generators, enable, fire!\nimport ChronoSim: PolicyStack\n\nmodule TwinFlag\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nimport ChronoSim: precondition, generators, enable, fire!\n\nconst RATE_FAST = 100.0\nconst RATE_SLOW = 0.05\n\n@keyedby FlagCell Int64 begin\n    a::Int64\n    b::Int64\nend\n\n@observedphysical FlagBoard begin\n    cell::ObservedVector{FlagCell,Member}\nend\n\nfunction FlagBoard(n::Int)\n    cells = ObservedArray{FlagCell,Member}(undef, n)\n    for i in eachindex(cells)\n        cells[i] = FlagCell(0, 0)\n    end\n    return FlagBoard(cells)\nend\n\nstruct Tick <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::Tick, state) = state.cell[evt.idx].a == 0\nenable(::Tick, state, when) = (Exponential(1 / RATE_FAST), when)\nfire!(evt::Tick, state, when, rng) = (state.cell[evt.idx].a = 1; nothing)\n\nstruct Corrupt <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::Corrupt, state) = state.cell[evt.idx].b == 0\nenable(::Corrupt, state, when) = (Exponential(1 / RATE_SLOW), when)\nfire!(evt::Corrupt, state, when, rng) = (state.cell[evt.idx].b = 1; nothing)\n\nfunction init!(state, when, rng)\n    for i in eachindex(state.cell)\n        state.cell[i].a = 0\n        state.cell[i].b = 0\n    end\n    return nothing\nend\n\n@invariant \"a xor b\" function (physical)\n    all(c.a == 0 || c.b == 0 for c in physical.cell)\nend\n\n@invariant \"counts nonnegative\" function (physical)\n    all(c.a >= 0 && c.b >= 0 for c in physical.cell)\nend\nend # module\n\nusing .TwinFlag: FlagBoard, Tick, Corrupt","title":"A worked violation"},{"category":"section","location":"invariants/#Compose-the-recorder-underneath-the-checker","page":"Declaring Invariants","text":"Run under a PolicyStack with the RecordSkeleton first and CheckInvariants second. Order matters: the recorder must capture the prefix before the checker throws, so the violation can carry a replay command.\n\nrec = RecordSkeleton()\nsim = SimulationFSM(FlagBoard(1), [Tick, Corrupt];\n    rng=Xoshiro(1234),\n    policy=PolicyStack(rec, CheckInvariants(TwinFlag)))\nerr = try\n    ChronoSim.run(sim, TwinFlag.init!, (p, i, e, w) -> false)\n    nothing\ncatch e\n    e\nend\n\nCheckInvariants(TwinFlag) is constructed from the module holding the declarations; it errors at construction if the module registers no invariant. It is opt-in and debug/test-tier — a sim built without the policy checks nothing and costs nothing, so production callers leave it off and test suites turn it on.","title":"Compose the recorder underneath the checker"},{"category":"section","location":"invariants/#Reading-an-InvariantViolation","page":"Declaring Invariants","text":"showerror(err) prints the forensic readout:\n\nInvariantViolation: invariant \"a xor b\" is false\n  model    : Main.TwinFlag\n  declared : /path/to/TwinFlag.jl:57\n  step     : 2 (fires since init)\n  event    : (:Corrupt, 1)\n  when     : 11.136634449684795\n  guilty   : 1 address(es) written by this fire AND read by the invariant\n    (cell, 1, b)\n  reads    : 2 address(es) in the failing evaluation\n    (cell, 1, a)\n    (cell, 1, b)\n  replay   : replay(sim_factory, skeleton; upto=1)   # reproduces the state one step before the violation\nThe invariant held after the previous step; the writes above broke it.\n\nLine by line:\n\ninvariant \"a xor b\" is false and declared : …:57 — the failing property and where you wrote it. With one invariant per clause, the name alone says what broke.\nstep : 2 / event : (:Corrupt, 1) — the fire that broke it. The invariant held after step 1; Corrupt on cell 1 at step 2 broke it.\nguilty : … (cell, 1, b) — the guilty address: the one address this fire wrote and the invariant reads. This is the smoking gun — Corrupt set cell[1].b, and the invariant reads b. When several addresses changed, only those the invariant actually reads appear here.\nreads : (cell, 1, a), (cell, 1, b) — every address the invariant touched in the failing evaluation, the superset the guilty set is filtered from.\nreplay : replay(sim_factory, skeleton; upto=1) — because a RecordSkeleton shared the stack, the violation carries the exact replay call that reconstructs the state one step before the break. Feed it recorded_skeleton(rec) and the same factory replay takes, and you are standing at step 1, about to fire the event that breaks the invariant.","title":"Reading an InvariantViolation"},{"category":"section","location":"invariants/#whystopped-is-the-readout","page":"Declaring Invariants","text":"You do not have to hand-parse the exception. Pass it to whystopped, which turns a caught InvariantViolation into the same forensics plus each guilty address's last and prior writers:\n\nwhystopped(err)\n\nwhystopped: invariant \"a xor b\" is false\n  step   : 2 (fires since init)\n  event  : (:Corrupt, 1)\n  when   : 11.136634449684795\n  guilty : 1 address(es)\n    (cell, 1, b)\n      last writer  : step 2 (:Corrupt, 1) t=11.136634449684795\n      prior writer : init\n  replay : replay(sim_factory, skeleton; upto=1)\n\nThe writer chain — last writer : step 2 (:Corrupt, 1), prior writer : init — tells you Corrupt overwrote a value that had stood untouched since initialization. whystopped also has a plain-skeleton method for reading out a run that ended without a violation; see Debugging a simulation.","title":"whystopped is the readout"},{"category":"section","location":"invariants/#Failure-forms","page":"Declaring Invariants","text":"The readout degrades gracefully, and each form tells you what to fix:\n\nstep : 0 — the initializer itself violated the invariant. Fix init, not an event; replay reads n/a (re-run the initializer to reproduce the state).\nguilty : none identified — no address this fire wrote is read by the invariant. Either the invariant reads state the tracker cannot see (e.g. Param-wrapped fields), or the corruption predates this step. Use the replay command and step forward.\nreplay : no skeleton recorded — no RecordSkeleton in the stack. Compose PolicyStack(RecordSkeleton(), CheckInvariants(MyModel)) to get a replayable prefix.\nWARNING : … not a pure function — re-evaluation under read capture returned true: the invariant gave two answers on the same state. Make it a pure function of physical.\nArgumentError: no @invariant is registered for this module — the module has no declarations, or was not loaded before the policy was constructed.\n@invariant \"name\" … returned <T>, not Bool — an invariant body returned a non-boolean. An invariant must be a Bool-valued function of the physical state.","title":"Failure forms"},{"category":"section","location":"invariants/#What-to-do-with-the-answer","page":"Declaring Invariants","text":"The guilty address names the field to look at and the event names the fire! that wrote it — start your fix there.\nThe replay command puts you one step before the break so you can inspect the pre-violation state and run guard_clauses on the breaking event.\nAn invariant that fires in a test under CheckInvariants but is expensive is exactly why the policy is opt-in: keep it in the test suite, leave it out of production replicates.","title":"What to do with the answer"},{"category":"section","location":"invariants/#Related","page":"Declaring Invariants","text":"Runbook: Check model invariants every step and Read out why the run stopped — the mechanical references.\nRecording and replaying a run — the RecordSkeleton/replay machinery the replay command uses.\nModel checking a simulation — proves the same @invariants hold over every reachable state to a bounded depth, not just the states one run visited.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"elevator_tutorial/#Build-an-Elevator","page":"Build an Elevator","text":"This tutorial builds a model of one elevator serving a small building, event by event. It is longer than Getting Started because it deliberately runs into the situations a real model runs into: an event that concerns the whole system rather than one entity, a precondition that needs a helper function, one event's enabling rule defined in terms of another's, and a generator written by hand so you can see what the automatic ones replace. When you finish, you will have seen every technique needed to write a model of your own. The full two-car elevator that this model is reduced from lives in the ChronoSimExamples.jl repository, alongside an epidemic model, if you want larger worked examples.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nimport ChronoSim: precondition, enable, fire!, generators\nusing Distributions\nusing Random","title":"Build an Elevator"},{"category":"section","location":"elevator_tutorial/#The-state","page":"Build an Elevator","text":"The building has several people, one call button per floor, and a single elevator car. A person is on a floor (with zero meaning they are riding the car), has a destination, and may be waiting for the car. A call button is either requested or not.\n\n@keyedby Person Int64 begin\n    floor::Int64\n    destination::Int64\n    waiting::Bool\nend\n\n@keyedby Call Int64 begin\n    requested::Bool\nend\n\nThe people live in a fixed-length vector, since nobody enters or leaves the building during a run. The call buttons live in a dictionary keyed by floor number. The car itself is just two values, so rather than invent a struct for it we store its floor and door state as plain scalar fields, which the framework tracks at the granularity of the whole field.\n\n@observedphysical Building begin\n    people::ObservedVector{Person,Member}\n    calls::ObservedDict{Int64,Call,Member}\n    car_floor::Int64\n    car_open::Bool\n    floor_cnt::Int64\nend\n\nfunction make_building(people_count, floor_count)\n    people = ObservedArray{Person,Member}(undef, people_count)\n    for i in eachindex(people)\n        people[i] = Person(1, 1, false)\n    end\n    calls = ObservedDict{Int64,Call,Member}()\n    for floor in 1:floor_count\n        calls[floor] = Call(false)\n    end\n    return Building(people, calls, 1, false, floor_count)\nend\nnothing #hide","title":"The state"},{"category":"section","location":"elevator_tutorial/#A-first-event,-with-its-generator-written-by-hand","page":"Build an Elevator","text":"A person who is standing on their destination floor eventually decides to go somewhere else. To show what generators are, we write this first event the manual way: a plain precondition function plus a @conditionsfor block that tells the framework when the precondition is worth checking.\n\nstruct PickDestination <: SimEvent\n    person::Int64\nend\n\nfunction precondition(evt::PickDestination, building)\n    person = building.people[evt.person]\n    return person.floor != 0 && !person.waiting && person.destination == person.floor\nend\n\n@conditionsfor PickDestination begin\n    @reactto changed(people[who].floor) do building\n        generate(PickDestination(who))\n    end\n    @reactto changed(people[who].destination) do building\n        generate(PickDestination(who))\n    end\n    @reactto changed(people[who].waiting) do building\n        generate(PickDestination(who))\n    end\nend\n\nEach @reactto changed(...) rule names a pattern of state addresses. When any write matches the pattern, the rule runs with the wildcard (here who) bound to the index from the written address, and it proposes candidate events with generate. Proposals are cheap: each one is filtered through the precondition, so proposing too much wastes a boolean check, while proposing too little silently loses behavior.\n\nNotice that the three rules correspond exactly to the three fields the precondition reads. That is not a coincidence, and it is the reason the manual block is usually unnecessary: the generator is the precondition's read set, restated. Writing it by hand means keeping the two in sync forever. For the remaining events we will let the @precondition macro derive the generators, and you can mix the two styles freely across the events of one model, as we have just done.\n\nThe rest of PickDestination is its rate and its effect. People decide to travel with a mean waiting time of ten minutes, and the new destination is a uniformly chosen different floor. Note that fire! draws from the rng argument, never from a global generator, so that runs are reproducible.\n\nenable(evt::PickDestination, building, when) = (Exponential(10.0), when)\n\nfunction fire!(evt::PickDestination, building, when, rng)\n    person = building.people[evt.person]\n    choices = [f for f in 1:building.floor_cnt if f != person.floor]\n    person.destination = rand(rng, choices)\nend","title":"A first event, with its generator written by hand"},{"category":"section","location":"elevator_tutorial/#Calling-the-car","page":"Build an Elevator","text":"A person on a floor, not yet waiting, whose destination is elsewhere, presses the call button. This precondition is declared with @precondition, so its generators are derived: any change to a person's floor, destination, or waiting field proposes CallCar for that person, which is precisely the hand-written block above, produced mechanically.\n\nstruct CallCar <: SimEvent\n    person::Int64\nend\n\n@precondition function precondition(evt::CallCar, building)\n    person = building.people[evt.person]\n    return person.floor != 0 && !person.waiting && person.destination != person.floor\nend\n\nenable(evt::CallCar, building, when) = (Exponential(0.5), when)\n\nfunction fire!(evt::CallCar, building, when, rng)\n    person = building.people[evt.person]\n    person.waiting = true\n    building.calls[person.floor].requested = true\nend","title":"Calling the car"},{"category":"section","location":"elevator_tutorial/#Moving-the-car,-with-a-helper-function","page":"Build an Elevator","text":"The car should move when the doors are shut and somebody, somewhere, wants it to be somewhere else: a pressed call button on another floor, or a rider whose destination is another floor. That \"somebody, somewhere\" is a scan over the state, and it reads naturally as a helper function.\n\nA helper that receives state must be marked with @fragment. The mark changes nothing about how the function runs; it registers the function's body so that the @precondition analysis can look inside it. An unmarked helper that receives state is a macro-time error, because the analysis refuses to guess what a function it cannot see might read.\n\n@fragment function wants_car_elsewhere(calls, people, here)\n    for (floor, call) in calls\n        if call.requested && floor != here\n            return true\n        end\n    end\n    for p in eachindex(people)\n        if people[p].floor == 0 && people[p].destination != here\n            return true\n        end\n    end\n    return false\nend\n\nThe event itself has no fields, because it concerns the single shared car rather than any person.\n\nstruct MoveCar <: SimEvent end\n\n@precondition function precondition(evt::MoveCar, building)\n    return !building.car_open &&\n           wants_car_elsewhere(building.calls, building.people, building.car_floor)\nend\n\nenable(evt::MoveCar, building, when) = (Exponential(0.5), when)\n\nfunction fire!(evt::MoveCar, building, when, rng)\n    targets = Int[]\n    for (floor, call) in building.calls\n        call.requested && push!(targets, floor)\n    end\n    for p in eachindex(building.people)\n        person = building.people[p]\n        person.floor == 0 && push!(targets, person.destination)\n    end\n    isempty(targets) && return nothing\n    nearest = targets[argmin(abs.(targets .- building.car_floor))]\n    building.car_floor += sign(nearest - building.car_floor)\nend\n\nTwo things are worth noticing here. First, the scans inside the helper read people and calls at loop indices, so the derivation cannot tell from a changed address which MoveCar instance to propose — but MoveCar has no fields, so there is only one instance, and the derived trigger simply proposes it whenever any call or any person's floor or destination changes. Second, fire! also scans the state, and that needs no @fragment and no care at all, because only preconditions are analyzed; firing functions just run, and the framework records what they write.","title":"Moving the car, with a helper function"},{"category":"section","location":"elevator_tutorial/#Opening-the-doors","page":"Build an Elevator","text":"The car opens its doors when it is at a floor where the button is pressed, or where a rider wants to get off. Reading building.calls[building.car_floor] indexes a container by state rather than by an event field, which is fine: the derivation widens that trigger, meaning a change to any call button proposes the (single) OpenDoors event and the precondition decides.\n\n@fragment function rider_arriving(people, here)\n    for p in eachindex(people)\n        if people[p].floor == 0 && people[p].destination == here\n            return true\n        end\n    end\n    return false\nend\n\nstruct OpenDoors <: SimEvent end\n\n@precondition function precondition(evt::OpenDoors, building)\n    return !building.car_open && (\n        building.calls[building.car_floor].requested ||\n        rider_arriving(building.people, building.car_floor)\n    )\nend\n\nenable(evt::OpenDoors, building, when) = (Exponential(0.1), when)\n\nfunction fire!(evt::OpenDoors, building, when, rng)\n    building.car_open = true\n    building.calls[building.car_floor].requested = false\nend","title":"Opening the doors"},{"category":"section","location":"elevator_tutorial/#Boarding-and-exiting","page":"Build an Elevator","text":"These two are straightforward person-keyed events in the same style as CallCar. A waiting person on the car's floor boards while the doors are open; a rider whose destination is the car's floor steps out.\n\nstruct Board <: SimEvent\n    person::Int64\nend\n\n@precondition function precondition(evt::Board, building)\n    person = building.people[evt.person]\n    return building.car_open && person.waiting && person.floor == building.car_floor\nend\n\nenable(evt::Board, building, when) = (Exponential(0.05), when)\n\nfunction fire!(evt::Board, building, when, rng)\n    person = building.people[evt.person]\n    person.floor = 0\n    person.waiting = false\nend\n\nstruct Exit <: SimEvent\n    person::Int64\nend\n\n@precondition function precondition(evt::Exit, building)\n    person = building.people[evt.person]\n    return building.car_open && person.floor == 0 && person.destination == building.car_floor\nend\n\nenable(evt::Exit, building, when) = (Exponential(0.05), when)\n\nfire!(evt::Exit, building, when, rng) = building.people[evt.person].floor = building.car_floor","title":"Boarding and exiting"},{"category":"section","location":"elevator_tutorial/#Closing-the-doors,-by-asking-other-preconditions","page":"Build an Elevator","text":"The doors should close when nobody can board and nobody can exit. We could restate those two conditions inline, but they already exist: they are the preconditions of Board and Exit. A @precondition body may call another event's precondition directly, and the analysis follows the call, so the derived generators for CloseDoors know about every address that Board and Exit read. The any(... for ...) form is analyzed like a loop.\n\nstruct CloseDoors <: SimEvent end\n\n@precondition function precondition(evt::CloseDoors, building)\n    return building.car_open &&\n           !any(precondition(Board(p), building) for p in eachindex(building.people)) &&\n           !any(precondition(Exit(p), building) for p in eachindex(building.people))\nend\n\nenable(evt::CloseDoors, building, when) = (Weibull(2.0, 0.5), when)\n\nfire!(evt::CloseDoors, building, when, rng) = building.car_open = false\n\nOne ordering rule applies here: because CloseDoors refers to the preconditions of Board and Exit, those two must be declared with @precondition earlier in the file, which they were.","title":"Closing the doors, by asking other preconditions"},{"category":"section","location":"elevator_tutorial/#Running-the-model","page":"Build an Elevator","text":"trajectory = Tuple{Tuple,Float64}[]\nrecord(building, when, event, changed) = push!(trajectory, (clock_key(event), when))\n\nbuilding = make_building(4, 5)\nsim = SimulationFSM(\n    building,\n    [PickDestination, CallCar, MoveCar, OpenDoors, Board, Exit, CloseDoors];\n    seed=90210,\n    observer=record,\n)\nnothing #hide\n\nThe initializer writes every field that events depend on. Construction alone is not enough, because it is the recorded writes that propose the first candidate events: writing each person's fields proposes their PickDestination, and writing the car's fields proposes the car events.\n\nfunction settle_in(building, when, rng)\n    for i in eachindex(building.people)\n        floor = rand(rng, 1:building.floor_cnt)\n        building.people[i].floor = floor\n        building.people[i].destination = floor\n        building.people[i].waiting = false\n    end\n    building.car_floor = 1\n    building.car_open = false\nend\n\nstopping(building, step, event, when) = when > 120.0\n\nChronoSim.run(sim, settle_in, stopping)\nlength(trajectory)\n\nA couple of hours of building life. Here are the first dozen firings.\n\nfirst(trajectory, 12)","title":"Running the model"},{"category":"section","location":"elevator_tutorial/#Inspecting-what-the-derivation-built","page":"Build an Elevator","text":"Every derived event can print its triggers. MoveCar is the interesting one, because everything it depends on arrives through the helper.\n\nusing ChronoSim: derivation_report\nderivation_report(MoveCar)\n\nThe report shows widened triggers on the call buttons and on people's fields, plus triggers on the two scalar car fields. Widened proposals are filtered by the precondition, and if you want to know what that filtering costs, the framework will count it for you:\n\nusing ChronoSim: collect_generation_stats, generation_stats, reset_generation_stats!\n\nreset_generation_stats!()\ncollect_generation_stats(true)\ntrajectory2 = Tuple{Tuple,Float64}[]\nsim2 = SimulationFSM(\n    make_building(4, 5),\n    [PickDestination, CallCar, MoveCar, OpenDoors, Board, Exit, CloseDoors];\n    seed=90210,\n    observer=(b, t, e, c) -> push!(trajectory2, (clock_key(e), t)),\n)\nChronoSim.run(sim2, settle_in, stopping)\ncollect_generation_stats(false)\ngeneration_stats()\n\nEach event type lists how many candidates its generators proposed and how many passed their precondition. Where the ratio is large and the model is slow, a hand-written @conditionsfor with tighter proposals is the remedy, and the Generators page discusses that trade-off.\n\nNote also that the second run produced the same trajectory as the first, because the two simulations used the same seed:\n\ntrajectory2 == trajectory","title":"Inspecting what the derivation built"},{"category":"section","location":"elevator_tutorial/#Where-to-go-from-here","page":"Build an Elevator","text":"You have now used every mechanism a typical model needs: observed state in vectors, dictionaries, and scalar fields; derived generators; a hand-written generator; @fragment helpers; precondition reuse; and the inspection tools. The Events and Generators manual pages state precisely what each piece requires, and the models in ChronoSimExamples.jl show the same techniques at full scale, including an epidemic model whose population events (SIRVillage) make a good template for models of interacting individuals.\n\n\n\nThis page was generated using Literate.jl.\n\n","title":"Where to go from here"},{"category":"section","location":"lint_footprints/#Linting-a-model's-footprints","page":"Linting a Model","text":"lint reads the static write masks that @fire derives and the static guard-read masks that @precondition/@guard derive, intersects them, and reports where one event can write an address another event's guard reads. Its headline job is to catch a missed trigger — a write that can flip a precondition whose event has no generator on that address, so the event is never proposed and the trajectory silently diverges. Two historical ChronoSim bugs, the doors_open and StopElevator-direction trigger omissions in the elevator, were exactly this shape; lint catches both before any run.","title":"Linting a model's footprints"},{"category":"section","location":"lint_footprints/#What-interference-means-here","page":"Linting a Model","text":"GSMP events communicate only through physical state. An edge A → B says \"event A can write an address that event B's precondition reads\". At runtime, B is (re)proposed only when a generator of B reacts to a change — so if A writes an address that B reads but no trigger of B covers that address, A can enable B without B ever being proposed. That is the missed-proposal risk, and it is the one thing lint promises to find (in the sound direction).\n\nlint reports three edge classes plus a smell:\n\nwrite→guard — the interference graph above. An edge that no trigger of the reader covers is a WARNING; everything else is info.\nwrite→write — two event types whose write masks intersect (info, with the shared address). Not a bug per se; a hint about shared state.\nwrite→rate — NOT analyzed in v1. Every report prints a fixed line saying so. Enable-time (enable) reads are runtime-only in this version; the dependency network tracks them dynamically, and a future version will lower them.\ndead addresses — physical fields written by no event and read by no guard (info; a rate-only input such as a distance matrix appears here by design).","title":"What interference means here"},{"category":"section","location":"lint_footprints/#Reading-the-report","page":"Linting a Model","text":"Take the SIRVillage reference report (captured verbatim in the runbook):\n\nLintReport: 6 events\n  write→guard: 18 edges over 2 addresses (0 warnings in 0 groups, 18 info)\n  write→write: 17 shared-address pairs (info)\n  write→rate edges: not analyzed (enable-time reads are runtime-only in v1; the depnet tracks them dynamically)\n  dead addresses: none\n  unanalyzed guards: InitEvent    unanalyzed effects: none\n  caps: none\n\nwrite→guard line — 18 interference edges over 2 addresses: 16 on [actors, ℤ, state] (Infect/Recover/Reset/InitEvent writing the state field the four guards read) and 2 on [actors, ℤ, haunt] (InitEvent/Travel writing the haunt field Infect's guard reads). All info: every reader has a changed(actors[who].state) (or .haunt) trigger that covers the write, so no proposal is missed. When there are warnings, they are grouped by (reader, address) with the writer list inline, so the elevator's 28 warning edges read as 15 groups.\nwrite→write line — 17 co-writer pairs, spread over actors (state/strain/haunt), locations, strains, and next_strain_id — actors.strain appears here, not in write→guard (no guard reads it). Informational.\nrate line — the honest gap. SIRVillage's Mutate → Infect rate dependence (Mutate writes strains[*].infectivity, Infect's enable reads it) appears in NO edge; this line is the disclosure.\ndead addresses — none here; landspread's distance matrix (read only in enable) is the canonical hit.\nunanalyzed guards / effects — events the lint could not read: no @guard/@precondition (guards) or no @fire (effects). InitEvent has no precondition, so it is listed — nothing is silently assumed harmless.\ncaps — every skipped or capped analysis prints a line. none means the live physical instance let every finite index domain be enumerated.\n\nshow prints this bounded summary (≤ ~30 lines). print_lint(io, report) prints every edge, one greppable line each, for CI logs.","title":"Reading the report"},{"category":"section","location":"lint_footprints/#Opting-a-hand-written-model-in","page":"Linting a Model","text":"Derived events (@precondition) already expose their reads through derivation_spec, and their generators react to exactly those reads, so a derived reader can never miss a trigger — it needs nothing.\n\nA hand-written model, whose generators are @conditionsfor/@reactto blocks, opts in by:\n\nprefixing each precondition with @guard — this emits the precondition verbatim (runtime behavior is byte-identical, proven by the differential test suite) and bakes its static read specification for the lint, WITHOUT deriving generators (the hand-written triggers stay in charge);\nmarking each state-receiving helper with @fragment so @guard can see the reads the helper performs.\n\nUnlike @precondition, @guard tolerates a zero-read body (precondition(evt, state) = true yields an empty spec) and records a whole-container read (length/keys) as a container-level pattern instead of demanding a covering trigger — because catching an uncovered whole-container read is precisely what the lint is for.","title":"Opting a hand-written model in"},{"category":"section","location":"lint_footprints/#The-allowlist","page":"Linting a Model","text":"A warning is not always a bug. A hand-written model often narrows its triggers deliberately: the reader is re-proposed through a correlated trigger in every reachable state, so a narrower trigger set is sound even though the mask analysis cannot see the reachability argument. Those intended narrowings go in a LintAllow vector passed to assert_lint_clean:\n\nChronoSim.assert_lint_clean(report; allow=[\n    ChronoSim.LintAllow(reader=:PickNewDestination, mask=\"[person, ℤ, waiting]\",\n                        reason=\"waiting is cleared with location; re-triggers on location\"),\n])\n\nEach LintAllow matches by reader/writer/mask (any field nothing is a wildcard) and carries a mandatory reason. The mask string is exactly what the report prints, so entries are grep-copyable from a failing report. An allow entry that matches no warning prints a staleness notice (it does not fail) — so the allowlist decays loudly when a narrowing is fixed. The hand-written elevator's 15-entry allowlist (one per warning group) is the worked example, reviewed once and kept in its test file.","title":"The allowlist"},{"category":"section","location":"lint_footprints/#What-static-analysis-can-and-cannot-see","page":"Linting a Model","text":"The honest-scope commitments (binding):\n\nMasks only. lint sees address masks — container, index kind, leaf field — not guard truth values, not arithmetic on indices, not relational constraints between index positions. It cannot tell that two events touch disjoint elements unless the index constraints are literal or finitely enumerable.\nNo expression semantics, no SMT. The optional Satisfiability stretch is dropped; the model checker subsumes it.\nNo rate (enable) reads. Write→rate edges are not analyzed in v1, stated in every report.\nEnumeration is finite and capped. With a live physical instance, a :possible index intersection is refined by enumerating the inferred finite domain (@domain, container keys, finite field types) with a hard enum_cap; a provably empty overlap demotes the edge to info (the edge stays in the report). Every skipped or capped domain prints a caps: line — never silent.\nWarnings are over-approximate (hence the allowlist); info edges are not proofs of interaction. The one guarantee is the soundness direction: a genuinely interfering pair always appears as an edge; only provably disjoint pairs are dropped. (The converse does NOT hold — an edge is not proof of real interference; mask analysis cannot deliver no-false-edges.) That direction is tested in CI as static ⊇ dynamic — a LintHarvest policy records every runtime enable-dependency edge on a smoke run and static_covers_dynamic asserts the static report covers all of them.","title":"What static analysis can and cannot see"},{"category":"section","location":"lint_footprints/#Related","page":"Linting a Model","text":"Runbook: Lint a model's footprints.\nStatic effect analysis (@fire) — where the write masks lint reads come from.\nDebugging a simulation — the runtime why-verbs that catch a missed trigger lint would flag statically.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"intro/#Introduction","page":"Introduction","text":"This page explains the ideas you need before you write a model. Every simulation framework has a way to decide what happens next, and understanding ChronoSim's way — preconditions, competing clocks, and observed state — makes everything else in the documentation follow naturally.","title":"Introduction"},{"category":"section","location":"intro/#Events-are-enabled-by-preconditions","page":"Introduction","text":"In ChronoSim, an event is a small value, such as Move(agent=7, direction=Up), together with a handful of functions that give it meaning. The most important of these is the precondition, which looks at the current state and answers one question: could this event happen right now? Here is a precondition for a movement event, which says an agent may move in some direction only when the square it would move into is empty.\n\nfunction precondition(event::Move, state)\n    loc = state.agents[event.agent].location\n    return state.location[loc + event.direction] == 0\nend\n\nThe moment a precondition becomes true, the event becomes enabled. An enabled event will fire at some future time, and that time is drawn from a probability distribution that you supply through the enable function.\n\nenable(event::Move, state, time) = (Weibull(2, 1.0), time)\n\nThis says that the waiting time between enabling the event and firing it follows a Weibull distribution. Every enabled event holds its own running clock like this, and the framework always fires the event whose clock rings first. If, before the clock rings, some other event changes the state so that the precondition becomes false, the event is disabled and its clock is discarded. There is no possibility of a stale event firing out of a state where it no longer makes sense.","title":"Events are enabled by preconditions"},{"category":"section","location":"intro/#The-framework-does-not-scan-every-event","page":"Introduction","text":"It may sound as though the framework must check every precondition every time anything fires, and in a naive implementation it would. ChronoSim avoids the scan by requiring each event type to have generators: rules that say which changes of state make it worth checking this event's precondition at all. You can write generators by hand with the @conditionsfor macro:\n\n@conditionsfor Move begin\n    @reactto changed(agents[who].location) do state\n        for direction in ALLDIRECTIONS\n            generate(Move(who, direction))\n        end\n    end\nend\n\nThis generator says that whenever any agent's location field changes, the framework should propose Move events for that specific agent in every direction. Proposed events are only candidates. Each one still has its precondition checked, so a generator is allowed to propose too much; the cost of an extra proposal is one wasted precondition call. What a generator must never do is propose too little, because an event that is never proposed can never become enabled, and the failure is silent.\n\nBecause the generator is really just the precondition's reads written backwards, ChronoSim can usually write it for you. If you declare the precondition with the @precondition macro instead of as a plain function, the framework analyzes the body and derives the generators itself:\n\n@precondition function precondition(event::Move, state)\n    loc = state.agents[event.agent].location\n    return state.location[loc + event.direction] == 0\nend\n\nFor most events this removes the second artifact entirely, and it removes the class of bugs where the precondition and its generators drift apart. The Generators page explains when the derivation applies, when you still write @conditionsfor by hand, and how to write preconditions so that the derivation does a precise job.","title":"The framework does not scan every event"},{"category":"section","location":"intro/#The-state-is-observed","page":"Introduction","text":"Both of the mechanisms above depend on the framework knowing which parts of the state are touched. ChronoSim therefore asks you to build your simulation state from observed containers, declared with the @observedphysical and @keyedby macros. Every field of the state then has an address, which is a tuple naming the path to it, and every read or write of a field is recorded under that address. When a firing function runs,\n\nfunction fire!(event::Move, state, when, rng)\n    state.location[state.agents[event.agent].location] = 0\n    state.agents[event.agent].location += event.direction\n    state.location[state.agents[event.agent].location] = event.agent\nend\n\nthe framework records that addresses like (:location, 12) and (:agents, 7, :location) were written. It compares those writes against the recorded reads of every enabled event's precondition, so it knows exactly which events to re-examine, and it hands the writes to the generators, so it knows exactly which new events to propose. Nothing else in the simulation is looked at. The Simulation State page shows how to declare state, and it summarizes the small contract the containers uphold so that this tracking is trustworthy.","title":"The state is observed"},{"category":"section","location":"intro/#What-you-write-for-each-event","page":"Introduction","text":"To define one event type in a ChronoSim model, you provide the following pieces.\n\nAn immutable struct that subtypes ChronoSim.SimEvent. Its fields identify the actors and resources involved, so they are usually integers, symbols, enums, or strings. The struct can also be empty when the event concerns the system as a whole.\nA precondition function returning Bool. Declaring it with @precondition also produces the event's generators; declaring it plain means you must also write a @conditionsfor block by hand.\nAn enable function that returns a distribution from Distributions.jl and the time from which that distribution is measured. There is an optional reenable for events whose rate should be recomputed when the state changes underneath them.\nA fire! function that changes the state when the event happens.","title":"What you write for each event"},{"category":"section","location":"intro/#Costs-and-payoffs","page":"Introduction","text":"It is fair to say that this is more structure than a small hand-written simulation needs, and the learning curve arrives all at once rather than gradually. When a model misbehaves, there are several distinct pieces — precondition, generator, rate, firing — where the mistake could live. The compensation is that each piece is small, single-purpose, and checkable on its own, and the framework can verify the pieces against each other: the set of preconditions defines exactly which events should be enabled in any state, which makes a strong global correctness check, and derived generators eliminate the most silent failure mode outright.\n\nThe structure also pays for itself in ways that are hard to retrofit into a hand-written loop. Events that stop making sense are cancelled exactly, not ignored later, so there are no ghost firings. Independent models compose by sharing state, with no glue code, because enabling flows through the state rather than through direct calls. And because a ChronoSim model defines a proper stochastic process, the framework can compute the log-likelihood of a trajectory, which opens the door to model fitting, Bayesian inference, and rare-event methods.\n\n","title":"Costs and payoffs"},{"category":"section","location":"observedmacro/#The-@obsread-and-@obswrite-Macros","page":"The @obsread and @obswrite Macros","text":"","title":"The @obsread and @obswrite Macros"},{"category":"section","location":"observedmacro/#When-you-would-want-these","page":"The @obsread and @obswrite Macros","text":"The observed containers described on the previous page do all of the recording for you, and they are the right choice for almost every model. Sometimes, though, a simulation needs a data structure that the observed containers do not offer — a specialized matrix type, a third-party spatial index, or a plain array that you want to use for performance reasons. In that situation you can hold the raw structure in the state and take over the recording duty yourself with two macros: @obsread marks a read, and @obswrite marks a write.","title":"When you would want these"},{"category":"section","location":"observedmacro/#How-to-use-them","page":"The @obsread and @obswrite Macros","text":"Declare the state with @observedphysical as usual, so that it has the machinery for collecting reports, but give it whatever field types you need.\n\n@observedphysical Fireflies begin\n    watersource::Matrix{Float64}\n    wind::Float64\n    cnt::Int64\nend\n\nThere is no observed container wrapped around watersource here, so nothing records access to its elements automatically. Instead, every place your events read or write it, you say so explicitly.\n\nvalue = @obsread fireflies.watersource[i, j]\n@obswrite fireflies.wind = 2.7\n\nThe first line records a read of the address (:watersource, (i, j)) and evaluates to the value. The second line performs the assignment and records a write of (:wind,).","title":"How to use them"},{"category":"section","location":"observedmacro/#The-responsibility-you-are-taking-on","page":"The @obsread and @obswrite Macros","text":"When you use these macros, the correctness of the simulation rests on your discipline. Every read that a precondition or enable function performs on the raw structure must go through @obsread, and every write that a fire! function performs must go through @obswrite. A missed write means some event that depends on that value will not be re-examined when it changes, and a missed read means an event will not wake up when the value it tested changes later. Both failures are silent, which is exactly the class of bug the observed containers exist to prevent, so prefer the containers wherever they fit.\n\nBe aware of two further limitations. The macros only understand direct field access and indexing, so a mutating call such as push! on a raw vector is invisible to them. And the @precondition derivation machinery analyzes container access syntax, so preconditions that go through @obsread are better served by hand-written @conditionsfor generators.\n\n","title":"The responsibility you are taking on"},{"category":"section","location":"plan/#Plan","page":"Plan","text":"note: Historical design note\nThis page records the framework's original design thinking. Some code sketches here (the @simulation macro, sampler = CombinedNextReaction) were never implemented or predate the current API; treat them as intent, not as usage guidance. For current usage see Events and the runbook.\n\nThis simulation framework asks a question: How can we simulate with time-varying hazards and get full expressiveness and statistical capability?","title":"Plan"},{"category":"section","location":"plan/#Problems-We-Want-to-Solve","page":"Plan","text":"GSPN requires building a large graph before simulating, and creating versions of a simulation requires creating versions of those graphs. It is painstaking.\nGSMP historically use Exponential distributions or have limited ideas of what a hazard rate can be and a limited definition of state. But this IS a GSMP by its statistical definition.\nChemical simulation with time-varying hazards does happen. It's just restricted to particular forms for the hazard rates and a limited definition of state (counts of chemicals). It's not wrong, but it doesn't do arbitrary, real-valued state.","title":"Problems We Want to Solve"},{"category":"section","location":"plan/#This-framework-has-an-opinion","page":"Plan","text":"Which events are enabled and disabled will be mediated by changes to a state. This is in contrast to a simulation where a firing function, within that function, will enable and disable events.\nEvents will be created dynamically based on the state. There is a way to avoid construction of the bipartite graph of the Petri net.\n\nBoth of these points exist to make it easier to compose simulations. They also enable us to use a simulation as a statistical evaluator of likelihoods of trajectories. We will, for instance, be able to modify a simulation by specifying a list of events to include in the simulation, substituting one event for another.","title":"This framework has an opinion"},{"category":"section","location":"plan/#This-framework-asks-questions","page":"Plan","text":"Is there a simulation component that will update state and clocks correctly in a way that helps write simulations but doesn't limit the kinds of uses? I mean is there some set of features we want in the state update that supports different applications like chemical simulation, disease simulation, reliability, etc?\nHow can we define simulation state in a way that is a) observable by the framework and b) can be transactional so we can roll back changes to the state?\nWhat are design patterns for dynamic generation of events from the state?\nHow can we make a framework that can be the base for a problem-specific DSL made with macros?","title":"This framework asks questions"},{"category":"section","location":"plan/#Why-use-this?","page":"Plan","text":"Curiosity to see how well/badly it works.\nGet ideas to do something different.\nYou have a sincerely difficult simulation problem, and this is the best expression of it.","title":"Why use this?"},{"category":"section","location":"plan/#Current-Capabilities","page":"Plan","text":"<iframe src=\"assets/d3_event_diagram.html\" style=\"width: 100%; height: 500px; border: none;\"></iframe>","title":"Current Capabilities"},{"category":"section","location":"plan/#Features","page":"Plan","text":"Rule-based events\nSampling methods\nDirac delta function times (for ODEs)\nDeterministic once random seed is set.\nRe-enabling of events\nRules that depend on events instead of just states.\nMacro and struct for event(key)\nObservers on events\nObservers of state changes\nImmediate events","title":"Features"},{"category":"section","location":"plan/#Examples","page":"Plan","text":"Elevators with TLA+.\nReliability with a set of trucks.\nSIR with strain mutation and individual movement models.\nMovement through a 2D domain for particle filtering.","title":"Examples"},{"category":"section","location":"plan/#Known-bugs","page":"Plan","text":"Observe macro needs hygiene to evaluate methods in defining context.","title":"Known bugs"},{"category":"section","location":"plan/#Future-Capabilities","page":"Plan","text":"","title":"Future Capabilities"},{"category":"section","location":"plan/#Features-2","page":"Plan","text":"Clarify in docs that this is a model + propagator, not a framework.\nImportance sampling\nfire!() uses probabilistic programming to get complete likelihood.\nThe user can supply a list of all events in order to build a complete depnet before simulating.\nPregeneration of all rule-based events.\nTransactional firing (for estimation of derivatives)\nMCMC sampling from trajectories\nSimulate backwards in time.","title":"Features"},{"category":"section","location":"plan/#Example-Simulations","page":"Plan","text":"Move, infect, age, birth.\nPolicy-driven movement.\nQueuing model.\nChemical equations.\nDrone search pattern with geometry.\nHMC for house-to-house infestation.\nJob shop problem.\nCars driving on a map.","title":"Example Simulations"},{"category":"section","location":"plan/#Example-Uses-of-Simulations","page":"Plan","text":"Hook into standard Julia analysis tools.\nSampling rare events.\nParameter fitting to world data.\nOptimization of parameters to minimize a goal function.\nHMC on trajectories to find a most likely event stream.\nPOMDP","title":"Example Uses of Simulations"},{"category":"section","location":"plan/#Performance-Questions","page":"Plan","text":"How stable can I make the type system in the running simulation? It uses Events in places and tuples in others.\nThe TrackedEntry needs to be timed and gamed.\nCould the TrackedEntry be an N-dimensional array? Could each entry be an array? A dictionary?\nCan the main simulation look over the keys to determine types before it instantiates?\nThe depnet is absolutely wrong for the current main loop. It might be closer to right for another mainloop. Should try various implementations.\nMeasure performance with profiling. Look for the memory leaks.","title":"Performance Questions"},{"category":"section","location":"plan/#Reframing-as-propagator","page":"Plan","text":"Three concepts around which to structure:\n\nEvent Propagation: Given current state and enabled events, calculate the probability distribution over next events\nLikelihood Calculation: Compute the exact probability of observed event sequences (for model fitting)\nCompositional Integration: Use as a component within larger statistical or numerical analyses","title":"Reframing as propagator"},{"category":"section","location":"plan/#Main-Pain-Points","page":"Plan","text":"The central idea is that a Model is a set of events mediated by their interaction with state. We insist on this central idea by forbidding a user from creating events in a firing function. It must always be the case that, given a simulation state, it is possible to apply events to it and figure out which are or aren't enabled. This is what makes the system a weak Markov system. It's a limitation that we should be able to turn into a strength. I'm not sure how though.\n\nThere is a tenuous connection from an updated state to the generators that see that state and connect it to events.\nOne debugging move: Always execute every enabling invariant at every step. If one is a yes but its generator didn't fire, then that's a problem.\nThere are at least five separate definitions to create an Event. That's a lot to do. Is it easy to understand or can we reduce complexity?\nI know from experience that debugging these simulations is very difficult. Is there some idea I'm missing that would elucidate why some event failed to fire or why an event fires when you don't think it should?\nA fired, so why wasn't B enabled? This is hard to track because A affects state, and maybe B doesn't see that state.\nWe could have the address of everything written and read. Relate it to what's in the network, like a local graph.","title":"Main Pain Points"},{"category":"section","location":"plan/#Improvements-to-the-Framework-User-Interface","page":"Plan","text":"This section is from conversations with AI about the spectrum of ways to improve the user interface. There were some good ideas in there so they are recorded here.","title":"Improvements to the Framework User Interface"},{"category":"section","location":"plan/#The-Five-parts","page":"Plan","text":"Component Role\nEvents The SimEvent interface (precondition, enable, reenable, fire!, isimmediate)\nGenerators The reactive system that creates events from state changes or other events (@reactto, @conditionsfor)\nObservable State Containers that track reads/writes (ObservedArray, ObservedDict, etc.)\nDependency Network Bidirectional graph: which events depend on which state \"places\"\nSimulationFSM The orchestrator that coordinates all of the above with CompetingClocks\n\nWhere does complexity live today? The plan.md lists many UX improvements (macros, traits, templates). Is the current interface genuinely\n\ndifficult, or is it just verbose? There's a difference between \"hard to understand\" and \"tedious to write.\"\n\nThe Observer pattern: You have observers on state and observers on events (the observer callback in SimulationFSM). How do users think about\n\nthese? Are they fundamentally different, or could they unify?\n\nLikelihood calculation: The trace_likelihood and steploglikelihood integration suggests you're targeting inference/estimation workloads. Is that\n\na primary use case or secondary?","title":"The Five parts"},{"category":"section","location":"plan/#generator-functions-use-do-function-syntax-so-make-it-easier.","page":"Plan","text":"Create a macro for generator functions that looks like you call generate(event) but really calls a do-function callback underneath.","title":"generator functions use do-function syntax so make it easier."},{"category":"section","location":"plan/#Simplify-enabling/reenabling","page":"Plan","text":"Ask the simulation to define the distribution and when but not the sampler, rng, or clock_key.","title":"Simplify enabling/reenabling"},{"category":"section","location":"plan/#Explicitly-register-functions","page":"Plan","text":"In framework.jl - automatic method generation   function registerevent(eventtype::Type{<:SimEvent}, spec::EventSpec)       # Generate precondition, generators, enable, fire! automatically       # Based on declarative specification   end","title":"Explicitly register functions"},{"category":"section","location":"plan/#Macro-to-say-what-generators-trigger-on","page":"Plan","text":"Framework provides path builder   @watches actors[].state  # Instead of [:actors, ℤ, :state]   @watches board[].occupant","title":"Macro to say what generators trigger on"},{"category":"section","location":"plan/#Put-common-event-patterns-into-template-structs","page":"Plan","text":"Framework could provide base types:\n\nActorEvent{T} - for single-actor events\nInteractionEvent{T} - for multi-actor events\nScheduledEvent{T} - for time-based events\nStateTransitionEvent{T} - for state machine transitions\n\nor make it a function:   Framework provides factory for common patterns   createstatetransitionevent(       :Break,       fromstate = :working,       tostate = :broken,       ratefield = :faildist,       agetracking = true   )","title":"Put common event patterns into template structs"},{"category":"section","location":"plan/#Put-common-enabling-patterns-into-template-structs","page":"Plan","text":"Common patterns built into framework   abstract type RateModel end   struct ConstantRate <: RateModel; dist; end   struct ActorRate <: RateModel; field::Symbol; end   struct TimeBasedRate <: RateModel; calc::Function; end","title":"Put common enabling patterns into template structs"},{"category":"section","location":"plan/#Help-build-the-simulation-itself","page":"Plan","text":"In framework.jl   @simulation MySimulation begin       state_type = IndividualState       events = [StartDay, EndDay, Break, Repair]       sampler = CombinedNextReaction\n\n  initialize = function(physical, rng)\n      # initialization code\n  end\n\n  stop_when = (physical, step, event, when) -> when > days\n\nend","title":"Help build the simulation itself"},{"category":"section","location":"plan/#Make-tools-with-which-to-make-simulation-DSLs","page":"Plan","text":"  # Framework should export these primitives\n  export create_generator, register_precondition, add_rate_function\n  export EventSpecification, GeneratorSpec, RateSpec\n\n  # So users can build their own DSLs:\n  macro my_reliability_event(name, spec)\n      quote\n          struct $(esc(name)) <: ActorEvent{IndividualState}\n              actor_idx::Int\n          end\n\n          # Use framework primitives\n          register_precondition($(esc(name)), $(spec.precondition))\n          add_rate_function($(esc(name)), $(spec.rate))\n      end\n  end","title":"Make tools with which to make simulation DSLs"},{"category":"section","location":"plan/#Use-traits-more-than-inheritance","page":"Plan","text":"Maybe both traits and hooks, where a user registers a function to call for a particular event.\n\n  # Framework defines traits\n  abstract type EventTrait end\n  struct HasActor <: EventTrait end\n  struct HasSchedule <: EventTrait end\n  struct HasInteraction <: EventTrait end\n\n  # Users can mix traits freely\n  event_traits(::Type{<:SimEvent}) = ()\n  event_traits(::Type{Break}) = (HasActor(), HasSchedule())\n\n  # Framework dispatches on traits\n  function generate_precondition(evt::Type{T}) where T\n      traits = event_traits(T)\n      # Compose behavior from traits\n  end\n\nThis could also help the functions on events.\n\n  # Instead of storing functions, use traits\n  abstract type PreconditionTrait end\n  struct StateCheck{S} <: PreconditionTrait\n      required_state::S\n  end\n\n  struct EventConfig{P <: PreconditionTrait}\n      precondition_trait::P\n  end\n\n  # Fast dispatch\n  @inline function check_precondition(evt::ActorEvent, physical, ::StateCheck{S}) where S\n      physical.actors[evt.actor_idx].state == S\n  end","title":"Use traits more than inheritance"},{"category":"section","location":"plan/#Make-Syntax-Trees-Accessible","page":"Plan","text":"  # If framework uses macros, expose the AST\n  macro framework_helper(expr)\n      ast = parse_event_ast(expr)\n      # Let users transform it\n      transformed = apply_user_transforms(ast)\n      return generate_code(transformed)\n  end\n\n  # Users can register transforms\n  register_ast_transform!(my_reliability_transform)","title":"Make Syntax Trees Accessible"},{"category":"section","location":"plan/#Macro-advice","page":"Plan","text":"Macro Design Best Practices\n\nAVOID These Patterns:\n\n  # 1. Rigid syntax requirements\n  @framework_event name::Type = value  # Forces specific syntax\n\n  # 2. Closed evaluation contexts\n  @framework_event Break begin\n      eval(:(struct Break ... end))  # Evaluates in framework module\n  end\n\n  # 3. Monolithic macros\n  @define_entire_event Break working broken fail_dist ...\n\nPREFER These Patterns:\n\n  # 1. Composable macro fragments\n  @event_struct Break actor_idx::Int\n  @event_precondition Break (evt, phys) -> phys.actors[evt.actor_idx].state == working\n  @event_rate Break (evt, phys) -> phys.params[evt.actor_idx].fail_dist\n\n  # 2. Pass-through to user context\n  macro framework_helper(name, user_expr)\n      quote\n          # Evaluate in caller's context\n          local user_result = $(esc(user_expr))\n          framework_process($(QuoteNode(name)), user_result)\n      end\n  end\n\n  # 3. Metadata-based approach\n  @event_metadata Break begin\n      traits = [:actor_based, :state_transition]\n      watches = [:actors]\n      # Users can add custom metadata\n  end","title":"Macro advice"},{"category":"section","location":"plan/#Sample-Implementation","page":"Plan","text":"","title":"Sample Implementation"},{"category":"section","location":"plan/#Of-a-framework-that-enables-user-DSLs","page":"Plan","text":"  # Framework provides:\n    module ChronoSim\n\n  # Low-level registration API\n  function register_event_type(T::Type, config::EventConfig)\n      # Store in global registry\n  end\n\n  # Composable specifications\n  # This should use parametric types\n  struct EventConfig{P,G,E,F}\n      precondition::Union{Function, Nothing}\n      generators::Vector{GeneratorSpec}\n      enable::Union{Function, Nothing}\n      fire::Union{Function, Nothing}\n      metadata::Dict{Symbol, Any}\n  end\n\n  # Give the parametric event config a solid constructor.\n  function actor_event_behavior(;\n      required_state::Symbol,\n      rate_field::Symbol,\n      fire_action::Function\n  )\n      EventBehavior(\n          # Specialized, inlinable functions\n          (evt, physical) -> getfield(physical.actors[evt.actor_idx], :state) == required_state,\n          (evt, sampler, physical, when, rng) -> enable!(\n              sampler,\n              clock_key(evt),\n              getfield(getfield(physical.params[evt.actor_idx], rate_field)),\n              when, when, rng\n          ),\n          fire_action,\n          default_actor_generators()\n      )\n  end\n\n  end # module\n\n  # User's DSL:\n  module ReliabilityDSL\n    using ChronoSim\n\n  macro reliability_event(name, from, to, rate_field)\n      quote\n          struct $(esc(name)) <: SimEvent\n              actor_idx::Int\n          end\n\n          config = actor_event_config(\n              precondition_state = $(esc(from)),\n              rate_distribution = evt -> evt.physical.params[evt.actor_idx].$rate_field,\n              fire_action = (evt, phys, when) -> begin\n                  phys.actors[evt.actor_idx].state = $(esc(to))\n                  # Custom reliability logic here\n              end\n          )\n\n          register_event_type($(esc(name)), config)\n      end\n  end\n\n  # Clean syntax for users\n  @reliability_event Break working broken fail_dist\n  @reliability_event Repair broken ready repair_dist\n\n  end # module","title":"Of a framework that enables user DSLs"},{"category":"section","location":"plan/#Of-an-ActorEvent{T}","page":"Plan","text":"  abstract type ActorEvent{T} <: SimEvent end\n\n  # Default implementation that concrete types can override\n  actor_index(evt::ActorEvent) = evt.actor_idx\n  actor_collection(::Type{<:ActorEvent{T}}) where T = :actors\n  actor_state_field(::Type{<:ActorEvent{T}}) where T = :state\n\n  # Generic precondition - can be overridden\n  function precondition(evt::E, physical) where E <: ActorEvent\n      actor_idx = actor_index(evt)\n      checkbounds(Bool, getfield(physical, actor_collection(E)), actor_idx) || return false\n\n      # Allow custom precondition logic\n      actor_precondition(evt, physical)\n  end\n\n  # Subtype must implement this\n  actor_precondition(evt::ActorEvent, physical) =\n      error(\"Must implement actor_precondition for $(typeof(evt))\")\n\n  # Generic generators for any ActorEvent\n  function generators(::Type{E}) where E <: ActorEvent{T} where T\n      collection = actor_collection(E)\n      state_field = actor_state_field(E)\n\n      return [\n          EventGenerator(\n              ToPlace,\n              [collection, ℤ, state_field],\n              function (f::Function, physical, actor)\n                  evt = try_create_event(E, actor, physical)\n                  !isnothing(evt) && f(evt)\n              end\n          )\n      ]\n  end\n\n  # Helper to create event if valid\n  try_create_event(::Type{E}, actor_idx, physical) where E <: ActorEvent = E(actor_idx)\n\n  # Generic enable with rate lookup\n  function enable(evt::E, sampler, physical, when, rng) where E <: ActorEvent\n      rate_dist = get_rate_distribution(evt, physical)\n      enable_time_args = get_enable_times(evt, physical, when)\n      enable!(sampler, clock_key(evt), rate_dist, enable_time_args..., rng)\n  end\n\n  # Default reenable delegates to enable\n  function reenable(evt::E, sampler, physical, first_enabled, curtime, rng) where E <: ActorEvent\n      rate_dist = get_rate_distribution(evt, physical)\n      reenable_time_args = get_reenable_times(evt, physical, first_enabled, curtime)\n      enable!(sampler, clock_key(evt), rate_dist, reenable_time_args..., rng)\n  end\n\n  # Subtype must implement rate lookup\n  get_rate_distribution(evt::ActorEvent, physical) =\n      error(\"Must implement get_rate_distribution for $(typeof(evt))\")\n\n  # Default time arguments\n  get_enable_times(evt::ActorEvent, physical, when) = (when, when)\n  get_reenable_times(evt::ActorEvent, physical, first_enabled, curtime) = (first_enabled, curtime)\n\nAnd what it does to the simulation code:\n\n struct Break <: ActorEvent{IndividualState}\n      actor_idx::Int\n  end\n\n  # Only need to specify unique behavior\n  actor_precondition(evt::Break, physical) =\n      physical.actors[evt.actor_idx].state == working\n\n  get_rate_distribution(evt::Break, physical) =\n      physical.params[evt.actor_idx].fail_dist\n\n  # Custom time calculation for non-memoryless distributions\n  get_enable_times(evt::Break, physical, when) =\n      (when - physical.actors[evt.actor_idx].work_age, when)\n\n  function fire!(evt::Break, physical, when, rng)\n      physical.actors[evt.actor_idx].state = broken\n      started_work = physical.actors[evt.actor_idx].started_working_time\n      physical.actors[evt.actor_idx].work_age += when - started_work\n  end\n\n  # EndDay is even simpler\n  struct EndDay <: ActorEvent{IndividualState}\n      actor_idx::Int\n  end\n\n  actor_precondition(evt::EndDay, physical) =\n      physical.actors[evt.actor_idx].state == working\n\n  get_rate_distribution(evt::EndDay, physical) =\n      physical.params[evt.actor_idx].done_dist\n\n  function fire!(evt::EndDay, physical, when, rng)\n      physical.actors[evt.actor_idx].state = ready\n      started_work = physical.actors[evt.actor_idx].started_working_time\n      physical.actors[evt.actor_idx].work_age += when - started_work\n  end\n\n  # Repair\n  struct Repair <: ActorEvent{IndividualState}\n      actor_idx::Int\n  end\n\n  actor_precondition(evt::Repair, physical) =\n      physical.actors[evt.actor_idx].state == broken\n\n  get_rate_distribution(evt::Repair, physical) =\n      physical.params[evt.actor_idx].repair_dist\n\n  function fire!(evt::Repair, physical, when, rng)\n      physical.actors[evt.actor_idx].state = ready\n      physical.actors[evt.actor_idx].work_age = 0.0\n  end","title":"Of an ActorEvent{T}"},{"category":"section","location":"plan/#Event-generation-in-particular","page":"Plan","text":"This is a macro language to generate events. This one would create a function called generators(::Type{MoveTransition}) that contains a list of EventGenerator objects.\n\n@conditionsfor MoveTransition begin\n    @reactto changed(agent[i].loc) begin physical\n        agent_loc = physical.agent[i].loc\n        for direction in valid_directions(physical.geom, agent_loc)\n            generate(MoveTransition(agent_who, direction))\n        end\n    end\n    @reactto fired(InfectTransition(sick, healthy)) begin physical\n        for neigh in neighborsof(physical, healthy)\n            for nextneigh in neighborsof(physical, neigh)\n            generate(InfectTransition(neigh, nextneigh))\n        end\n    end\nend\n\nThe @reactto changed(agent[i].loc) creates a generator that reacts ToPlace where the search string is [:agent, ℤ, :loc]. Then it makes a function closure with the arguments (generate::Function, physical, i) where the value passed to i is the ℤ match.\n\nThe @reactto fired(InfectTransition(sick, healthy)) creates a generator that reacts ToEvent where the search string is [:InfectTransition] and the function closure has the arguments (generate::Function, physical, sick, healthy). The values for sick and healthy are taken from the matched event.","title":"Event generation in particular"},{"category":"section","location":"plan/#Idea-3:-The-Command-Handler-Pattern-(The-\"Made-Up\"-Idea)","page":"Plan","text":"What if we get rid of enable and rate functions as top-level concepts for the user and borrow from patterns like CQRS/Event Sourcing?\n\nThe Concept: The simulation loop is driven by Commands.\nState is your physical state.\nA Command is a data struct representing an intent to cause an event (e.g., TryToBreak(actor_idx)). Users can  dispatch commands at any time.\nA Handler is a function handle(command, state). It looks at the command and the current state and returns  zero or more potential (Event, Distribution) tuples. For TryToBreak(1), the handler checks if actor 1 is  working and, if so, returns (BreakOccurred(1), fail_dist). This single function effectively combines the  \"generator\", \"precondition\", and \"rate\" logic.\nAn Event is a data struct representing something that happened (e.g., BreakOccurred(actor_idx)).\nThe fire! function is replaced by a pure \"reducer\" function: apply(event, state) -> new_state.\nThe main loop would look like:\nCollect all possible Commands that could be issued in the current state.\nRun them through their Handlers to get a list of possible (Event, Distribution) pairs.\nPass this list to CompetingClocks.jl to sample the next Event.\napply the chosen event to the state.\nRepeat.\nHow it Simplifies:\nConceptual Clarity: The flow is very clear: you issue a Command, a Handler translates it into a potential   Event, the sampler picks one, and an apply function enacts it. The roles are extremely well-defined.\nIt almost gets rid of event keys: The sampler would work with Event structs directly. The clock_key could be   generated implicitly from the event's type and data. You wouldn't need a separate \"key\" struct.\nIt combines Generator, Enable, and Rate: The Handler function serves all three purposes in one go.\nThis takes the five parts of each event and spreads them across the simulation. It looks more like a traditional simulation. In that case, how is this simulation style, splitting the event enabling from the state, so different from simulation where, during the handling of a command, new events are created? What is the intrinsic different, and if there is an advantage to splitting events and state, how do we realize that advantage while retaining the narrative structure of traditional simulation styles? Or how do we realize the advantages of avoiding that narrative structure? Where is the payoff?\n\nThis is a more functional approach that radically simplifies the user's conceptual model, even if it adds a few new concepts (Command, Handler).\n\nThe payoff is that this purity and separation are precisely what unlocks the advanced capabilities you want and makes your \"propagator\" vision a reality.\n\nThe Event Log Becomes a First-Class Citizen: Because the apply function is pure (state, event) -> newstate, the  entire simulation history can be represented as newstate = apply(eventn, ... apply(event1, apply(event0,  initialstate))). The sequence of events [event0, event1, ..., event_n] is a complete, tangible, and replayable  log of what happened. This isn't just a printout; it's a data structure you can analyze.\nLikelihood Calculation Becomes Trivial: With the event log as data, calculating the likelihood of that specific  trajectory is straightforward. At each step k, you know exactly which possibilities the handle function presented  to the sampler and which event_k was chosen. The total likelihood is the product of those individual  probabilities.\nTime-Travel, Counterfactuals, and Debugging: This is the killer feature.\nWant to simulate backwards? You have the whole event history.\nWant to know \"what if\"? Go back to state_k, inspect the possibilities generated by the handlers, and manually   run the propagator with a different event choice. You can fork reality at any point.\nDebugging is transformed. A bug is no longer a mysterious state corruption. It's an apply function that   returned the wrong newstate for a given (event, oldstate). You can test your apply reducers like any other   pure function, completely isolated from the complexity of the simulation loop.\nEnforced Separation of Concerns: Your commitment to separating the model from the sampler is now enforced by the  function signatures. The model code (handlers and reducers) literally cannot access the sampler. This makes the  system architecture incredibly clean and robust.","title":"Idea 3: The Command-Handler Pattern (The \"Made Up\" Idea)"},{"category":"section","location":"plan/#It-looks-like-the-AI-caught-on-to-my-plan:","page":"Plan","text":"A Possible Reframing\n\nWhat if the mental model were:\n\nUser defines: ModelSpec     ├── State type (plain Julia struct, no ObservedArray needed)     ├── Event types     └── Event interface methods (fire!, enable, precondition)\n\nChronoSim provides: Execution strategies     ├── ForwardSimulation (adds generators, ObservedState, DependencyNetwork)     ├── LikelihoodReplay (uses core model only)     └── GenDistribution / TuringModel wrappers","title":"It looks like the AI caught on to my plan:"},{"category":"section","location":"plan/#Hooks!","page":"Plan","text":"ChronoSim.jl runs simulations forward but doesn't have hooks for:\n\nPausing at decision points\nBranching into multiple rollouts\nComparing outcomes and selecting actions\n\nAnd we really need to make clear this isn't just about forward simulation.\n\n","title":"Hooks!"},{"category":"section","location":"generators/#Generators","page":"Generators","text":"Every event type needs generators: rules that tell the framework which changes of state make it worth checking that event's precondition. This page explains why they exist, how to write them by hand with @conditionsfor, how to have them derived automatically with @precondition, and how to decide between the two.","title":"Generators"},{"category":"section","location":"generators/#Why-generators-exist","page":"Generators","text":"A precondition-driven simulation faces an old problem. The classic activity-scanning style of discrete-event simulation, described in Pidd's Computer Simulation in Management Science, re-checks every conditional activity after every event, which is simple and correct and much too slow when the model has many events. Stochastic Petri nets solve the problem structurally: every transition declares input arcs, so the simulator knows exactly which transitions to reconsider when a place changes. ChronoSim takes the Petri-net idea but gets the arcs from code instead of from a drawn graph. A generator is, in effect, a declared set of input arcs for one event type.\n\nThe contract for a generator is asymmetric, and the asymmetry is the most important thing on this page. A generator may propose too much: every proposal is filtered through the event's precondition, so an unnecessary candidate costs one wasted boolean check. A generator must never propose too little: an event that is never proposed can never become enabled, no error is raised, and the model silently loses behavior. When in doubt, generate.","title":"Why generators exist"},{"category":"section","location":"generators/#Writing-generators-by-hand:-@conditionsfor","page":"Generators","text":"A @conditionsfor block attaches one or more reaction rules to an event type. Each rule begins with @reactto and reacts either to a change of state or to the firing of another event.\n\n@conditionsfor CallElevator begin\n    @reactto changed(person[who].destination) do system\n        generate(CallElevator(who))\n    end\nend\n\nRead the pattern person[who].destination as follows: person is a field of the state, the bracketed position is a wildcard index, and destination is a field of the element. Whenever a write is recorded at any address matching (:person, <any index>, :destination), the rule's body runs with the wildcard variable who bound to the concrete index from the address. The body then calls generate with each candidate event it wants proposed. A body may generate zero, one, or many events, and it may read the state through its argument (here system) to decide:\n\n@reactto changed(calls[callkey].requested) do system\n    for elidx in 1:length(system.elevator)\n        generate(OpenElevatorDoors(elidx))\n    end\nend\n\nThis second form is common when the changed address does not identify which event instances are affected — a new call could matter to every elevator, so the rule proposes them all and lets the precondition sort it out.\n\nThe other kind of rule reacts to an event firing rather than to a state change:\n\n@reactto fired(Departure(who)) do state\n    generate(Arrival(who))\nend\n\nA fired rule expresses causal scheduling — this happening leads to that — which is information that does not live in the state at all. Event-to-event scheduling of this kind is the native vocabulary of Schruben's event graphs, and it is the one kind of trigger that can never be derived from a precondition, because a precondition only reads state.","title":"Writing generators by hand: @conditionsfor"},{"category":"section","location":"generators/#Deriving-generators:-@precondition","page":"Generators","text":"For most events the generators repeat information the precondition already contains: each @reactto changed(...) names a place the precondition reads. ChronoSim can therefore derive the generators. Declare the precondition with the @precondition macro and do not write a @conditionsfor block at all:\n\n@precondition function precondition(evt::CallElevator, system)\n    person = system.person[evt.person]\n    return person.location != 0 &&\n           person.location != person.destination &&\n           !person.waiting\nend\n\nThe macro emits the precondition unchanged, and it also emits generators built from the body's reads: this example produces triggers on person[i].location, person[i].destination, and person[i].waiting, each proposing CallElevator(i). Use either @precondition or @conditionsfor for a given event type, not both, because each one defines that type's complete generator set.\n\nYou can always inspect what was derived, and it is a good habit after converting each event:\n\njulia> derivation_report(CallElevator)\n\nThe report lists each trigger's address pattern, whether it binds the event's fields precisely or proposes over a whole domain, and where each domain came from.","title":"Deriving generators: @precondition"},{"category":"section","location":"generators/#How-the-derivation-reads-a-precondition","page":"Generators","text":"The macro analyzes the body's syntax. Understanding the few rules it follows tells you how to write preconditions that derive well.\n\nReads indexed by event fields derive precise triggers. When the body reads system.person[evt.person].waiting, the index is an event field, so the derived trigger binds it: a change at (:person, 4, :waiting) proposes exactly CallElevator(4). Literal indices work too and become a runtime check. Local variables that simply name an element, as person does in the example above, are followed transparently.\n\nAny other index widens the trigger. If the body reads a container at an index computed from state, or inside a loop over the container, the derivation cannot know from the address which event instance is affected, so it proposes the event over the whole domain of its fields — precisely what the hand-written scan-all-elevators rule above did. Widening is always sound, because generators are licensed to over-propose; what it costs is extra filtered candidates, discussed below.\n\nLoops, if, boolean operators, and comparisons are all fine. Scans such as for pidx in 1:length(system.person) are analyzed, with the loop-indexed reads widened as just described. The reducers any, all, count, sum, prod, minimum, and maximum over a generator expression, such as any(p.waiting for p in ...), are analyzed the same way as loops.\n\nHelper functions must be marked. A call that passes state to an ordinary function is rejected at macro time, because the analysis cannot see the reads inside a function it only knows by name, and guessing would risk the silent under-proposal failure. Mark the helper with @fragment and it participates:\n\n@fragment function people_waiting(people, floor, dirn)\n    ...\nend\n\n@fragment defines the function exactly as written and additionally registers its body, so that when a @precondition calls it, the analysis inlines the body with the caller's arguments substituted. Define helpers before the preconditions that call them. One precondition may also call another — precondition(EnterElevator(pidx), system) inside a body is analyzed by inlining the registered precondition of EnterElevator — which is useful when one event's enabling rule is naturally phrased in terms of another's.\n\nFree fields need domains. A widened trigger, or a clean read that binds only some of the event's fields, must propose the event over every value the unbound fields could take. The domain of each field is resolved automatically where possible: a field that the body uses as a container index gets that container's indices or keys, and a field whose type is an @enum or Bool gets its instances. When neither rule applies, you declare the domain once:\n\n@domain Move.direction = ALLDIRECTIONS\n\nThe right-hand side is evaluated against the live state under the name physical. If a needed domain is missing, constructing the simulation fails with a message that names the field and shows the exact @domain line to write. In the three example models shipped with ChronoSimExamples.jl, every domain was inferred and no @domain declaration was needed.\n\nOut-of-fragment constructs fail loudly. Anything the analysis cannot handle — an unmarked helper receiving state, a precondition that reads no state at all — is a macro-time error with a message naming the offending expression and the fix. The derivation never silently drops a read, because a dropped read is exactly the under-proposal failure the whole mechanism exists to prevent.","title":"How the derivation reads a precondition"},{"category":"section","location":"generators/#When-you-still-write-@conditionsfor-by-hand","page":"Generators","text":"Three situations call for hand-written generators, and they are worth recognizing in advance.\n\nRate-driven events with trivial preconditions. Some events are always eligible and are governed entirely by their rate. In the SIRVillage example model, individuals travel between locations at stochastic intervals, and travel is always possible:\n\nprecondition(event::Travel, physical) = true\n\n@conditionsfor Travel begin\n    @reactto changed(actors[who].haunt) do physical\n        generate(Travel(who))\n    end\nend\n\nThere is nothing to derive from true, and the @precondition macro will say so. The generator here reacts to the event's own effect — each move proposes the next move — which is causal knowledge the modeler supplies.\n\nEvent-to-event scheduling. Any trigger of the fired(...) kind is by nature hand-written, as described above.\n\nPrecision worth paying for. A derived widened trigger proposes over a whole domain, and sometimes the modeler knows a much smaller set. The clearest case in the example models is infection in SIRVillage: the event Infect(source, sink) is derived with triggers that propose every (infectious person, any person) pair, while the hand-written generator proposes only pairs that are at the same location. Both are correct, and the measured difference on a 30-person village was about thirty times as many proposals, all filtered. If profiling shows that filtering dominates, writing the tighter generator by hand is the remedy, and the two styles coexist freely across the events of one model.","title":"When you still write @conditionsfor by hand"},{"category":"section","location":"generators/#Checking-your-generators","page":"Generators","text":"Two runtime tools verify the relationship between preconditions and generators, and both are worth turning on in a model's tests.\n\nChronoSim.check_derivation_coverage(true)\n\nmakes every precondition evaluation of a derived event assert that each address it actually read is covered by some derived trigger. A violation throws, with the offending address, and indicates a bug in the derivation rather than in your model, so please report one if you see it.\n\nChronoSim.collect_generation_stats(true)\n\ncounts, per event type, how many candidates the generators proposed and how many were admitted by their preconditions. The ratio is the price of over-proposal. In the shipped examples the ratio is exactly one for events whose reads are all keyed by event fields, and it grows where widening or free-field domains are involved, which is what the statistics are for: they tell you where a hand-written generator would actually pay.\n\n","title":"Checking your generators"},{"category":"section","location":"categorize/#Where-ChronoSim-Fits","page":"Where ChronoSim Fits","text":"ChronoSim does not sit squarely inside any one of the traditional categories of simulation. This page places it relative to the styles and results it draws on, for readers who know some of that literature and want to orient themselves. Nothing here is needed to write a model.","title":"Where ChronoSim Fits"},{"category":"section","location":"categorize/#Exact,-stochastic,-continuous-time","page":"Where ChronoSim Fits","text":"The sampling is exact continuous-time sampling of competing clocks. This family of algorithms is often called Gillespie sampling, though first-to-fire sampling from a heap predates Gillespie's papers, and the method is not specific to chemistry. ChronoSim delegates sampling to CompetingClocks.jl, which implements several exact samplers, including next-reaction variants in the lineage of Gibson and Bruck (2000).","title":"Exact, stochastic, continuous time"},{"category":"section","location":"categorize/#The-generalized-semi-Markov-process","page":"Where ChronoSim Fits","text":"The stochastic process a ChronoSim model defines is closest to a generalized semi-Markov process (GSMP), the formalism surveyed by Glynn (1989). A GSMP has a physical state and a set of clocks, each clock running down toward a transition at a rate that may depend on the state, and each transition changing part of the state. ChronoSim borrows the GSMP's separation between physical state and clock state, and even the phrase \"physical state.\" Where a classical GSMP fixes the dependency structure between states and clocks in advance, ChronoSim discovers it while the simulation runs, by observing what each precondition and rate function actually reads.\n\nAnderson and Kurtz's treatment of continuous-time processes as coupled counting processes (Stochastic Analysis of Biochemical Systems, Springer,\n\nshowed that the sampling machinery can be separated cleanly from the\n\nmodel: a sampler needs only the enabling times and distributions of the clocks. That observation is what allows CompetingClocks.jl to exist as an independent, testable package, with all model logic remaining in ChronoSim.","title":"The generalized semi-Markov process"},{"category":"section","location":"categorize/#Stochastic-Petri-nets","page":"Where ChronoSim Fits","text":"A generalized stochastic Petri net (GSPN) declares its dependency structure graphically: transitions consume and produce tokens at places, so the net itself says which transitions to reconsider when a place changes, and properties such as reachability can be computed from the graph. The cost is that the graph must be constructed up front, which becomes burdensome when the state is large and heterogeneous — a population stratified by age, sex, location, and disease state multiplies into a very large net. ChronoSim keeps the useful part of the GSPN idea, the place-to-transition dependency graph, but builds it dynamically from observed reads and writes, and it allows immediate transitions in the same sense a GSPN does. What it gives up is the ability to analyze the whole graph before running.","title":"Stochastic Petri nets"},{"category":"section","location":"categorize/#Survival-analysis,-actuarial-and-reliability-modeling","page":"Where ChronoSim Fits","text":"Actuaries and reliability engineers care that simulated event times honor estimated hazard rates, including hazards that accumulate with age or exposure. ChronoSim's enable function returns a distribution together with the time from which it is measured, which is the mechanism for expressing accumulated hazard, and the framework's likelihood support exists so that a simulation can be fit to data with the same seriousness those fields apply. One way to describe the project is as survival analysis run forward.","title":"Survival analysis, actuarial and reliability modeling"},{"category":"section","location":"categorize/#Agent-based-simulation","page":"Where ChronoSim Fits","text":"Agent-based modeling is a way of stating invariants — an agent persists, is somewhere, and acts — rather than a distinct mathematical object, and agent-based models can be written in ChronoSim by making events that respect those invariants. If your model is naturally a synchronous-update agent model, a dedicated framework such as Agents.jl will serve you better; the comparison page Why Not Hand Made says more about when ChronoSim is and is not the right tool.","title":"Agent-based simulation"},{"category":"section","location":"categorize/#Formal-specification","page":"Where ChronoSim Fits","text":"An event defined by a precondition and an action reads very much like a TLA+ action, and the resemblance is useful rather than accidental: both designs take the position that a system is best described by when its transitions are allowed and what they change. The elevator example in the test suite exports its trajectories for checking against a TLA+ specification, which is a practical benefit of keeping the two vocabularies close.","title":"Formal specification"},{"category":"section","location":"categorize/#Summary","page":"Where ChronoSim Fits","text":"ChronoSim is exact, stochastic, continuous-time simulation with time-varying hazards and immediate transitions, over a GSMP-style state, with the GSPN-style dependency graph recovered dynamically from ordinary model code rather than declared up front.\n\n","title":"Summary"},{"category":"section","location":"debugging/#Debugging-a-simulation","page":"Debugging a Simulation","text":"Most simulation bugs do not crash. The run completes, the numbers come back, and they are quietly wrong: an event that should have fired never did, or a run spins forever on state the stop condition cannot see. This guide walks one real bug end to end — from the symptom to the fix — using the three why-verbs (whynot, whyrunning, whystopped) built on the recorded TrajectorySkeleton. The runbook is the terse reference; this page is the narrative.","title":"Debugging a simulation"},{"category":"section","location":"debugging/#The-symptom","page":"Debugging a Simulation","text":"An elevator that reaches the top or bottom floor should park — its StopElevator event fires and its direction becomes Stationary. In one model it never does. Elevators ride to a boundary floor and sit there with a stale direction; people keep waiting. Nothing throws. There is no stack trace, no assertion, no NaN — the executor simply never proposes the event, so from the outside the run looks like a slightly-too-idle elevator system.\n\nThis is the missed-trigger bug class, and it is invisible to exceptions: a hand-written generator's trigger set omits an address the precondition depends on, so a change to that address flips the precondition to true without any generator noticing. The event that should now be enabled is never even proposed.","title":"The symptom"},{"category":"section","location":"debugging/#Record-what-happened","page":"Debugging a Simulation","text":"The why-verbs work on a recorded skeleton, not on a live run. Recording is opt-in and costs a production run nothing; you turn it on by passing a RecordSkeleton policy:\n\nusing ChronoSim\nusing Random: Xoshiro\n\nrec = RecordSkeleton()\nsim = SimulationFSM(ElevatorSystem(3, 2, 5), EVENTS;\n    rng=Xoshiro(93472934), policy=rec)\ns = 5    # a correct twin of this model first fires StopElevator(2) at step 5,\n         # so stop this run just before that step: the state where the stop is due\nChronoSim.run(sim, init_physical, (p, i, e, w) -> i >= s)\nskel = recorded_skeleton(rec)\nshow(skel)                                                  # a replayable artifact\n\nskel is a replayable artifact: the pre-init RNG state, every fired event, and the enable/disable/proposal history — not a transcript you squint at, but data the verbs query.","title":"Record what happened"},{"category":"section","location":"debugging/#Ask-the-direct-question","page":"Debugging a Simulation","text":"You suspect StopElevator for elevator 2 never fired. Ask why. whynot needs the skeleton, a sim_factory that rebuilds the same simulation (the replay contract), and the event instance:\n\nfactory = policy -> (SimulationFSM(ElevatorSystem(3, 2, 5), EVENTS;\n    rng=Xoshiro(93472934), policy=policy),\n    init_physical)\n\nrep = whynot(skel, factory, StopElevator(2))\n\nThe readout:\n\nwhynot (:StopElevator, 2): NEVER PROPOSED over 4 recorded steps\n  declared triggers (hand_written):\n    (elevator, _index, floor) | (elevator, _index, doors_open)\n  fired-event triggers : none\n  precondition at final replayed state : true\n    !! the precondition holds now, yet the event was never proposed:\n    !! a trigger for one of the MISSING addresses below is required\n  precondition reads (true trigger set) :\n    (elevator, 2, direction) | (elevator, 2, floor) | (elevator, 2, buttons_pressed)\n    (elevator, 2, doors_open)\n  MISSING triggers (reads no declared trigger covers) :\n    (elevator, 2, direction) | (elevator, 2, buttons_pressed)\n  near-miss writes (same container, different index/leaf) : 2 total\n    step 4 (:DispatchElevator, 3, Up) wrote (elevator, 1, direction)  [container_near_miss]\n    step 4 (:DispatchElevator, 3, Up) wrote (elevator, 1, floor)  [index_near_miss]\n  note: trigger-set analysis is one precondition evaluation at the final replayed state; short-circuited reads may be missing. fired() triggers (if any) can cover reads without a place trigger. Full per-claus…","title":"Ask the direct question"},{"category":"section","location":"debugging/#Reading-the-output,-line-by-line","page":"Debugging a Simulation","text":"NEVER PROPOSED — the furthest lifecycle stage the event reached. It was not rejected (proposed then filtered) and not outraced (enabled then beaten): no generator ever proposed it. That already tells you the bug is in the trigger set, not in the precondition or the rates.\ndeclared triggers (hand_written) — the two addresses the hand-written generator reacts to: elevator[_index].floor and elevator[_index].doors_open (_index is the wildcard the generator matches any elevator on). This is the suspect list.\nprecondition at final replayed state : true followed by the !! lines — the strongest possible finding. whynot replayed the skeleton and evaluated StopElevator(2)'s real precondition: it is true right now. The event is fireable at this very state, yet nothing will ever propose it. A trigger gap is now certain.\nprecondition reads (true trigger set) — the addresses the precondition actually read at that state: direction, floor, buttons_pressed, doors_open. These are the addresses whose changes should propose the event.\nMISSING triggers — the reads that no declared trigger covers: (elevator, 2, direction) and (elevator, 2, buttons_pressed). The generator reacts to floor and doors_open, but the precondition also depends on direction and buttons_pressed. A change to either can flip the precondition with no generator watching.\nnear-miss writes — the smoking gun. At step 4 DispatchElevator wrote (elevator, 1, direction) — a container_near_miss against the trigger set (same container elevator, different leaf direction instead of floor). DispatchElevator writes direction, direction is a missing trigger, and the precondition reads it. That write should have been a trigger.","title":"Reading the output, line by line"},{"category":"section","location":"debugging/#The-fix","page":"Debugging a Simulation","text":"Add the missing triggers to the hand-written generator (this is exactly the fix commit 53c18d2 applied to the elevator example):\n\n@conditionsfor StopElevator begin\n    @reactto changed(elevator[elidx].floor) do system\n        generate(StopElevator(elidx))\n    end\n    @reactto changed(elevator[elidx].doors_open) do system\n        generate(StopElevator(elidx))\n    end\n    @reactto changed(elevator[elidx].direction) do system      # <- added\n        generate(StopElevator(elidx))\n    end\n    @reactto changed(elevator[elidx].buttons_pressed) do system # <- added\n        generate(StopElevator(elidx))\n    end\n    @reactto changed(calls[callkey].requested) do system        # <- added\n        for elidx in 1:length(system.elevator)\n            generate(StopElevator(elidx))\n        end\n    end\nend\n\nBetter still: delete the hand-written block and derive the generators from the precondition with @precondition, which reads the trigger set off the body and cannot omit an address it depends on — see the derivation guide. The derived twin is what caught this bug in the first place: it proposed StopElevator where the hand-written twin never did, and the differential test diverged.","title":"The fix"},{"category":"section","location":"debugging/#Verify","page":"Debugging a Simulation","text":"Re-run with the trigger added and ask again: whynot now answers :fired, and the differential twin agrees. The fix is confirmed by the same tool that found the bug.","title":"Verify"},{"category":"section","location":"debugging/#Which-verb-answers-which-question","page":"Debugging a Simulation","text":"The three verbs partition the questions you ask about a run that finished but looks wrong:\n\nQuestion Verb Reads\nWhy did event X never fire? whynot skeleton + factory + event\nWhy won't the run stop? whyrunning final sim + skeleton + stop predicate\nWhy did the run stop (or break)? whystopped an InvariantViolation, or a skeleton\n\nA rejected event — proposed but always filtered — is whynot's :rejected stage: it replays to the rejection steps, runs guard_clauses, and names the failing conjunct with the values and last writers of its reads. See the runbook entry \"Ask why an event did not fire\".\nA run that will not stop is whyrunning: it shows the stop predicate's reads, their values and writers, and whether the recent event churn touches any of them. Its static \"can never stop\" verdict is not yet implemented (it carries the line reachability analysis requires effect analysis (not yet run)). See \"Ask why the run has not stopped\".\nA violated invariant is whystopped: it names the guilty writer event, the guilty addresses with their last and prior writers, and the exact replay(...) command that reproduces the state one step before the break. Run under PolicyStack(RecordSkeleton(), CheckInvariants(Model)) — recorder first — so the violation carries a replayable prefix. See \"Read out why the run stopped\".","title":"Which verb answers which question"},{"category":"section","location":"debugging/#Related","page":"Debugging a Simulation","text":"Runbook: Ask why an event did not fire, Ask why the run has not stopped, Read out why the run stopped.\nRecording and replaying a run — the skeleton the verbs read.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"events/#Events","page":"Events","text":"This page defines each piece of an event precisely. The Getting Started example shows the same pieces in the flow of a working program, so it can help to read the two side by side.","title":"Events"},{"category":"section","location":"events/#The-event-struct","page":"Events","text":"An event type is an immutable struct that subtypes SimEvent. Its fields identify which instance of the event this is — which person, which machine, which pair of individuals — and the framework calls these the identifying fields.\n\nstruct Repair <: SimEvent\n    machine::Int64\nend\n\nInternally the framework refers to an event by its clock key, which is a tuple of the type name followed by the field values, such as (:Repair, 3). Clock keys appear in logs and diagnostics, and they are the identity under which an event's clock lives in the sampler. Two event values with equal fields are the same event, so fields should be plain values with sensible equality: integers, symbols, strings, enums, or tuples of these.\n\nAn event struct may also be empty, when the event concerns the system as a whole rather than any particular entity.","title":"The event struct"},{"category":"section","location":"events/#precondition","page":"Events","text":"precondition(event, state)::Bool\n\nThe precondition answers whether the event could happen in the current state. It must be a pure test: it reads the state and returns a boolean, and it must not modify anything. The framework records which addresses it reads and re-evaluates the precondition when any of them changes, so the precondition is also, implicitly, a declaration of what the event depends on.\n\nWrite preconditions to be self-contained, meaning the precondition itself excludes every state in which fire! would misbehave. It is tempting to rely on the fact that generators only propose an event in reasonable circumstances, but that coupling is fragile, and the derivation machinery assumes the precondition tells the whole truth. If firing requires that a person be on a floor, the precondition should test that the person is on a floor.\n\nDeclaring the precondition with the @precondition macro, rather than as a plain function, additionally derives the event's generators from its body. The Generators page describes that in full.","title":"precondition"},{"category":"section","location":"events/#enable-and-reenable","page":"Events","text":"enable(event, state, when) -> (distribution, te)\n\nWhen a precondition first becomes true, the framework calls enable to learn the distribution of the event's firing time. The return value is a tuple of a continuous univariate distribution from Distributions.jl and a time te from which the distribution is measured. In the common case you return (dist, when), meaning the waiting time starts now. Returning an earlier te shifts the distribution's origin into the past, which expresses an event whose hazard has already been accumulating — for example, a machine whose failure clock started when it began working, not when you happened to enable the event.\n\nThe framework records which addresses enable reads, and those reads are the event's rate dependencies. If a later firing changes one of them while the event remains enabled, the framework calls reenable:\n\nreenable(event, state, first_enabled, when)\n\nThe default reenable returns nothing, which means \"keep the clock I already have.\" If the event's rate genuinely tracks the state — a repair rate that depends on how many repair crews are free, say — forward it to enable so the distribution is recomputed:\n\nreenable(evt::Repair, state, _, when) = enable(evt, state, when)\n\nThis treatment of long-lived clocks with state-dependent rates follows the generalized semi-Markov process view of discrete-event systems, and readers who want the theory can start with Glynn's 1989 survey of the GSMP formalism.","title":"enable and reenable"},{"category":"section","location":"events/#fire!","page":"Events","text":"fire!(event, state, when, rng)\n\nFiring is the only place the model changes the state. The framework records every address written during fire! and uses that set to decide which events to re-examine and which new candidates to propose; nothing else triggers a re-examination, which is why all mutation must happen here (or in the initializer, described below). If fire! draws random numbers — to choose a destination, say — it must use the rng argument rather than a global generator, both for reproducibility and because the drawn values are then part of the trajectory's likelihood.","title":"fire!"},{"category":"section","location":"events/#Immediate-events","page":"Events","text":"An event type can declare itself immediate:\n\nisimmediate(::Type{OpenValve}) = true\n\nAn immediate event fires at the same instant as the timed event that made its precondition true, with no waiting time and no enable call. Chains of immediate events are resolved before the next timed event is sampled. Use them for consequences that are logically instantaneous, such as a valve that opens the moment pressure crosses a threshold. This is the same distinction that generalized stochastic Petri nets draw between timed and immediate transitions.","title":"Immediate events"},{"category":"section","location":"events/#Running-a-simulation","page":"Events","text":"A model's event types, plus an initial state, are assembled into a simulation like this:\n\nsim = SimulationFSM(physical, event_types; seed=838109, observer=callback)\nChronoSim.run(sim, initializer, stop_condition)\n\nSimulationFSM takes the physical state and a vector of the event types in the model. By default it builds the historical next-reaction sampler from CompetingClocks.jl; you can select a different one by passing a sampler method spec via the sampler keyword (e.g. sampler=FirstReactionMethod() after using CompetingClocks: FirstReactionMethod), and either a seed or an explicit rng.\n\nThe initializer is a function that sets up the state at time zero. Writes it makes are recorded exactly like the writes of a firing, and those writes are what propose the first candidate events, so every part of the state that events depend on should be written (not merely constructed) during initialization.\n\nThe stop_condition is a function (physical, step_idx, event, when) -> Bool that is consulted with the event that is about to fire, before it fires. Returning true stops the run, so a condition like when > 100.0 stops the clock strictly between events.\n\nThe observer keyword accepts a function (physical, when, event, changed_places) -> nothing that the framework calls after each firing. This is the intended place to record trajectories, compute summary statistics, or check invariants; the examples in this documentation use it to save (event, time) sequences.\n\n","title":"Running a simulation"},{"category":"section","location":"not_chrono/#Starting-from-Simple-Simulation","page":"Why Not Hand Made","text":"","title":"Starting from Simple Simulation"},{"category":"section","location":"not_chrono/#Hand-made-Can-Be-Excellent","page":"Why Not Hand Made","text":"Let's make a cute little simulation of wandering, spawing agents without using ChronoSim.jl. This is less thorough than the spatial rock-paper-scissors in Agents.jl. Start with a state that will be a vector of Agents.\n\nusing DataStructures, Distributions, Random\nrng = Xoshiro(92347223)\n@enum Matter Rock Paper Scissors\nWINNER = Dict(\n    (Rock, Rock) => 2,\n    (Paper, Paper) => 2,\n    (Scissors, Scissors) => 2,\n    (Rock, Paper) => 2,\n    (Rock, Scissors) => 1,\n    (Paper, Rock) => 1,\n    (Paper, Scissors) => 2,\n    (Scissors, Rock) => 2,\n    (Scissors, Paper) => 1,\n)\nmutable struct Agent\n    location::Vector{Int64}\n    kind::Matter\nend;\nnothing #hide\n\nWe need a queue of events that happen next.\n\nμ, γ = (1.0, 3.0)  # Rates for movement and spawning.\nqueue = BinaryMinHeap{Tuple{Float64,Tuple}}()\nschedule(queue, distribution, key) = push!(queue, (rand(rng, distribution), key));\nnothing #hide\n\nThe heart of the simulation will process the event to fire and then add future events to the queue.\n\nDIRECTIONS = [[-1, 0], [1, 0], [0, 1], [0, -1]]\nevent_dict = Dict(\n    :move => function (state, when, who, direction)\n        newloc = state.agent[who].location + DIRECTIONS[direction]\n        if checkbounds(Bool, state.locations[newloc])\n            if state.locations[newloc] == 0\n                state.locations[newloc] = who\n                state.locations[state.agent[who].location] = 0\n            else\n                opponent = state.locations[newloc]\n                if WINNER[(state.agent[who].kind, state.agent[opponent].kind)] == 1\n                    state.locations[newloc] = who\n                    state.locations[state.agent[who].location] = 0\n                    state.agent[opponent].location = [0, 0]\n                end\n            end\n        end\n        schedule(queue, Exponential(μ), (:move, who, rand(rng, 1:length(DIRECTIONS))))\n    end,\n    :spawn => function (state, when, who)\n        kind = state.agent[who].kind\n        push!(state.agent, Agent(state.agent[who].location, kind))\n        child = length(state.agent)\n        schedule(queue, Exponential(μ), (:move, child, rand(rng, 1:length(DIRECTIONS))))\n        schedule(queue, Exponential(γ), (:spawn, child))\n        schedule(queue, Exponential(γ), (:spawn, who))\n    end,\n);\nnothing #hide\n\nThe main loop of the simulation ends up being initialization and a small event loop.\n\nmutable struct Board\n    agent::Vector{Agent}\n    locations::Array{Int,2}\n    Board(n) = new(Agent[], zeros(Int, n, n))\nend\n\nfunction rununtil(eventcnt)     N = 10     state = Board(N)     for aidx in 1:N         locfound = false         agentloc = [0, 0]         while !locfound             agentloc = rand(rng, 1:N, 2)             locfound = state.locations[agentloc[1], agentloc[2]] == 0         end         agent = Agent(agentloc, rand(rng, instances(Matter)))         state.locations[agentloc[1], agentloc[2]] = aidx         push!(state.agent, agent)         schedule(queue, Exponential(μ), (:move, aidx, rand(rng, 1:length(DIRECTIONS))))         schedule(queue, Exponential(γ), (:spawn, aidx))     end     now = 0.0     for eventidx in 1:eventcnt         (when, (eventtype, args...)) = pop!(queue)         now = when         @show (when, eventtype, args)         # Skip events if they refer to an agent that has been knocked off the board.         if state.agent[args[1]].location[1] != 0             eventdict[eventtype](state, now, args...)         end     end end run_until(10)\n\nThis works great because it's very clear how events flow, which makes debugging easy. Why make life any more complicated?","title":"Hand-made Can Be Excellent"},{"category":"section","location":"not_chrono/#Why-change","page":"Why Not Hand Made","text":"There is a moment in the event logic where it decides what events could happen next. Let's say multiple events could queue up the same kind of follow-up event. Shouldn't the follow-up event decide when it could happen instead of the events that queue it? And if an event is no longer possible, shouldn't there be a way to cancel it?\n\ntip: Idea\nLet's put the precondition for an event to fire with the event itself.\n\nWhat if we want to extend the simulation with more events? You need to go in and change what events are queued after one has fired.\n\ntip: Idea\nLet's use an Observer pattern to let an event subscribe to previous events or to state changes.\n\nThere is another complication that is sometimes overlooked for continuous-time simulation. When the state of a simulation changes, an event that was already queued might need to happen sooner or it might be pushed back in time. This is a re-enabling of the event where it can re-evaluate its probability to fire at future times.\n\ntip: Idea\nLet's explicitly calculate a re-enabling time when appropriate.\n\nLet's see what ChronoSim does with a similar simulation.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nimport ChronoSim: precondition, enable, fire!, generators\n\n@keyedby Critter Int64 begin\n    location::Vector{Int}\n    kind::Matter\nend\n\n@keyedby Square Tuple{Int64,Int64} begin\n    resident::Int64\nend\n\n@observedphysical Land begin\n    agent::ObservedVector{Critter}\n    board::ObservedArray{Square,2}\nend\n\nfunction Land(board_side::Int, person_cnt::Int)\n    for aidx in 1:N\n        loc_found = false\n        agent_loc = [0, 0]\n        while !loc_found\n            agent_loc = rand(rng, 1:N, 2)\n            loc_found = state.locations[agent_loc[1], agent_loc[2]] == 0\n        end\n        agent = Agent(agent_loc, rand(rng, instances(Matter)))\n        state.locations[agent_loc[1], agent_loc[2]] = aidx\n        push!(state.agent, agent)\n        schedule(queue, Exponential(μ), (:move, aidx, rand(rng, 1:length(DIRECTIONS))))\n        schedule(queue, Exponential(γ), (:spawn, aidx))\n    end\nend\n\n\n\nThis page was generated using Literate.jl.\n\n","title":"Why change"},{"category":"section","location":"observedphysical/#Declaring-the-Simulation-State","page":"Declaring the Simulation State","text":"","title":"Declaring the Simulation State"},{"category":"section","location":"observedphysical/#Why-the-state-is-special","page":"Declaring the Simulation State","text":"ChronoSim needs to know which parts of the state each event reads and writes, because that knowledge is what drives the whole simulation: it decides which events get re-checked and which new events get proposed after every firing. The ChronoSim.ObservedState module therefore provides containers and macros that give every piece of your state a unique address and report every read and write of it.\n\nAn address is a tuple that names the path from the top of the state down to one value. A few examples show the pattern.\n\nReading state.board[3, 2].food records the address (:board, (3, 2), :food).\nWriting state.actors[17].speed records the address (:actors, 17, :speed).\nReading state.car_floor records the address (:car_floor,).\n\nYou do not construct or handle addresses yourself when writing a model. They appear in diagnostics, and they are the vocabulary that generators use, so it helps to know what they look like.","title":"Why the state is special"},{"category":"section","location":"observedphysical/#Declaring-element-types-with-@keyedby","page":"Declaring the Simulation State","text":"The mutable structs that live inside observed containers are declared with @keyedby. The macro takes three things: the name of the type, the type of the index that will locate it inside its container, and the fields.\n\nusing ChronoSim.ObservedState\n\n@keyedby Piece Int64 begin\n    speed::Float64\n    kind::String\nend\n\nA Piece will live in a vector or an Int-keyed dictionary, so its index type is Int64. An element of a two-dimensional array is indexed by a tuple of two integers, so it is declared like this:\n\n@keyedby Square NTuple{2,Int64} begin\n    grass::Float64\n    food::Float64\nend\n\nThe struct that @keyedby creates is mutable, and it carries a hidden field that records where it currently sits inside its owning container. That hidden field is how a write like piece.speed = 2.0 can report its full address even though the assignment never mentions the container.","title":"Declaring element types with @keyedby"},{"category":"section","location":"observedphysical/#Declaring-the-state-with-@observedphysical","page":"Declaring the Simulation State","text":"The top-level state is declared with @observedphysical, which creates a mutable struct plus the machinery that collects read and write reports.\n\n@observedphysical Board begin\n    board::ObservedArray{Square,2,Member}\n    actors::ObservedDict{Int64,Piece,Member}\n    actors_max::Int64\n    params::Param{Dict{Symbol,Float64}}\nend\n\nEach field is tracked according to its type, and there are four cases worth understanding.\n\nObserved containers — ObservedArray (with ObservedVector and ObservedMatrix as aliases), ObservedDict, and ObservedSet — track their contents at the level of individual elements and fields. The final Member type parameter says the container is addressed as a named field of the state; you write it as shown and do not need to think about it further.\nPlain scalar fields such as actors_max::Int64 are tracked at the granularity of the whole field. Reading or writing state.actors_max records the address (:actors_max,). This is the right choice for global quantities such as a count, a mode flag, or the position of a single shared resource.\nParam-wrapped fields are deliberately invisible to tracking. Wrap a field in Param{T} when it is configuration that never changes during a run, such as a table of rate constants or a precomputed distance matrix. Reads of it cost nothing and create no dependencies, and the field is accessed transparently, so state.params[:gravity] works without unwrapping.\nPlain container fields (an ordinary Dict or Vector that is neither observed nor Param-wrapped) are a trap and should be avoided: assigning the whole field is tracked, but mutating its interior is not, so an event whose precondition depends on the interior will not be re-checked when it changes. If the contents change during the simulation, use an observed container; if they never change, wrap the field in Param.","title":"Declaring the state with @observedphysical"},{"category":"section","location":"observedphysical/#Choosing-between-an-array-and-a-dictionary","page":"Declaring the Simulation State","text":"The two main containers correspond to two ways of thinking about identity, and the distinction matters because addresses must keep meaning the same thing over the life of a run.\n\nUse an ObservedArray when the population is fixed and densely indexed: grid cells, a fleet of exactly n machines, the floors of a building. The integer position is the identity of the element. For this reason observed arrays have a fixed extent: operations that would change their length, such as push!, pop!, or resize!, throw a FixedExtentError. Allocate the final size up front and fill it by assignment.\n\nUse an ObservedDict when entities are created or destroyed during the run, or when they carry natural identifiers: people arriving and leaving, strains of a pathogen appearing by mutation, calls keyed by the floor that placed them. The key is the identity, insertion and deletion are ordinary setindex! and delete!, and each affects exactly one address.\n\nUse an ObservedSet when the state you care about is membership itself and events should react to any change of the set. A set's elements have no individual addresses; every operation reads or writes the set as a whole.","title":"Choosing between an array and a dictionary"},{"category":"section","location":"observedphysical/#Initializing-the-state","page":"Declaring the Simulation State","text":"Construction usually takes a short loop, because each element must be its own instance.\n\nboard_data = ObservedArray{Square,Member}(undef, 3, 3)\nfor i in 1:3, j in 1:3\n    board_data[i, j] = Square(0.5, 1.0)\nend\n\nactor_data = ObservedDict{Int64,Piece,Member}()\nactor_data[1] = Piece(2.5, \"walker\")\nactor_data[2] = Piece(3.0, \"runner\")\n\nparams = Dict(:gravity => 9.8, :friction => 0.1)\nboard_state = Board(board_data, actor_data, 10, params)\n\nThe constructor that @observedphysical generates takes the fields in declaration order. A Param field accepts the unwrapped value and wraps it for you.\n\nAfter construction, ordinary Julia syntax does the right thing. The assignment below looks like any other assignment, and it is also recorded as a write to (:actors, 1, :speed), which is what lets the framework react to it.\n\nboard_state.actors[1].speed = 2.0","title":"Initializing the state"},{"category":"section","location":"observedphysical/#The-contract,-in-brief","page":"Declaring the Simulation State","text":"The containers promise three things, and the rest of the framework is built on them. First, whenever the state changes, the set of recorded write addresses covers everything that actually changed, so no event can miss a change it depends on. Second, an element's recorded address always names the place it currently occupies, so reports are never misfiled. Third, addresses are stable, deterministic names: the same access produces the same address in every run, and two runs with the same seed produce the same trajectory. These three promises are checked mechanically by a fuzz tester in the test suite, and the precise statement of the contract, including what \"covers\" means and which operations are deliberately coarse, is in the State Contract page of the Development section.\n\n","title":"The contract, in brief"},{"category":"section","location":"record_replay/#Recording-and-replaying-a-run","page":"Recording & Replaying","text":"A stochastic simulation that misbehaves on step 4,000 of a seeded run is hard to study: you cannot pause it, you cannot step backward, and re-running from the top to reach the interesting moment means waiting through everything before it. The record/replay layer fixes all three. RecordSkeleton captures a run as a replayable TrajectorySkeleton; replay re-executes that skeleton bit-for-bit and hands you the live simulation positioned at any step you name, where you can inspect state, run guard_clauses, or attach probes. This is time-travel debugging for a GSMP. The runbook entry is the terse reference; this page is the narrative walkthrough of the whole workflow.","title":"Recording and replaying a run"},{"category":"section","location":"record_replay/#The-one-workflow","page":"Recording & Replaying","text":"Five pieces fit together:\n\nRecordSkeleton — an opt-in policy you pass to SimulationFSM. It observes a run and captures a TrajectorySkeleton.\nrecorded_skeleton / save_skeleton / load_skeleton — retrieve the skeleton after the run, and optionally persist it.\nreplay — re-execute the skeleton exactly, returning the live sim.\nupto=k — stop the replay after step k, so you hold the state the original run had at that moment.\nprobes and guard_clauses — observe every replayed step, and interrogate a precondition at the paused state.","title":"The one workflow"},{"category":"section","location":"record_replay/#Recording-is-free-when-off","page":"Recording & Replaying","text":"Recording is opt-in and costs a production run nothing: a SimulationFSM built without the policy keyword records nothing and pays nothing (the default NoPolicy compiles every hook to return nothing). The recorder only observes — it never draws from the RNG and never mutates state — so a recorded run's trajectory is byte-identical to the same-seed run unrecorded.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nusing Random: Xoshiro\nimport ChronoSim: precondition, generators, enable, fire!\n\nmodule Race\nusing ChronoSim\nusing ChronoSim.ObservedState\nusing Distributions\nimport ChronoSim: precondition, generators, enable, fire!\n\nconst LA = 2.0\nconst LB = 3.0\n\n@keyedby RaceCell Int64 begin\n    a::Int64\n    b::Int64\nend\n\n@observedphysical RaceBoard begin\n    cell::ObservedVector{RaceCell,Member}\nend\n\nfunction RaceBoard(n::Int)\n    cells = ObservedArray{RaceCell,Member}(undef, n)\n    for i in eachindex(cells)\n        cells[i] = RaceCell(0, 0)\n    end\n    return RaceBoard(cells)\nend\n\nstruct FireA <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireA, state) = state.cell[evt.idx].a >= 0\nenable(::FireA, state, when) = (Exponential(1 / LA), when)\nfire!(evt::FireA, state, when, rng) = (state.cell[evt.idx].a += 1; nothing)\n\nstruct FireB <: SimEvent\n    idx::Int64\nend\n@precondition precondition(evt::FireB, state) = state.cell[evt.idx].b >= 0\nenable(::FireB, state, when) = (Exponential(1 / LB), when)\nfire!(evt::FireB, state, when, rng) = (state.cell[evt.idx].b += 1; nothing)\n\nfunction init!(state, when, rng)\n    state.cell[1].a = 0\n    state.cell[1].b = 0\n    return nothing\nend\nend # module\n\nusing .Race: RaceBoard, FireA, FireB\n\n# Record a 30-step run of the two-clock race.\nrec = RecordSkeleton(metadata=(model=\"race\", n=1, seed=12345))\nsim = SimulationFSM(RaceBoard(1), [FireA, FireB];\n    rng=Xoshiro(12345), policy=rec)\nChronoSim.run(sim, Race.init!, (p, i, e, w) -> i > 30)\nskel = recorded_skeleton(rec)\n\nmetadata is stored opaquely and never read by the framework — put model identification there (module, constructor arguments, git SHA) so a persisted skeleton is self-describing. skel displays as a five-line summary:\n\nTrajectorySkeleton\n  clock key  : Tuple\n  steps      : 30\n  time span  : 0.0 -> 5.5075583294278285\n  top events : FireB 19 | FireA 11\n\nand packs to one line inside another structure: TrajectorySkeleton(30 steps, t=0.0..5.5075583294278285). It is data, not a transcript: per step it holds the fired clock key, firing time, changed addresses, and the full enable/disable/proposal history. Persist it with save_skeleton(\"race.skel\", skel) and reload with load_skeleton(\"race.skel\") — but do not archive .skel files across Julia or package upgrades; the serialization format is version-bound.","title":"Recording is free when off"},{"category":"section","location":"record_replay/#The-determinism-contract","page":"Recording & Replaying","text":"replay does not store state snapshots — it re-runs the model. To make that re-run reproduce the original exactly, it restores the skeleton's pre-init RNG state and drives the same executor kernel run uses, checking the fired (clock_key, when) against the recording at every step. That only works if you give it a factory that rebuilds the identical simulation:\n\nfactory = policy -> (SimulationFSM(RaceBoard(1), [FireA, FireB];\n    rng=Xoshiro(12345),\n    policy=policy),\n    Race.init!)\n\nThe contract on sim_factory is precise:\n\nIt is called as factory(policy) and must return (sim, initializer) — a fresh SimulationFSM and the same initializer you handed run.\nIt must pass through the policy it is given (policy=policy); replay installs its own probe/verification policy there. Dropping it is an ArgumentError.\nIt must rebuild with the same constructor arguments as the recorded run. Any seed/rng it sets is overwritten — replay restores skel.rng_state, so the re-run consumes the identical random stream. Reproducing the constructor is the caller's job; this is why the constructor arguments belong in skel.metadata.\n\nIf the rebuilt run ever fires a different (clock_key, when) than the recording, replay raises a ReplayDivergence — the determinism contract was broken. There is no tolerance: replay is exact.","title":"The determinism contract"},{"category":"section","location":"record_replay/#Time-travel-to-just-before-a-step","page":"Recording & Replaying","text":"Now the payoff. Say step 10 is interesting and you want the state the instant before it fired. Replay with upto=9:\n\nsim9 = replay(factory, skel; upto=9)\nsim9.physical.cell[1].a   # => 2\nsim9.physical.cell[1].b   # => 7\nsim9.when                 # => 1.4823489375249403\n\nupto=9 stops after step 9, so sim9 holds exactly the state the original run had when step 10 was about to fire (two FireAs and seven FireBs have happened). upto=0 returns the freshly-initialized sim; omitting upto replays the whole skeleton. The recording tells you what fires next:\n\nskel.steps[10].clock   # => (:FireA, 1)\nskel.steps[10].when    # => 1.530787664150075","title":"Time-travel to just before a step"},{"category":"section","location":"record_replay/#Interrogating-the-paused-state","page":"Recording & Replaying","text":"At a paused state you can run any read-only diagnostic. guard_clauses evaluates a derived event's @precondition conjunct by conjunct against the live state — the tool for asking why the event that fired next was (or was not) fireable here:\n\nguard_clauses(FireA(1), sim9.physical)\n\n  (\"(state.cell[evt.idx]).a >= 0\", true)\n\nFireA's single guard is true at this state, consistent with it being the event that fires at step 10. On a model with a compound precondition each top-level && conjunct prints on its own line, and the verdict is the first non-true value in source order — see the runbook entry for reading a multi-clause guard and its failure forms.","title":"Interrogating the paused state"},{"category":"section","location":"record_replay/#Watching-the-whole-run-with-probes","page":"Recording & Replaying","text":"To observe every step instead of pausing at one, pass probes — a tuple of functions each called probe(sim, step, phase, event, when), with phase one of :init, :prefire, :postfire:\n\nvals = Tuple{Int,Symbol,Float64}[]\nwatch(s, step, phase, event, when) =\n    phase === :postfire && step <= 5 && push!(vals, (step, clock_key(event)[1], when))\nreplay(factory, skel; probes=(watch,))\n\n(1, :FireB, 0.26521642934417394)\n(2, :FireB, 0.41819747975031385)\n(3, :FireB, 0.504107517742944)\n(4, :FireB, 0.8380454671783777)\n(5, :FireB, 0.8819792933503997)\n\nProbes never perturb the replay — two replays with different probes fire the identical sequence. The same ProbePolicy works on a forward run too: SimulationFSM(...; policy=ProbePolicy((watch,))).","title":"Watching the whole run with probes"},{"category":"section","location":"record_replay/#Failure-forms","page":"Recording & Replaying","text":"The determinism contract surfaces as clear errors:\n\nReplayDivergence at step k — the rebuilt sim fired a different (clock_key, when) than the skeleton recorded. Because replay is exact, any divergence means the re-run is not the recorded run. Here, tampering step 9's recorded clock key produces one:\nReplayDivergence at step 9\n  expected : (:FireB, 1) at t=1.4823489375249403\n  actual   : (:FireA, 1) at t=1.4823489375249403\nReplay is exact: this rebuilt simulation is not the recorded run.\nCheck: same constructor args; same package/Julia versions; no randomness outside sim.rng.\nIn real use the three causes are exactly those the message names: different constructor arguments, a different package/Julia version, or model code drawing randomness from outside sim.rng. When only the time differs the message reads time differs by …; an actual : (no event; sampler exhausted before this step) means the re-run ran out of events early — again a determinism break.\nArgumentError: sim_factory must pass the policy it is given — the factory dropped the policy argument. Add policy=policy to the SimulationFSM call.\nArgumentError: upto=… is outside this skeleton's 0:N steps — upto was negative or larger than the recorded step count.\nArgumentError: sim_factory(policy) must return (sim, initializer) — the factory returned something other than the required 2-tuple.\nArgumentError: this RecordSkeleton has not observed a run — you called recorded_skeleton before run. Record first.","title":"Failure forms"},{"category":"section","location":"record_replay/#What-to-do-with-it","page":"Recording & Replaying","text":"Bisect a bad run. Binary-search upto=k until the paused state first shows the corruption; the step that produced it is your suspect.\nFeed replay to other verbs. The sim_factory here is exactly the one whynot and validate_trace take, and the skeleton is what whystopped reads. Record once, and every offline diagnostic runs on the same artifact.\nReproduce for a colleague. A saved .skel plus the constructor arguments in its metadata is a complete, replayable bug report.","title":"What to do with it"},{"category":"section","location":"record_replay/#Related","page":"Recording & Replaying","text":"Runbook: Record a trajectory skeleton, Replay a recorded run, and Explain a precondition clause by clause — the mechanical references.\nDeclaring and checking invariants — compose RecordSkeleton with CheckInvariants so a violation carries a replayable prefix.\nDebugging a simulation — the why-verbs that read a recorded skeleton.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"},{"category":"section","location":"derivation_internals/#Derivation-Internals","page":"Derivation Internals","text":"This page explains precisely how @precondition turns an enabling rule into generators. The Generators page of the manual tells a modeler what to write; this page tells a contributor what the machinery does and why it is sound. The implementation lives in src/derive.jl, and the runtime checks live in src/coverage.jl.","title":"Derivation Internals"},{"category":"section","location":"derivation_internals/#Three-binding-times","page":"Derivation Internals","text":"The derivation is staged, and each stage runs with exactly the information that is available to it.\n\nMacro-expansion time. The @precondition macro sees only syntax. It walks the body, resolves local aliases, classifies every state read, and embeds the result — a vector of ReadSpec values — into the emitted code. It emits the precondition method verbatim, a generators(::Type{Evt}) method that defers to the next stage, and a derivation_spec(::Type{Evt}) method that exposes the specs to diagnostics and to the coverage oracle.\nSetup time. The first call to generators(Evt), which happens when SimulationFSM assembles its generator search, runs derived_generators(Evt, specs). At this point the event type is a real type, so field names, field types, and @domain methods are all available. This stage deduplicates and merges the specs, resolves a domain for every field that some trigger must enumerate, reports missing domains as errors, and closes over everything to build ordinary EventGenerator values. The runtime dispatch (GeneratorSearch) does not know derived generators from hand-written ones.\nLoop time. The generator closures run when a matching address changes. A closure receives the concrete indices from the changed address, reconstructs the event's identifying fields from them, checks any literal-index guards, enumerates the domains of unbound fields, and calls generate for each candidate. Domain closures execute against the live state, so container-keyed domains track the container's current contents.\n\nReaders who know partial evaluation will recognize the staging: the macro performs a binding-time separation in the sense of Jones, Gomard, and Sestoft, with the event type and state left dynamic until setup and loop time respectively.","title":"Three binding times"},{"category":"section","location":"derivation_internals/#What-the-taint-pass-records","page":"Derivation Internals","text":"A ReadSpec describes one read site: the masked address pattern (field names kept, index positions replaced by a placeholder), one index classification per index position, and the source text for diagnostics. Index classifications are the heart of the analysis.\n\nA field binding records that this position is exactly evt.f (or a local provably equal to it), so at loop time the concrete index at this position is the value of field f.\nA literal records a constant index, which becomes a runtime guard: the trigger matches the pattern but proposes only when the concrete index equals the literal.\nA tainted position is anything else — an index computed from state, a loop variable, an arithmetic expression over event fields, a helper's return value. Tainted means the address alone cannot identify the event instance.\nA tuple index (a dictionary keyed by tuples, or a multi-dimensional array) classifies each component separately.\n\nThe walker handles straight-line code, if/&&/||/comparisons, for and while loops (including iteration over state containers with destructuring), and the reducers any/all/count/sum/prod/ minimum/maximum over generator expressions, which it walks exactly like loops. Local variables are tracked in scoped environments: a local bound to a state access acts as an alias whose reads resolve through it, and a local bound to an expression over event fields and literals is evt-pure and may appear in indices without tainting them. Aliases introduced inside a loop or branch are scoped to it.\n\nCalls are the fragment boundary. A call whose arguments include the state, a state alias, or a state access is admissible only if the callee is registered: @fragment registers a helper's parameters and body at macro-expansion time, keyed by module, name, and arity, and @precondition registers its own body keyed by event type. At a registered call site the walker substitutes the actual argument expressions for the formals, renames the callee's locals with fresh symbols so call sites stay independent, and walks the substituted body under the same judgment, to a bounded depth and with cycle detection. Substitution is what preserves precision: a caller passing evt.f into a parameter used as an index yields a field binding, not a widened trigger. For calls to precondition(EvtType(args...), state), the field-to-argument correspondence comes from fieldnames reflection on the already-defined event type. An unregistered callee that receives state is an error, never a guess.","title":"What the taint pass records"},{"category":"section","location":"derivation_internals/#From-specs-to-triggers","page":"Derivation Internals","text":"Setup time groups the specs by masked pattern and merges each group into one trigger.\n\nIf every spec in the group is clean, the trigger is clean. It carries one binding set per distinct way the pattern's indices bind event fields, and at loop time it proposes one event per binding set, with literal guards applied and any unbound fields enumerated over their domains.\nIf any spec in the group is tainted, the whole trigger widens: it ignores the concrete indices and proposes the event over the full product of its field domains. Widening a pattern that also has clean specs is deliberate, because the widened proposals are a superset of the bound ones.\n\nDomains resolve per field, in a fixed order of precedence: an explicit @domain method wins; otherwise, if some clean spec binds the field at a container index position, the container's eachindex, keys, projected key component, or axis supplies the domain; otherwise a finite field type (Enum or Bool) supplies its instances; otherwise setup fails with a message naming the field and all three attempts. A scalar state field (a read like state.floor_cnt) produces a single-component pattern with an empty binding set, which behaves like a widened trigger for that one address.","title":"From specs to triggers"},{"category":"section","location":"derivation_internals/#Why-this-is-sound","page":"Derivation Internals","text":"The generator contract is that the proposed events must be a superset of the events whose enabling could have changed. Two observations carry the argument. First, within the analyzed fragment, every address a precondition can dynamically read is an instance of one of its recorded patterns, with the concrete components at bound positions equal to the event's field values — the walker collects every syntactic form that can read state and errs on anything it cannot see through. Second, if an event's enabling value differs between the states before and after a firing, the precondition must have read some changed address in one of those states, so the changed address matches one of the event's patterns, and the trigger at that pattern proposes the event: exactly when clean bindings recover it, or by domain enumeration when widened. Over-proposal is filtered by the precondition; under-proposal cannot occur within the fragment. The remaining obligations — that recorded writes cover actual changes, and that declared domains cover the values an enabled event can take — belong to the state contract and to domain resolution respectively.\n\nReaders familiar with incremental computation will recognize the division of labor: for events that are already enabled, the framework re-executes exactly the reads that changed, in the manner of self-adjusting computation, while the derived triggers handle the case self-adjusting computation leaves open, namely computations (events) that do not exist yet. Production-rule systems solved the matching side of this problem with the Rete algorithm, but with hand-declared patterns; the derivation's contribution is that the patterns come from the enabling rule itself.","title":"Why this is sound"},{"category":"section","location":"derivation_internals/#The-runtime-checks","page":"Derivation Internals","text":"Two switchable diagnostics tie the static analysis back to observed behavior.\n\nThe coverage oracle, check_derivation_coverage(true), intercepts every precondition evaluation of an event type that has a derivation_spec method and asserts that each address the evaluation actually read matches one of the derived patterns after masking. Its error distinguishes an address whose leading field no spec mentions (possibly a read of untracked configuration, which suggests a Param wrapping) from an address that matches a spec's container but not its shape (which indicates a derivation bug). Only precondition reads are checked: enable reads are rate dependencies, which the dynamic dependency network owns for enabled events, and which birth triggers never need.\n\nThe candidate counters, collect_generation_stats(true), tally proposed and admitted events per event type, so the cost of widening and domain enumeration is measurable rather than guessed at. The differential tests in ChronoSimExamples.jl run both twins of each model — hand-written and derived generators — under one seed and assert byte-identical trajectories, which is made meaningful by the framework's policy of processing event candidates in sorted clock-key order, so that trajectories depend on the proposed set and not on the order generators emit proposals.\n\n","title":"The runtime checks"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"This page lists every documented name in ChronoSim and its ObservedState submodule. The names a model uses directly are the event interface (SimEvent, precondition, enable, reenable, fire!, isimmediate), the generator macros (@precondition, @conditionsfor, @reactto, @fragment, @domain), the state macros and containers (@observedphysical, @keyedby, ObservedArray, ObservedVector, ObservedMatrix, ObservedDict, ObservedSet, Param), the simulation itself (SimulationFSM, run), and the diagnostics (derivation_report, check_derivation_coverage, collect_generation_stats, generation_stats, reset_generation_stats!). Everything else documented here is infrastructure that the manual pages mention in passing.\n\n","title":"Reference"},{"category":"section","location":"reference/#ChronoSim","page":"Reference","text":"","title":"ChronoSim"},{"category":"section","location":"reference/#ChronoSim.ObservedState","page":"Reference","text":"","title":"ChronoSim.ObservedState"},{"category":"type","location":"reference/#ChronoSim.AddressPattern","page":"Reference","text":"AddressPattern\n\nA masked address pattern: mask is the placekey_mask_index tuple of Member/MEMBERINDEX components (generators.jl), indices holds one constraint (FieldBinding/LiteralIndex/TaintedIndex/TupleIndex, derive.jl) per MEMBERINDEX position in path order, and subtree=true means the pattern covers this address and every descendant.\n\n\n\n\n\n","title":"ChronoSim.AddressPattern"},{"category":"type","location":"reference/#ChronoSim.CheckEffects","page":"Reference","text":"CheckEffects(events::AbstractVector) -> CheckEffects\n\nAn ExecutionPolicy that verifies, after initialization and after every fired event, that every captured changed address matches some WriteSpec mask of the fired event type (unioned with the specs of any isimmediate event types in events, since immediate-event writes merge into the same changed_places). A miss throws EffectCoverageError; event types without an effect_spec are skipped.\n\nTest-tier and opt-in: pass policy=CheckEffects(events) to SimulationFSM or compose it via PolicyStack. The policy is read-only and consumes no randomness, so trajectories with it on equal trajectories with it off.\n\n\n\n\n\n","title":"ChronoSim.CheckEffects"},{"category":"type","location":"reference/#ChronoSim.CheckInvariants","page":"Reference","text":"CheckInvariants(model::Module)\n\nAn ExecutionPolicy that evaluates every @invariant registered by model after initialization and after every fired event, in declaration order. On the first invariant that returns false it throws an InvariantViolation identifying the invariant, the firing event, and the written addresses that the invariant reads (the guilty addresses).\n\nChecking is opt-in and debug/test-tier: pass policy=CheckInvariants(MyModel) to SimulationFSM, or compose it with a recorder so the violation carries a replayable prefix:\n\nrec = RecordSkeleton()\nsim = SimulationFSM(physical, events; seed=42,\n    policy=PolicyStack(rec, CheckInvariants(MyModel)))\n\nThrows an ArgumentError at construction when model registers no invariants. The per-fire cost is the cost of the invariant bodies themselves; state reads are only captured on the failure path.\n\n\n\n\n\n","title":"ChronoSim.CheckInvariants"},{"category":"type","location":"reference/#ChronoSim.DepNetNaive","page":"Reference","text":"For testing, we make an equivalent version of the dependency network but this one uses a very different internal structure, an edge list.\n\n\n\n\n\n","title":"ChronoSim.DepNetNaive"},{"category":"type","location":"reference/#ChronoSim.DependencyNetwork","page":"Reference","text":"This is a multi-graph from places to events. It is a multi-graph because there can be two kinds of edges from the same place to the same event: an enabling edge and a rate edge. The graph is mutable, too, so you can add and remove events which adds and removes edges from the graph.\n\n\n\n\n\n","title":"ChronoSim.DependencyNetwork"},{"category":"type","location":"reference/#ChronoSim.DerivationCoverageError","page":"Reference","text":"DerivationCoverageError\n\nA precondition read an address no derived trigger covers. classification is :missing_field when the address's top-level container is named by no spec at all (a plausible dead read or unobserved-parameter read — a future-allowlist candidate) or :shape_mismatch when the same container is covered but at a different leaf/index shape (a genuine index/classification miss in the derivation).\n\n\n\n\n\n","title":"ChronoSim.DerivationCoverageError"},{"category":"type","location":"reference/#ChronoSim.DynamicCoverage","page":"Reference","text":"DynamicCoverage\n\nResult of static_covers_dynamic: covered, plus the runtime missing_reads and missing_edges the static report failed to cover.\n\n\n\n\n\n","title":"ChronoSim.DynamicCoverage"},{"category":"type","location":"reference/#ChronoSim.EffectCoverageError","page":"Reference","text":"EffectCoverageError\n\nA fired event changed an address that no WriteSpec of the fired (or immediate) event types covers. classification is :missing_container when the address's top-level field is named by no spec at all (an undeclared effect — the seeded-bug class) or :shape_mismatch when the container is covered but at a different leaf/index shape (a walker classification miss, or an observed-container-valued field assignment). immediate_types names the isimmediate event types whose specs were unioned into the check.\n\n\n\n\n\n","title":"ChronoSim.EffectCoverageError"},{"category":"type","location":"reference/#ChronoSim.EffectSpec","page":"Reference","text":"EffectSpec\n\nEverything @fire derived for one event type. writes are the static write sites; reads are the body's state reads (free information — no covering-trigger or zero-read discipline applies to them); widened_writes counts writes with any TaintedIndex (the over-approximation counter); notes records widening reasons, opaque-call sightings, when-demotions, and alias-staleness demotions.\n\n\n\n\n\n","title":"ChronoSim.EffectSpec"},{"category":"type","location":"reference/#ChronoSim.EnableRecord","page":"Reference","text":"EnableRecord{CK}\n\nOne distribution commitment to the sampler: a clock key clock, the distribution handed to the sampler, and te, the absolute zero time of that distribution. Stored inside a SkeletonStep or SkeletonInit.\n\n\n\n\n\n","title":"ChronoSim.EnableRecord"},{"category":"type","location":"reference/#ChronoSim.EventDependency","page":"Reference","text":"EventDependency{ClockKey}(event_generator)\n\nRepresents the dependency graph between the set of events and the set of physical states. It uses clock keys to represent events and physical addresses to represent the physical state. This struct combines dynamic event generation with a static graph in order to present to the framework a unified version of the bipartite graph of events and physical state. It could be replaced by a static graph of events and physical states, such as is used in generalized stochastic Petri nets (GSPN).\n\n\n\n\n\n","title":"ChronoSim.EventDependency"},{"category":"type","location":"reference/#ChronoSim.EventGenerator","page":"Reference","text":"EventGenerator(match_what, matchstr, generator::Function)\n\nWhen an event fires, it changes the physical state. The simulation observes which parts of the physical state changed and sends those parts to this EventGenerator. The EventGenerator is a rule that matches changes to the physical state and creates SimEvent that act on that physical state.\n\nThe matchstr is a list of symbols (array_name, ℤ, struct_member). The ℤ represents the integer index within the array. For instance, if we simulated chess, it might be (:board, ℤ, :piece).\n\nThe generator is a callback that the simulation uses to determine which events need to be enabled given recent changes to the state of the board. Its signature is:\n\n    callback_function(f::Function, physical_state, indices...)\n\nHere the indices are the integer index that matches the ℤ above. This callback function should look at the physical state and call f(transition) where transition is an instance of SimEvent.\n\n\n\n\n\n","title":"ChronoSim.EventGenerator"},{"category":"type","location":"reference/#ChronoSim.ExecutionPolicy","page":"Reference","text":"ExecutionPolicy\n\nAbstract supertype for executor policies. A policy observes the simulation executor at seven hook points: on_preinit, on_init, on_propose, on_enable, on_disable, on_prefire, and on_postfire. Every hook has a no-op default returning nothing, so a concrete policy overrides only the hooks it needs. The policy is stored as a type parameter of SimulationFSM; the default NoPolicy compiles to nothing at every hook site and costs a production run zero time and zero allocation.\n\nHooks are called with the live simulation as the second argument. A policy must not mutate the physical state, the sampler, or the RNG; doing so breaks the determinism guarantees the differential tests enforce.\n\n\n\n\n\n","title":"ChronoSim.ExecutionPolicy"},{"category":"type","location":"reference/#ChronoSim.GuardEvalError","page":"Reference","text":"GuardEvalError\n\nStructured failure from guard_clauses. kind is one of :no_precondition (event not registered via @precondition), :unsupported_call / :unsupported_node / :early_return / :mutating_call / :unknown_symbol (construct outside the interpreter's fragment — the payload names it), or :prelude_threw (a statement before the precondition's returned expression raised cause).\n\n\n\n\n\n","title":"ChronoSim.GuardEvalError"},{"category":"type","location":"reference/#ChronoSim.GuardSpec","page":"Reference","text":"GuardSpec\n\nThe static guard-read specification baked by @guard. reads are ReadSpecs (the same IR as derivation_spec); whole_containers are Members read via length/keys/values/pairs/isempty without a covering element read (recorded as container-level subtree reads); notes records e.g. a zero-read guard.\n\n\n\n\n\n","title":"ChronoSim.GuardSpec"},{"category":"type","location":"reference/#ChronoSim.InitializeEvent","page":"Reference","text":"InitializeEvent is a concrete transition type that represents the first event in the system, initialization.\n\n\n\n\n\n","title":"ChronoSim.InitializeEvent"},{"category":"type","location":"reference/#ChronoSim.InvariantDef","page":"Reference","text":"InvariantDef\n\nOne registered @invariant: name, the compiled Bool-valued checker function of the physical state, the declared state parameter statesym (free in body), the source body AST (the model-checker compiler's input), and the declaration source::LineNumberNode.\n\n\n\n\n\n","title":"ChronoSim.InvariantDef"},{"category":"type","location":"reference/#ChronoSim.InvariantViolation","page":"Reference","text":"InvariantViolation\n\nThrown by CheckInvariants when a registered invariant evaluates to false. Carries the full forensic payload; whystopped prints it, and showerror renders the same fields.\n\nFields\n\nname::String — the violated invariant's declared name.\nmodel::Module — the module whose registry supplied it.\nsource::LineNumberNode — where the invariant was declared.\nstep::Int — fires since initialization; 0 means the initializer itself left the state in violation.\nevent::Any — clock key of the event whose fire broke the invariant (the initializer's key at step 0).\nwhen::Float64 — the firing time.\nguilty::Vector{Tuple} — addresses both written by this fire and read by the failing invariant evaluation: which write broke it. May be empty when the invariant reads untracked state or the overlap is invisible.\nreads::Vector{Tuple} — every address the failing evaluation read.\nchanged::Vector{Tuple} — every address this fire wrote.\nreproduced::Bool — false when re-evaluation under read capture returned true, i.e. the invariant is not a pure function of state.\nskeleton::Union{Nothing,TrajectorySkeleton} — the recorded prefix, when a RecordSkeleton shares the policy stack.\nreplay_command::Union{Nothing,String} — the exact replay(...) call that reproduces the state one step before the violation; nothing when no skeleton was recorded or the violation is at step 0.\n\n\n\n\n\n","title":"ChronoSim.InvariantViolation"},{"category":"type","location":"reference/#ChronoSim.LintAllow","page":"Reference","text":"LintAllow(; reader=nothing, writer=nothing, mask=nothing, reason)\n\nAn allowlist descriptor for assert_lint_clean. A nothing field is a wildcard; mask is matched against the exact string the report prints for the overlap mask (e.g. \"[person, ℤ, waiting]\"). reason is mandatory: it documents WHY this interference is an intended trigger narrowing.\n\n\n\n\n\n","title":"ChronoSim.LintAllow"},{"category":"type","location":"reference/#ChronoSim.LintEdge","page":"Reference","text":"LintEdge\n\nOne interference edge. kind is :write_guard or :write_write; writer/reader are event-type names (for :write_write, the lexically first/second writer); overlap_mask is the address group; level is :warning or :info; trigger_covered/covering_trigger apply to :write_guard; verdict is :overlap/:possible/:empty; note records enumeration outcome.\n\n\n\n\n\n","title":"ChronoSim.LintEdge"},{"category":"type","location":"reference/#ChronoSim.LintFailure","page":"Reference","text":"LintFailure\n\nThrown by assert_lint_clean when the report has warning-level edges not matched by an allowlist entry.\n\n\n\n\n\n","title":"ChronoSim.LintFailure"},{"category":"type","location":"reference/#ChronoSim.LintHarvest","page":"Reference","text":"LintHarvest()\n\nAn ExecutionPolicy that records, at every on_postfire/on_init, each enabled event's enable-dependency places (masked to (reader, place)) and, for each address the fired event changed, the (writer, reader, place) interactions the live dependency network shows. Feed the result to static_covers_dynamic to check the static report covers every runtime edge (soundness direction only). Test-tier and opt-in; do not run it in production replicates.\n\n\n\n\n\n","title":"ChronoSim.LintHarvest"},{"category":"type","location":"reference/#ChronoSim.LintReport","page":"Reference","text":"LintReport\n\nThe result of lint: events (linted names, sorted), edges, dead_addresses, unanalyzed_guards, unanalyzed_effects, the fixed rate_note, and caps (enumeration / missing-physical notes). show prints a bounded summary; print_lint prints every edge.\n\n\n\n\n\n","title":"ChronoSim.LintReport"},{"category":"type","location":"reference/#ChronoSim.Member","page":"Reference","text":"This represents a field in a struct. It is a wrapper around Symbol. Wrap the Symbol so that it doesn't conflict with dictionary keys that are symbols.\n\n\n\n\n\n","title":"ChronoSim.Member"},{"category":"type","location":"reference/#ChronoSim.NoPolicy","page":"Reference","text":"NoPolicy()\n\nThe default ExecutionPolicy: every hook is a no-op that the compiler removes. Constructing a SimulationFSM without a policy keyword uses this policy.\n\n\n\n\n\n","title":"ChronoSim.NoPolicy"},{"category":"type","location":"reference/#ChronoSim.PhysicalState","page":"Reference","text":"PhysicalState is an abstract type from which to inherit the state of a simulation. A PhysicalState should put all mutable values, the values upon which events depend, into TrackedVector objects. For instance:\n\n@tracked_struct Square begin\n    occupant::Int\n    resistance::Float64\nend\n\n# Everything we know about an agent.\n@tracked_struct Agent begin\n    health::Health\n    loc::CartesianIndex{2}\nend\n\nmutable struct BoardState <: PhysicalState\n    board::TrackedVector{Square}\n    agent::TrackedVector{Agent}\nend\n\nThe PhysicalState may contain other properties, but those defined with TrackedVectors are used to compute the next event in the simulation.\n\n\n\n\n\n","title":"ChronoSim.PhysicalState"},{"category":"type","location":"reference/#ChronoSim.PolicyStack","page":"Reference","text":"PolicyStack(policies::ExecutionPolicy...)\n\nCompose several ExecutionPolicys into one: every hook fans out to each member in the given order. The member tuple is a type parameter, so a concrete stack is statically dispatched and an empty stack compiles away like NoPolicy. Order matters when members interact: place a RecordSkeleton before a CheckInvariants so the recorded skeleton already contains the violating step when the violation is thrown.\n\n\n\n\n\n","title":"ChronoSim.PolicyStack"},{"category":"type","location":"reference/#ChronoSim.ProbePolicy","page":"Reference","text":"ProbePolicy(probes::Tuple)\n\nAn ExecutionPolicy that calls each probe at three points of every step: probe(sim, step, phase, event, when) where phase is :init (once, step 0, after initialization), :prefire (before the step's event mutates state; sim.when still holds the previous time), and :postfire (state and enabled set settled, before the user observer). Probes must only observe: never mutate state, the sampler, or the RNG. Installed automatically by replay's probes keyword; usable directly as SimulationFSM(...; policy=ProbePolicy((p1, p2))) to probe a forward run.\n\n\n\n\n\n","title":"ChronoSim.ProbePolicy"},{"category":"type","location":"reference/#ChronoSim.QuintCompilationReport","page":"Reference","text":"QuintCompilationReport\n\nPer-event status and every deviation from a 1:1 transliteration. Base.show prints the plan's bounded plain-text block (stable order):\n\nquint compilation: elevator (9 events)\n  clean   : PickNewDestination CallElevator OpenElevatorDoors ...\n  widened : (none)\n  assumed : (none)\n  skipped : (none)\n  refused : (none)\nconstants promoted: floor_cnt\nrecords collapsed : ElevatorCall -> requested\nfields erased     : (none)\ninvariants        : 8 compiled, 0 refused\nwidenings total   : 0 (v1 refuses where the design allowed widening; markers reserved)\n\n\n\n\n\n","title":"ChronoSim.QuintCompilationReport"},{"category":"type","location":"reference/#ChronoSim.QuintCompileError","page":"Reference","text":"QuintCompileError\n\nAll refusals from one compile_quint call. Each entry names the subject (event, invariant, @fragment helper, or :schema), the offending construct, the source location, and the category: :no_precondition | :no_fire | :float_read | :unclassified_loop | :rand_in_loop | :unsupported_call | :unsupported_node | :loop_read_write_overlap | :unordered_fold | :enum_collision | :unsupported_state | :bitwise_int | :while_loop | :early_return. showerror prints one block per entry, capped at 10 with a \"+k more\" line.\n\n\n\n\n\n","title":"ChronoSim.QuintCompileError"},{"category":"type","location":"reference/#ChronoSim.QuintModel","page":"Reference","text":"QuintModel\n\nA compiled Quint module: name, the module text, the QuintCompilationReport, and the trace-validation metadata: varnames (emitted state vars, schema order), actions::Dict{Symbol,Symbol} (event name → parameterized action name), schema (the reflection result reused by the trace emitter), and partial::Bool.\n\n\n\n\n\n","title":"ChronoSim.QuintModel"},{"category":"type","location":"reference/#ChronoSim.QuintToolchain","page":"Reference","text":"QuintToolchain\n\nLocated checker binaries. quint is an npx --prefix <dir> quint (or bare quint) Cmd, or nothing; java_home is a directory or nothing; versions is filled lazily.\n\n\n\n\n\n","title":"ChronoSim.QuintToolchain"},{"category":"type","location":"reference/#ChronoSim.ReadSpec","page":"Reference","text":"ReadSpec\n\nOne state-read site derived from a @precondition (or @guard) body: matchstr is the masked address path, indices classifies each index position (FieldBinding/LiteralIndex/TaintedIndex/TupleIndex), and source is the original access text for diagnostics.\n\n\n\n\n\n","title":"ChronoSim.ReadSpec"},{"category":"type","location":"reference/#ChronoSim.RecordSkeleton","page":"Reference","text":"RecordSkeleton(; metadata=nothing)\n\nAn ExecutionPolicy that records a TrajectorySkeleton of the run: the pre-initialization RNG state, and per fired event its clock key, firing time, changed state addresses, and the enable/disable/proposal history that preceded it. Recording is opt-in: pass policy=RecordSkeleton() to SimulationFSM; a simulation constructed without it records nothing and pays nothing. metadata is stored opaquely in the skeleton — put model identification (module, constructor arguments, git SHA) there; the framework never reads it.\n\nThe policy only observes: it never draws from the RNG and never mutates state, so a recorded run's trajectory is identical to the same seed run unrecorded. Retrieve the result with recorded_skeleton after run returns. Re-initializing the same simulation discards the previous recording.\n\nrec = RecordSkeleton(metadata=(model=\"sirvillage\", people=30))\nsim = SimulationFSM(physical, events; seed=2938423, policy=rec)\nChronoSim.run(sim, InitEvent(), stop)\nskel = recorded_skeleton(rec)\n\n\n\n\n\n","title":"ChronoSim.RecordSkeleton"},{"category":"type","location":"reference/#ChronoSim.ReplayDivergence","page":"Reference","text":"ReplayDivergence(step, expected, actual)\n\nThrown by replay when the re-executed run fires a different (clock_key, when) than the skeleton recorded at step. expected is (clock, when) from the skeleton; actual is the re-run's (clock, when), or nothing when the re-run's sampler ran out of events before reaching step. Replay is exact — there is no tolerance — so any divergence means the rebuilt simulation is not the recorded one: check constructor arguments, package versions, and that no model code uses randomness outside sim.rng.\n\n\n\n\n\n","title":"ChronoSim.ReplayDivergence"},{"category":"type","location":"reference/#ChronoSim.SimEvent","page":"Reference","text":"SimEvent\n\nThis abstract type is the parent of all transitions in the system.\n\n\n\n\n\n","title":"ChronoSim.SimEvent"},{"category":"method","location":"reference/#ChronoSim.SimulationFSM-Tuple{Any, Any}","page":"Reference","text":"SimulationFSM(physical_state, trans_rules; sampler, key_type, step_likelihood,\n              likelihood_eltype, seed, rng, observer=nothing, policy=NoPolicy())\n\nCreate a simulation.\n\nThe physical_state is of type PhysicalState. The trans_rules are a list of type SimEvent. The seed is an integer seed for a Xoshiro random number generator. The observer is a callback with the signature:\n\nobserver(physical, when::Float64, event::SimEvent, changed_places::AbstractSet{Tuple})\n\nThe changed_places argument is a set-like object with tuples that are keys that represent which places were changed.\n\nThe policy keyword is an ExecutionPolicy that observes the executor at its hook points; it defaults to NoPolicy, which compiles away and costs a production run nothing.\n\nSampler selection\n\nThe sampler keyword takes a CompetingClocks sampler method spec or a CompetingClocks.SamplerBuilder, and SimulationFSM owns the random number generator and builds the underlying SamplingContext itself.\n\nsampler=nothing (default) uses NextReactionMethod(), which reproduces the historical CombinedNextReaction sampler and its exact rng stream.\nsampler=spec where spec isa CompetingClocks.SamplerSpec (e.g. NextReactionMethod(), FirstReactionMethod()) builds a context around that method.\nsampler=builder where builder isa SamplerBuilder uses the builder as-is; in that case key_type, step_likelihood, and likelihood_eltype must not also be passed (the builder already carries them).\n\nThe key_type keyword overrides the clock-key type (default common_base_key_tuple of the events). Set step_likelihood=true to opt in to the trace-likelihood machinery (trace_likelihood); passing a non-Float64 likelihood_eltype (e.g. eltype(θ) for ForwardDiff) auto-enables it.\n\nPassing a sampler instance (e.g. CombinedNextReaction{K,Float64}() or a MemorySampler) is no longer supported and throws an ArgumentError with migration guidance.\n\n\n\n\n\n","title":"ChronoSim.SimulationFSM"},{"category":"type","location":"reference/#ChronoSim.SkeletonInit","page":"Reference","text":"SkeletonInit{CK}\n\nThe initialization record: when (sim.when at initialization), the addresses the initializer wrote (changed), and the clocks enabled (with distributions), disabled (always empty today, kept for format stability), and proposed before the first step.\n\n\n\n\n\n","title":"ChronoSim.SkeletonInit"},{"category":"type","location":"reference/#ChronoSim.SkeletonStep","page":"Reference","text":"SkeletonStep{CK}\n\nThe record of one fired event: clock (the fired clock key), when (firing time), changed (addresses written by the event and any immediate events it triggered, in write order), and the enabled, disabled, and proposed clocks from bringing the event set back into agreement with the new state.\n\n\n\n\n\n","title":"ChronoSim.SkeletonStep"},{"category":"type","location":"reference/#ChronoSim.StopWriteAnalysis","page":"Reference","text":"StopWriteAnalysis\n\nResult of can_stop_change: the verdict (:can_change | :cannot_change | :unknown), the hits (each a (event, write, read) mask triple), the unanalyzed event names lacking an effect_spec, and — when enabled_types was given — the split of hitting event names into enabled_hits and disabled_hits.\n\n\n\n\n\n","title":"ChronoSim.StopWriteAnalysis"},{"category":"type","location":"reference/#ChronoSim.TraceEvaluation","page":"Reference","text":"TraceEvaluation\n\nThe result of evaluating a recorded trace against a model with trace_likelihood.\n\nFields\n\nloglikelihood::L — sum of the per-step log-likelihoods, or -Inf when the trace is infeasible under the model. The element type L is sim.likelihood_eltype (Float64 by default, or a ForwardDiff.Dual when likelihood_eltype=eltype(θ) was passed for gradients).\nfeasible::Bool — true when every step of the trace named an enabled event at a time strictly after the previous event.\nsteps_evaluated::Int — number of steps successfully evaluated; equals length(steploglik). When infeasible, evaluation stopped at step steps_evaluated + 1.\nfirst_infeasible::Union{Nothing,Tuple{Int,Any,Symbol}} — nothing for a feasible trace, else (step, event, reason) for the first failing step. reason is :not_enabled (the event was not in sim.enabled_events when its turn came) or :time_order (its time was not strictly greater than the simulation clock).\nsteploglik::Vector{L} — the per-step log-likelihood contributions, one entry per evaluated step.\n\nThe sim passed to trace_likelihood must be constructed with step_likelihood=true so its SamplingContext records the enabled-clock likelihood. To make the result differentiable with ForwardDiff, also pass likelihood_eltype=eltype(θ), which sets L to the Dual number type.\n\n\n\n\n\n","title":"ChronoSim.TraceEvaluation"},{"category":"type","location":"reference/#ChronoSim.TraceValidationReport","page":"Reference","text":"TraceValidationReport\n\ninvariants/transitions verdicts (:passed|:failed|:skipped|:error — :error means the checker itself crashed or produced unparseable output; the log tail is in skip_reason), steps_checked, steps_total, first_failure (nothing or (step, event, when, stage) — step 0 is the initial state; step k names the recorded event whose transition/poststate failed), skip_reason texts, checker log paths, wall-clock seconds. Base.show is the bounded block captured in the runbook.\n\n\n\n\n\n","title":"ChronoSim.TraceValidationReport"},{"category":"type","location":"reference/#ChronoSim.TrajectorySkeleton","page":"Reference","text":"TrajectorySkeleton{CK}\n\nThe recorded skeleton of one simulation run, produced by RecordSkeleton. CK is the simulation's clock-key type.\n\nFields\n\nrng_state::Xoshiro — a copy of the simulation RNG taken before the initializer ran; restoring it and re-running reproduces the trajectory exactly.\nmetadata::Any — the opaque value given to RecordSkeleton.\ninit::SkeletonInit{CK} — the initialization record: when, the addresses the initializer wrote (changed), and the clocks proposed and enabled (with distributions) before the first step.\nsteps::Vector{SkeletonStep{CK}} — one entry per fired event, in firing order. Each step carries clock (the fired clock key), when (firing time), changed (addresses written by the event and any immediate events it triggered, in write order), and the enabled (with the committed distribution object and its zero time te), disabled, and proposed clock keys from bringing the event set back into agreement with the new state.\n\nA step's enabled includes re-enables (distribution replacements for a still-enabled clock); an enabled interval for a clock runs from its first enable after it was last disabled or fired. init.disabled is always empty under the current executor and is kept for format stability.\n\n\n\n\n\n","title":"ChronoSim.TrajectorySkeleton"},{"category":"type","location":"reference/#ChronoSim.WhynotReport","page":"Reference","text":"WhynotReport\n\nResult of whynot. Fields, in order:\n\nclock::Any — clock_key of the queried event.\nstage::Symbol — :never_proposed | :rejected | :enabled_never_fired | :fired.\nnsteps::Int — recorded steps examined (the whole skeleton).\ndetail::NamedTuple — stage-specific payload (schemas below).\n\n:never_proposed — (trigger_source, required, fired_triggers, true_reads, precondition_now, missing_triggers, near_misses, near_miss_total, exact_hits, note). trigger_source is :derivation_spec or :hand_written; required is the declared trigger addresses (derived: evt-bound indices substituted; hand-written: wildcards left as the mask sentinel, printing _index); true_reads are the precondition's reads at the final replayed state; precondition_now is that evaluation's value (Bool, or the exception it threw); missing_triggers are true_reads covered by no declared trigger mask; near_misses is a vector of (address, step, clock, when, class) NamedTuples with class ∈ (:index_near_miss, :container_near_miss), capped at 12 with the full count in near_miss_total; exact_hits (same NamedTuple shape, class = :exact) holds recorded writes that exactly matched a declared trigger — should-be-impossible generator/dedup anomalies, printed with a warning line, never dropped.\n\n:rejected — (n_proposals, rejection_steps, examined). Each element of examined is (step, when, clause_analysis, verdict, clauses, failing_clause, reads).\n\n:enabled_never_fired — (intervals, total_duration, n_enables, distributions, preempted_by, sampled_time_note).\n\n:fired — (count, occurrences).\n\n\n\n\n\n","title":"ChronoSim.WhynotReport"},{"category":"type","location":"reference/#ChronoSim.WhyrunningReport","page":"Reference","text":"WhyrunningReport\n\nResult of whyrunning. Fields, in order:\n\npredicate_value::Bool — the predicate's value now (true means the question was mistaken: the run will stop at the next check).\nreads::Vector — one (address, value, writer) NamedTuple per address the predicate read, in read order.\nwindow::UnitRange{Int} — the recorded steps summarized.\ntop_events::Vector — up to 5 (event, count, writes, touches_predicate) NamedTuples.\npredicate_writes_in_window::Vector — (step, clock, address) for every recorded write in the window whose address exactly equals a predicate read.\nreachability::String — Phase-2 stub, see whyrunning.\n\n\n\n\n\n","title":"ChronoSim.WhyrunningReport"},{"category":"type","location":"reference/#ChronoSim.WhystoppedReport","page":"Reference","text":"WhystoppedReport\n\nResult of whystopped. Fields, in order: kind::Symbol (:invariant_violation | :end_of_run), invariant::Union{Nothing,String}, step::Int, event::Any (breaking event's clock key, or the last fired clock), when::Float64, guilty::Vector ((address, writer, prior_writer) NamedTuples; empty for :end_of_run), still_enabled::Vector (clock keys open at the end of the skeleton; empty for :invariant_violation), verdict::Symbol (:invariant_violated | :no_events_enabled | :stopped_while_events_enabled), and replay_command::Union{Nothing,String}.\n\n\n\n\n\n","title":"ChronoSim.WhystoppedReport"},{"category":"type","location":"reference/#ChronoSim.WriteSpec","page":"Reference","text":"WriteSpec\n\nOne static write site derived from a @fire body. Mirrors ReadSpec with the effect payload Phases 3 (interference lints) and 4 (Quint effect lowering) consume.\n\nFields: matchstr/indices use the same alphabet as ReadSpec (Member, MEMBERINDEX; FieldBinding/LiteralIndex/TaintedIndex/TupleIndex); subtree is true for whole-element ops (a compound setindex!/@obswrite container write) that cover this address and every descendant; op is the mutation kind; rhs is the value classification (:evt_pure | :state_expr | :stochastic | :opaque); rhs_ast is the always-kept alias-resolved rhs (or the mutator call) for Phase 4; source is the statement text for diagnostics.\n\n\n\n\n\n","title":"ChronoSim.WriteSpec"},{"category":"type","location":"reference/#ChronoSim.WriterIndex","page":"Reference","text":"WriterIndex(skeleton) -> WriterIndex\n\nAn index over the skeleton's recorded writes (init.changed and every steps[i].changed), built once in one pass, for repeated last_writer queries.\n\n\n\n\n\n","title":"ChronoSim.WriterIndex"},{"category":"method","location":"reference/#ChronoSim._build_schema-Tuple{Module, Any, Any}","page":"Reference","text":"_build_schema(model, physical, events) -> _QuintSchema\n\nReflect the physical type into a _QuintSchema. events drives D6 constant promotion (a scalar with no WriteSpec is a pure val).\n\n\n\n\n\n","title":"ChronoSim._build_schema"},{"category":"method","location":"reference/#ChronoSim._classify_rhs-Tuple{ChronoSim._FireCtx, Any}","page":"Reference","text":"_classify_rhs(ctx, rhs) -> Symbol\n\nClassify an assignment rhs by the precedence :stochastic > :opaque(when) > :evt_pure > :state_expr > :opaque. Appends a note for the when and alias-staleness demotions.\n\n\n\n\n\n","title":"ChronoSim._classify_rhs"},{"category":"function","location":"reference/#ChronoSim._derive_effectspecs","page":"Reference","text":"_derive_effectspecs(body, statesym, evtsym, whensym, rngsym, mod) -> EffectSpec\n\nRun the write-side taint pass over a fire! body. Errors (macro time) only on the zero-write case and on unrecognized bang-mutations of state; reads are collected without any covering-trigger enforcement.\n\n\n\n\n\n","title":"ChronoSim._derive_effectspecs"},{"category":"function","location":"reference/#ChronoSim._derive_readspecs","page":"Reference","text":"_derive_readspecs(body, statesym, evtsym) -> (specs, notes)\n\nRun the taint pass over a precondition body and return the derived ReadSpecs. Throws (macro-time) on out-of-fragment constructs, uncovered whole-container reads, and zero-read preconditions.\n\n\n\n\n\n","title":"ChronoSim._derive_readspecs"},{"category":"method","location":"reference/#ChronoSim._emit_value-Tuple{ChronoSim._QuintSchema, Any, Any}","page":"Reference","text":"_emit_value(schema, T, v) -> String\n\nSerialize a live value v of Julia type T to a Quint literal (D15). Dict/array keys are emitted in canonical order for byte-stable goldens.\n\n\n\n\n\n","title":"ChronoSim._emit_value"},{"category":"method","location":"reference/#ChronoSim._intersect_verdict-Tuple{ChronoSim.AddressPattern, ChronoSim.AddressPattern}","page":"Reference","text":"_intersect_verdict(a::AddressPattern, b::AddressPattern) -> Symbol\n\nThree-valued interference kernel: :overlap (a concrete address matches both), :possible (index constraints could be uninhabited — the enumeration pass refines it), or :disjoint (provably no shared address). The sound direction: never :disjoint unless provably so.\n\n\n\n\n\n","title":"ChronoSim._intersect_verdict"},{"category":"method","location":"reference/#ChronoSim.access_to_argnames-Tuple{Expr}","page":"Reference","text":"access_to_argnames(expr)\n\nIt extracts variable arguments to an accessor expression. An accessor expression uses getfield or getindex to find members of structs and containers. Turns this: :(agent[j].value[k]), into this: [:(j), :(k)].\n\n\n\n\n\n","title":"ChronoSim.access_to_argnames"},{"category":"method","location":"reference/#ChronoSim.access_to_searchkey-Tuple{Expr}","page":"Reference","text":"access_to_searchkey(expr::Expr)\n\nConvert a Julia access expression into a search key pattern for the event generator system. Field names become Member objects, and array/container indices become MEMBERINDEX placeholders.\n\nExamples\n\n# Simple member access\njulia> access_to_searchkey(:(obj.field))\n[Member(:obj), Member(:field)]\n\n# Array access with index\njulia> access_to_searchkey(:(arr[5].field))  \n[Member(:arr), MEMBERINDEX, Member(:field)]\n\n# Multi-dimensional array access\njulia> access_to_searchkey(:(board[i][j].piece))\n[Member(:board), MEMBERINDEX, MEMBERINDEX, Member(:piece)]\n\nThe resulting pattern is used to match against place keys in the simulation's state change tracking.\n\n\n\n\n\n","title":"ChronoSim.access_to_searchkey"},{"category":"method","location":"reference/#ChronoSim.assert_lint_clean-Tuple{LintReport}","page":"Reference","text":"assert_lint_clean(report::LintReport; allow=LintAllow[], io=stdout)\n\nThrow LintFailure if report contains any warning-level edge not matched by an allow entry. Info-level findings never fail. Unused allow entries print a staleness notice to io (not a failure). This is the CI entry point.\n\n\n\n\n\n","title":"ChronoSim.assert_lint_clean"},{"category":"method","location":"reference/#ChronoSim.can_stop_change-Tuple{Any, AbstractVector}","page":"Reference","text":"can_stop_change(stop_reads, events; enabled_types=nothing) -> StopWriteAnalysis\n\nAnswer whyrunning's unreachability question: can any event ever write an address the stop predicate reads? stop_reads is the address iterable that capture_state_reads returned for the stop-predicate evaluation; events is the model's event-type vector. Every event type's WriteSpec masks are intersected with the masked stop reads.\n\nThe verdict is :cannot_change only when every event type has an effect_spec and no mask intersects; any type without a spec makes the verdict :unknown and is listed in unanalyzed. With enabled_types, hits split into currently-enabled and disabled events.\n\nThe v1 mask intersection ignores index constraints and treats subtree masks as covering all descendants — it over-approximates toward :can_change, never toward :cannot_change, so the unreachability claim stays sound. masks_intersect (lint.jl) offers index-aware unification when more precision is needed.\n\n\n\n\n\n","title":"ChronoSim.can_stop_change"},{"category":"method","location":"reference/#ChronoSim.capture_state_changes-Tuple{Function, Any}","page":"Reference","text":"capture_state_changes(f::Function, physical_state)\n\nThe callback function f will modify the physical state. This function records which parts of the state were modified. The callback should have no arguments and may return a result.\n\n\n\n\n\n","title":"ChronoSim.capture_state_changes"},{"category":"method","location":"reference/#ChronoSim.capture_state_reads-Tuple{Function, Any}","page":"Reference","text":"capture_state_reads(f::Function, physical_state)\n\nThe callback function f will read the physical state. This function records which parts of the state were read. The callback should have no arguments and may return a result.\n\n\n\n\n\n","title":"ChronoSim.capture_state_reads"},{"category":"method","location":"reference/#ChronoSim.check_derivation_coverage-Tuple{Bool}","page":"Reference","text":"check_derivation_coverage(flag::Bool)\ncheck_derivation_coverage() -> Bool\n\nToggle the derivation-coverage oracle (default off). When on, each precondition evaluation of an event type that has a derivation_spec verifies that every place address the precondition read is covered by one of the derived triggers. An uncovered read is a soundness bug in the derivation and throws DerivationCoverageError. Events without a derivation_spec (hand-written generators) are skipped.\n\nOnly precondition reads are checked. enable/reenable reads are rate dependencies that legitimately touch state outside the precondition — and thus outside derivation_spec, which is derived from the precondition body alone — so checking them would false-positive on every model whose rate depends on state the condition does not.\n\n\n\n\n\n","title":"ChronoSim.check_derivation_coverage"},{"category":"method","location":"reference/#ChronoSim.clock_key-Tuple{T} where T<:SimEvent","page":"Reference","text":"clock_key(::SimEvent)::Tuple\n\nAll SimEvent objects are immutable structs that represent events but don't carry any mutable state. A clock key is a tuple version of an event.\n\n\n\n\n\n","title":"ChronoSim.clock_key"},{"category":"method","location":"reference/#ChronoSim.collect_generation_stats-Tuple{Bool}","page":"Reference","text":"collect_generation_stats(flag::Bool)\ncollect_generation_stats() -> Bool\n\nToggle accumulation of per-event-type candidate counts (default off). See generation_stats.\n\n\n\n\n\n","title":"ChronoSim.collect_generation_stats"},{"category":"method","location":"reference/#ChronoSim.common_base_key_tuple-Tuple{Any}","page":"Reference","text":"Look at events and determine a common base type. Internally the simulation tracks events with sets of tuples by turning each event instance into a tuple. If all the tuples have the same type, this should turn out to be performant.\n\n\n\n\n\n","title":"ChronoSim.common_base_key_tuple"},{"category":"method","location":"reference/#ChronoSim.compile_quint-Tuple{Module, AbstractVector, Any}","page":"Reference","text":"compile_quint(model::Module, events::AbstractVector, physical;\n              name::String=lowercase(String(nameof(model))),\n              skip_events::Vector{Symbol}=Symbol[],\n              assume_true_guards::Vector{Symbol}=Symbol[],\n              invariants::Module=model,\n              mutate_for_test=nothing) -> QuintModel\n\nCompile a ChronoSim model to a Quint module. Inputs are the model module (source of enum/const/type bindings and, by default, of @invariants), the event-type vector (the same list passed to SimulationFSM), and a live physical state (the init snapshot, array extents, and Param constants). invariants names the module whose @invariants are compiled when they live apart from the events (e.g. a hand-written twin module).\n\nEvery event must carry @precondition (guard AST via precondition_ast) and @fire (effect AST via fire_ast), except: an event named in assume_true_guards compiles with guard true (for hand-written-generator events whose precondition literally returns true); an event named in skip_events is omitted and the module is marked // PARTIAL. Any other gap, any out-of-fragment construct, and any float read in a lowered expression is a QuintCompileError — all problems are gathered and reported in one throw, each naming the event (or invariant/helper), the construct, and its source. Where the design allowed widening (dict-key argmin tie-breaks, non-uniform draw supports), v1 refuses instead — never a silent over-approximation.\n\nThe result carries the module text, the QuintCompilationReport (clean/assumed/skipped per event; erased fields; promoted constants; collapsed records), and the metadata validate_trace needs. Compilation is pure (no eval, no I/O, no toolchain requirement).\n\nmutate_for_test = (event=:MoveElevator, from=:<, to=:<=, occurrence=1) mis-emits one guard comparison operator — test-only, proves trace validation can fail. The mutation applies to the named event's GUARD only and is recorded in report.mutated only when the occurrence was actually reached.\n\n\n\n\n\n","title":"ChronoSim.compile_quint"},{"category":"method","location":"reference/#ChronoSim.deal_with_changes-Union{Tuple{CK}, Tuple{Sampler}, Tuple{State}, Tuple{SimulationFSM{State, Sampler, CK}, Any, Any, Any}} where {State, Sampler, CK}","page":"Reference","text":"deal_with_changes(sim::SimulationFSM, event_dependency, fired_event_keys, changed_places)\n\nAn event changed the state. This function modifies events to respond to changes in state.\n\nsim - the simulation\nevent_dependency - the bipartite graph of addresses and clocks, separated out from the sim so that we can test more easily.\nfired_event_keys - a list of what fired. It's a list because of immediate events.\nchanged_places - the addresses of physical state affected by firing.\n\n\n\n\n\n","title":"ChronoSim.deal_with_changes"},{"category":"method","location":"reference/#ChronoSim.derivation_report-Union{Tuple{T}, Tuple{IO, Type{T}}} where T","page":"Reference","text":"derivation_report([io], EvtType)\n\nHuman-readable dump of the derived triggers: each trigger's matchstr, whether it is CLEAN (with the fields it binds and any literal guards) or WIDENED (with the reason), plus per-field domain accounting.\n\n\n\n\n\n","title":"ChronoSim.derivation_report"},{"category":"method","location":"reference/#ChronoSim.derived_generators-Union{Tuple{T}, Tuple{Type{T}, Vector{ChronoSim.ReadSpec}}} where T","page":"Reference","text":"derived_generators(EvtType, specs) -> Vector{EventGenerator}\n\nBuild place-triggered EventGenerators from the read specs. Called at setup time (the first generators() call). Errors here name the event, the field, and the exact @domain line to add when a free field lacks a domain.\n\n\n\n\n\n","title":"ChronoSim.derived_generators"},{"category":"function","location":"reference/#ChronoSim.effect_spec","page":"Reference","text":"effect_spec(::Type{EvtType}) -> EffectSpec\n\nThe static effect specification derived by @fire. Defined only for @fire-annotated event types (consumers hasmethod-gate).\n\n\n\n\n\n","title":"ChronoSim.effect_spec"},{"category":"method","location":"reference/#ChronoSim.enable-Tuple{SimEvent, Any, Any}","page":"Reference","text":"enable(event, physical, when)\n\nGiven that precondition(event, physical) is true, this determines the probability distribution for when the event might fire, starting from time when. We consider the returned tuple (probability distribution, offset time) a rate for the event. When enable is called, the framework tracks the specific physical addresses that were read in order to compute the rate.\n\n\n\n\n\n","title":"ChronoSim.enable"},{"category":"method","location":"reference/#ChronoSim.escaped_args-Tuple{Any}","page":"Reference","text":"Instead of escaping the argument list as-is, we reach into arguments that are tuples and escape them individually. The idea is to destructure the arguments into escaped variables.\n\n\n\n\n\n","title":"ChronoSim.escaped_args"},{"category":"method","location":"reference/#ChronoSim.find_policy-Union{Tuple{T}, Tuple{Type{T}, ChronoSim.ExecutionPolicy}} where T","page":"Reference","text":"find_policy(::Type{T}, policy) -> Union{T,Nothing}\n\nReturn the first policy of type T in policy — policy itself, or, when it is a PolicyStack, its first matching member (searching nested stacks depth-first) — else nothing.\n\n\n\n\n\n","title":"ChronoSim.find_policy"},{"category":"method","location":"reference/#ChronoSim.find_quint_toolchain-Tuple{}","page":"Reference","text":"find_quint_toolchain(; quint_dir=get(ENV, \"CHRONOSIM_QUINT\", nothing),\n                       java_home=get(ENV, \"JAVA_HOME\", nothing)) -> QuintToolchain\n\nLocate the pinned checkers. Never installs anything.\n\n\n\n\n\n","title":"ChronoSim.find_quint_toolchain"},{"category":"function","location":"reference/#ChronoSim.fire_ast","page":"Reference","text":"fire_ast(::Type{EvtType}) -> (evtsym, statesym, whensym, rngsym, body)\n\nThe ORIGINAL un-inlined fire! body (a QuoteNode literal at macro time) plus its argument names, baked as a runtime method (the compile-time _FIRE_REGISTRY is empty after precompilation). Phase 4's effect lowering consumes it.\n\n\n\n\n\n","title":"ChronoSim.fire_ast"},{"category":"method","location":"reference/#ChronoSim.generation_stats-Tuple{}","page":"Reference","text":"generation_stats() -> Dict{Symbol,@NamedTuple{proposed::Int, admitted::Int}}\n\nPer-event-type-Symbol candidate counts since the last reset:\n\nproposed  — candidates a generator yielded and that were newly added to the dedup (seen) set during invariant processing.\nadmitted  — candidates whose precondition evaluated true and that were newly enabled.\n\nadmitted <= proposed for derived events (the precondition filters the over-approximation). Empty when collection is disabled.\n\n\n\n\n\n","title":"ChronoSim.generation_stats"},{"category":"method","location":"reference/#ChronoSim.generators-Tuple{Type{<:SimEvent}}","page":"Reference","text":"generators(::Type{SimEvent})::Vector{EventGenerator}\n\nEvery transition in the simulation needs generators that notice changes to state or events fired and create the appropriate transitions. Implement a generators function as part of the interface of each transition.\n\n\n\n\n\n","title":"ChronoSim.generators"},{"category":"method","location":"reference/#ChronoSim.guard_clauses-Union{Tuple{T}, Tuple{Type{T}, Any, Any}} where T","page":"Reference","text":"guard_clauses(EvtType, evt, physical; mod=parentmodule(EvtType))\n    -> Vector{Tuple{String,Any}}\n\nEvaluate the registered @precondition body of EvtType clause by clause against live state, without calling eval. Returns one (source_text, value) pair per top-level && conjunct of the precondition's returned expression, in source order. A top-level || expression is a single clause. Local bindings and loops before the return are executed first; each clause is then evaluated independently, so every clause gets a value even when an earlier one is false. A clause whose evaluation throws (e.g. a key lookup that an earlier haskey clause guards) reports the exception object as its value.\n\nThe real precondition's verdict is the first non-true value in order: false means rejected there; all true means accepted; an exception before any false means the precondition itself would throw.\n\nRequires an @precondition-registered event (mod must be the module where @precondition was expanded — by default the event type's module). Events with hand-written @conditionsfor generators are not registered and raise a GuardEvalError; so does any construct outside the interpreter's fragment (the error names the construct and its source text).\n\n\n\n\n\n","title":"ChronoSim.guard_clauses"},{"category":"function","location":"reference/#ChronoSim.guard_spec","page":"Reference","text":"guard_spec(::Type{EvtType}) -> GuardSpec\n\nThe static guard-read specification baked by @guard. Defined only for @guard-annotated event types; derived events expose the same information through derivation_spec.\n\n\n\n\n\n","title":"ChronoSim.guard_spec"},{"category":"method","location":"reference/#ChronoSim.initialize!-Tuple{Any, Function, SimulationFSM}","page":"Reference","text":"Initialize the simulation. You could call it as a do-function. It is structured this way so that the simulation will record changes to the physical state.\n\n    initialize!(sim) do init_physical\n        initialize!(init_physical, agent_cnt, sim.rng)\n    end\n\n\n\n\n\n","title":"ChronoSim.initialize!"},{"category":"method","location":"reference/#ChronoSim.isconsistent-Tuple{PhysicalState}","page":"Reference","text":"isconsistent(physical_state)\n\nA simulation in debug mode will assert isconsistent(physical_state) is true. Override this to verify the physical state of your simulation.\n\n\n\n\n\n","title":"ChronoSim.isconsistent"},{"category":"method","location":"reference/#ChronoSim.isimmediate-Tuple{Type{<:SimEvent}}","page":"Reference","text":"isimmediate(EventType)\n\nAn immediate event should return true for this function.\n\n\n\n\n\n","title":"ChronoSim.isimmediate"},{"category":"method","location":"reference/#ChronoSim.key_clock-Tuple{Tuple, Dict{Symbol, DataType}}","page":"Reference","text":"key_clock(key::Tuple, event_dict::Dict{Symbol, DataType})::SimEvent\n\nTakes a tuple of the form (:symbol, arg, arg) and a dictionary mapping symbols to struct types, and returns an instantiation of the struct named by :symbol. We pass in the list of datatypes because, if we didn't, then instantiation of a type from a symbol would need to search for the correct constructor in the correct module, and that would be both wrong and slow.\n\n\n\n\n\n","title":"ChronoSim.key_clock"},{"category":"method","location":"reference/#ChronoSim.last_writer-Tuple{ChronoSim.WriterIndex, Tuple}","page":"Reference","text":"last_writer(skeleton, address; at_step=length(skeleton.steps))\nlast_writer(index::WriterIndex, address; at_step=...)\n\nThe most recent recorded write to address at or before step at_step, as a NamedTuple (step, clock, when): step is 1-based into skeleton.steps, with step = 0, clock = :init for a write by the initializer; returns nothing when nothing wrote address in steps 0:at_step. Addresses compare by exact tuple equality — no index masking. The skeleton method builds a fresh WriterIndex per call; pass an index for repeated queries. Throws ArgumentError when at_step is outside 0:length(skeleton.steps).\n\n\n\n\n\n","title":"ChronoSim.last_writer"},{"category":"method","location":"reference/#ChronoSim.lint-Tuple{AbstractVector}","page":"Reference","text":"lint(events::AbstractVector; physical=nothing, enum_cap=10_000) -> LintReport\nlint(mod::Module; kwargs...) -> LintReport\n\nConstruction-time footprint lint over the static read/write masks of events (the same event-type vector passed to SimulationFSM). Computes write→guard interference edges (an edge no trigger covers is a WARNING), write→write races (info), dead-address smells (info; requires physical); write→rate edges are NOT analyzed in v1 and the report says so.\n\nWith a live physical instance, :possible index intersections are refined by enumerating the inferred finite domains; a provably empty overlap demotes the edge to info (the edge remains). Enumeration is capped at enum_cap per domain; caps are reported. Passing a physical type enables dead-address reflection only.\n\nThe lint is static and over-approximate — it sees address masks, not expression semantics. See the \"Linting a model's footprints\" guide.\n\n\n\n\n\n","title":"ChronoSim.lint"},{"category":"method","location":"reference/#ChronoSim.load_skeleton-Tuple{AbstractString}","page":"Reference","text":"load_skeleton(path) -> TrajectorySkeleton\n\nRead a skeleton written by save_skeleton. The format is bound to the Julia version and package struct layouts; see save_skeleton for the caveats.\n\n\n\n\n\n","title":"ChronoSim.load_skeleton"},{"category":"method","location":"reference/#ChronoSim.masks_intersect-Tuple{ChronoSim.AddressPattern, ChronoSim.AddressPattern}","page":"Reference","text":"masks_intersect(a::AddressPattern, b::AddressPattern) -> Bool\n\ntrue unless the two masked address patterns are provably disjoint by component-wise unification. false means no concrete address matches both — the sound direction.\n\n\n\n\n\n","title":"ChronoSim.masks_intersect"},{"category":"method","location":"reference/#ChronoSim.module_invariants-Tuple{Module}","page":"Reference","text":"module_invariants(mod::Module) -> Vector{InvariantDef}\n\nThe invariants registered by mod's @invariant declarations, in declaration order.\n\n\n\n\n\n","title":"ChronoSim.module_invariants"},{"category":"method","location":"reference/#ChronoSim.on_disable-Tuple{ChronoSim.ExecutionPolicy, Any, Any}","page":"Reference","text":"on_disable(policy, sim, clock_key)\n\nCalled when an enabled clock is disabled because its precondition no longer holds, before it is removed from sim.enabled_events. The clock that fires is consumed, not disabled, and is reported by on_prefire and on_postfire instead.\n\n\n\n\n\n","title":"ChronoSim.on_disable"},{"category":"method","location":"reference/#ChronoSim.on_enable-Tuple{ChronoSim.ExecutionPolicy, Vararg{Any, 5}}","page":"Reference","text":"on_enable(policy, sim, clock_key, event, distribution, te)\n\nCalled each time a clock's firing distribution is committed to the sampler: both when a newly-admitted event is enabled and when a still-enabled event is re-enabled with a replacement distribution. te is the absolute zero time of distribution; the enabling wall-clock time is sim.when.\n\n\n\n\n\n","title":"ChronoSim.on_enable"},{"category":"method","location":"reference/#ChronoSim.on_init-Tuple{ChronoSim.ExecutionPolicy, Any, Any, Any}","page":"Reference","text":"on_init(policy, sim, init_evt, changed_places)\n\nCalled once, after the initializer has run and the initial event set is enabled (state/event invariant holds), before the user observer sees the initial state. changed_places is the set of addresses the initializer wrote.\n\n\n\n\n\n","title":"ChronoSim.on_init"},{"category":"method","location":"reference/#ChronoSim.on_postfire-Tuple{ChronoSim.ExecutionPolicy, Vararg{Any, 5}}","page":"Reference","text":"on_postfire(policy, sim, clock_key, event, when, changed_places)\n\nCalled after event fired at when and the enabled-event set has been brought back into agreement with the new state, before the user observer runs. changed_places contains every address written by the event and by any immediate events it triggered.\n\n\n\n\n\n","title":"ChronoSim.on_postfire"},{"category":"method","location":"reference/#ChronoSim.on_prefire-Tuple{ChronoSim.ExecutionPolicy, Vararg{Any, 4}}","page":"Reference","text":"on_prefire(policy, sim, clock_key, event, when)\n\nCalled immediately before event fires at time when: sim.when still holds the previous event time and no state has been mutated yet.\n\n\n\n\n\n","title":"ChronoSim.on_prefire"},{"category":"method","location":"reference/#ChronoSim.on_preinit-Tuple{ChronoSim.ExecutionPolicy, Any}","page":"Reference","text":"on_preinit(policy, sim)\n\nCalled as the first act of initialize!, before the initializer callback runs and before the framework consumes any random numbers. This is the hook at which a recording policy snapshots the RNG state that a later replay will restore; it also sees the fully constructed SimulationFSM, so a policy can size its storage from sim (e.g. the clock-key type). Called again if the simulation is re-initialized.\n\n\n\n\n\n","title":"ChronoSim.on_preinit"},{"category":"method","location":"reference/#ChronoSim.on_propose-Tuple{ChronoSim.ExecutionPolicy, Any, Any}","page":"Reference","text":"on_propose(policy, sim, event)\n\nCalled once per candidate event that a generator proposed and that survived deduplication, before its precondition is evaluated. Admitted candidates are reported by the subsequent on_enable.\n\n\n\n\n\n","title":"ChronoSim.on_propose"},{"category":"method","location":"reference/#ChronoSim.over_event_rates-Tuple{Function, ChronoSim.EventDependency, Any, Any, Any}","page":"Reference","text":"Note that this method requires a first call to over_event_invariants and that this method interacts with that call by excluding events that were iterated in over_event_invariants.\n\n\n\n\n\n","title":"ChronoSim.over_event_rates"},{"category":"method","location":"reference/#ChronoSim.over_generated_events-Tuple{Function, GeneratorSearch, Any, Any, Any}","page":"Reference","text":"over_generated_events(callback, generators, physical, event_keys, changed_places)\n\ncallback is a function in the main event loop of the framework that accepts an event as an argument.\ngenerators is the struct containing all event generators.\nphysical is physical state.\nevent_keys is a vector or set of keys.\nchanged_places is a vector or set of physical addresses.\n\n\n\n\n\n","title":"ChronoSim.over_generated_events"},{"category":"method","location":"reference/#ChronoSim.over_generated_events-Tuple{Function, Vararg{Any, 4}}","page":"Reference","text":"over_generated_events(f::Function, generators, physical, fired_event_key, changed_places)\n\nGiven a fired event and the set of places changed by that event, create new events that may depend on that fired event and those changed places.\n\n\n\n\n\n","title":"ChronoSim.over_generated_events"},{"category":"method","location":"reference/#ChronoSim.over_tracked_physical_state-Union{Tuple{T}, Tuple{Function, T}} where T<:PhysicalState","page":"Reference","text":"over_tracked_physical_state(fcallback::Function, physical::PhysicalState)\n\nIterate over all tracked vectors in the physical state.\n\n\n\n\n\n","title":"ChronoSim.over_tracked_physical_state"},{"category":"method","location":"reference/#ChronoSim.precondition-Tuple{SimEvent, Any}","page":"Reference","text":"precondition(event, physical_state)\n\nThis determines whether an event should be in the enabled state given the current physical state. When this method is called, the framework tracks the specific addresses of the physical state that were read in order to determine whether this event should be enabled.\n\n\n\n\n\n","title":"ChronoSim.precondition"},{"category":"method","location":"reference/#ChronoSim.print_lint-Tuple{IO, LintReport}","page":"Reference","text":"print_lint(io::IO, report::LintReport)\n\nThe full, unbounded, greppable report: every edge on one line, stable field order. CI test logs use this; show(io, MIME\"text/plain\"(), report) prints the bounded summary.\n\n\n\n\n\n","title":"ChronoSim.print_lint"},{"category":"method","location":"reference/#ChronoSim.recorded_skeleton-Tuple{RecordSkeleton}","page":"Reference","text":"recorded_skeleton(policy::RecordSkeleton) -> TrajectorySkeleton\n\nReturn the skeleton recorded by policy. Throws an ArgumentError when the policy has not yet observed an initialized run.\n\n\n\n\n\n","title":"ChronoSim.recorded_skeleton"},{"category":"method","location":"reference/#ChronoSim.reenable-Tuple{SimEvent, Any, Any, Any}","page":"Reference","text":"reenable(event, physical, first_enabled, when)\n\nCalled for events that were enabled before a state change and remain enabled after. The framework has already verified the precondition still passes. This function determines whether the event's distribution needs to be updated in the sampler.\n\nThree conditions determine whether to call reenable:\n\nInvariant - A place read by precondition was modified by fire!.\nAddresses - The precondition now reads different places than before (relative event).\nRate - A place read by enable was modified by fire!.\n\nInvariant Addresses Rate reenable? Reason\n✅ ❌ ❌ ❌ Same places, precondition still holds, rate unaffected\n✅ ❌ ✅ ✅ Same places, precondition still holds, rate unaffected\n✅ ✅ — ✅ Relative event: dependencies shifted to new places\n❌ ❌ ✅ ✅ Rate dependencies changed\n❌ ❌ ❌ ❌ Nothing relevant changed\n\nKey: ✅ = changed, ❌ = unchanged, — = doesn't matter\n\nThe default implementation returns nothing (no update needed). To update the distribution, forward to enable:\n\nreenable(e::MyEvent, phys, _, t) = enable(e, phys, t)\n\n\n\n\n\n","title":"ChronoSim.reenable"},{"category":"method","location":"reference/#ChronoSim.replay-Tuple{Function, TrajectorySkeleton}","page":"Reference","text":"replay(sim_factory, skeleton; upto=nothing, probes=()) -> SimulationFSM\n\nDeterministically re-execute a run recorded as a TrajectorySkeleton and return the live simulation for inspection.\n\nsim_factory is called as sim_factory(policy) and must return (sim, initializer): a fresh SimulationFSM built with the SAME constructor arguments as the recorded run and with the given policy passed through (SimulationFSM(...; policy=policy)), plus the same initializer (a SimEvent or an init function) that was given to run. Any seed/rng the factory passes is overwritten: replay restores skeleton.rng_state before initializing, so the re-run consumes the identical random stream.\n\nAt every step, the fired (clock_key, when) is checked against skeleton.steps at the same code point where run evaluates its stop condition (before firing). A mismatch throws ReplayDivergence — determinism is broken, which is itself a bug to surface loudly: the usual causes are different constructor arguments, a different package/Julia version, or model code drawing randomness from outside sim.rng.\n\nupto=k stops after step k fires and returns the sim in exactly the state the original run had after its k-th event (upto=0 returns the initialized state). probes is a tuple of functions called as probe(sim, step, phase, event, when) with phase one of :init, :prefire, :postfire; see ProbePolicy.\n\nskel = load_skeleton(\"smoke.skel\")\nsim = replay(skel; upto=17) do policy\n    physical = Village(30, 10, 1.0, Xoshiro(0))   # same ctor args; rng is overwritten\n    sim = SimulationFSM(physical, EVENTS; policy=policy)\n    (sim, InitEvent())\nend\nguard_clauses(Infect(3, 7), sim.physical)\n\n\n\n\n\n","title":"ChronoSim.replay"},{"category":"method","location":"reference/#ChronoSim.reset_generation_stats!-Tuple{}","page":"Reference","text":"reset_generation_stats!()\n\nClear the accumulated proposed/admitted counters.\n\n\n\n\n\n","title":"ChronoSim.reset_generation_stats!"},{"category":"method","location":"reference/#ChronoSim.save_skeleton-Tuple{AbstractString, TrajectorySkeleton}","page":"Reference","text":"save_skeleton(path, skel::TrajectorySkeleton) -> path\nload_skeleton(path) -> TrajectorySkeleton\n\nWrite/read a skeleton with the Serialization stdlib. The format is bound to the Julia version and to the struct layouts of ChronoSim and Distributions: a file written under one Julia or package version may fail to load under another. Use it for debugging sessions and CI artifacts, not archival; a portable format is future work.\n\n\n\n\n\n","title":"ChronoSim.save_skeleton"},{"category":"method","location":"reference/#ChronoSim.sim_event_precondition-Tuple{SimEvent, Any}","page":"Reference","text":"The three sim_event_* functions call user-defined code, so we separate this out in order to check the calls and return values. This also provides a way to mock interaction with both events and the sampler for testing the central function deal_with_changes(). Subclass SimEvent to create a fake interaction.\n\n\n\n\n\n","title":"ChronoSim.sim_event_precondition"},{"category":"method","location":"reference/#ChronoSim.static_covers_dynamic-Tuple{LintReport, ChronoSim.LintHarvest}","page":"Reference","text":"static_covers_dynamic(report::LintReport, harvest::LintHarvest;\n                      ignore_writers=Symbol[]) -> DynamicCoverage\n\nCheck static ⊇ dynamic: every harvested enable-dependency place of a linted event is covered by some static guard pattern, and every harvested (writer, reader, place) interaction is covered by a write→guard edge (info or warning — demoted edges still count). ignore_writers skips initializer events absent from the linted vector. covered == true is the CI assertion.\n\n\n\n\n\n","title":"ChronoSim.static_covers_dynamic"},{"category":"method","location":"reference/#ChronoSim.trace_likelihood-Tuple{SimulationFSM, SimEvent, Function, AbstractVector}","page":"Reference","text":"trace_likelihood(sim::SimulationFSM, initializer, trace) -> TraceEvaluation\n\nEvaluate a recorded trace against a model: initialize sim, then walk the trace, accumulating each step's log-likelihood and firing the step's event, and return a TraceEvaluation. The trace is an AbstractVector of (when::Float64, clock_key::Tuple) pairs, as recorded by an observer via clock_key(event). The initializer is either an initialization function (physical, when, rng) -> nothing or a SimEvent whose fire! initializes the state; a third method accepts (sim, init_evt::SimEvent, init_func::Function, trace).\n\nInfeasible traces do not throw. If a step names an event that is not enabled, or a time that is not strictly after the previous event's time, evaluation stops and the result has feasible == false, loglikelihood == -Inf, and first_infeasible identifying the step. A trace entry with a non-finite time ends evaluation early without marking the trace infeasible.\n\nsim must be constructed with step_likelihood=true so its SamplingContext records the enabled-clock likelihood. To make the result differentiable with ForwardDiff, also pass likelihood_eltype=eltype(θ):\n\nsim = SimulationFSM(physical, events; step_likelihood=true, likelihood_eltype=eltype(θ))\n\n\n\n\n\n","title":"ChronoSim.trace_likelihood"},{"category":"method","location":"reference/#ChronoSim.validate_trace-Tuple{ChronoSim.QuintModel, Any, Any}","page":"Reference","text":"validate_trace(qm::QuintModel, skeleton::TrajectorySkeleton, sim_factory;\n               maxsteps::Int=50, workdir=mktempdir(),\n               toolchain::QuintToolchain=find_quint_toolchain())\n    -> TraceValidationReport\n\nCheck a recorded trajectory against the compiled spec. The skeleton stores only (clock, when, changed) per step, so full states are reconstructed by replay with a snapshotting probe; sim_factory is exactly replay's factory: sim_factory(policy) -> (sim, initializer) with the recorded run's constructor arguments.\n\nTwo checks, reported separately:\n\ninvariants — every reconstructed state satisfies every compiled invariant (quint run on the generated trace module; Node only).\ntransitions — every recorded step is accepted by the recorded event's compiled action from the preceding state (quint verify/Apalache on the forced-step module; needs a JVM).\n\nEach verdict is :passed, :failed (with first_failure naming the step, its event and when, localized by the checker output or the ascending prefix loop), :skipped (toolchain absent — the report prints how to install, quoting quintspike/VERSIONS.md), or :error (the checker crashed; log tail in `skipreason). Steps beyondmaxsteps` are not checked and the report says so; there are no silent caps.\n\nvalidate_trace(model, events, physical, skeleton, sim_factory; kwargs...)\n\nConvenience form that compiles first: compile keywords (name, skip_events, assume_true_guards, invariants, mutate_for_test) go to compile_quint; an unknown keyword is an ArgumentError.\n\n\n\n\n\n","title":"ChronoSim.validate_trace"},{"category":"method","location":"reference/#ChronoSim.value_at-Tuple{Any, Tuple}","page":"Reference","text":"value_at(physical, address::Tuple) -> Any\n\nWalk an ObservedState address to its current value: a Member component is a field access, anything else is a container index (dict keys, integer or tuple array indices). value_at(physical, (Member(:person), 5, Member(:location))) is physical.person[5].location. Throws whatever the underlying access throws (e.g. KeyError for a deleted dict key). Reading through value_at notifies the read tracker like any other read; the framework drains that buffer at its next capture, so simulation behavior is unaffected.\n\n\n\n\n\n","title":"ChronoSim.value_at"},{"category":"method","location":"reference/#ChronoSim.verify_read_coverage-Union{Tuple{T}, Tuple{Type{T}, Any}} where T","page":"Reference","text":"verify_read_coverage(::Type{T}, reads)\n\nCheck every read address against derivation_spec(T); throw on the first uncovered read. A no-op for event types without a derivation_spec.\n\n\n\n\n\n","title":"ChronoSim.verify_read_coverage"},{"category":"method","location":"reference/#ChronoSim.verify_write_coverage-Union{Tuple{T}, Tuple{Type{T}, Any, CheckEffects}} where T","page":"Reference","text":"verify_write_coverage(::Type{T}, changed, chk::CheckEffects)\n\nCheck every changed address against effect_spec(T) (unioned with chk's immediate-event specs); throw EffectCoverageError on the first uncovered write. A no-op for event types without an effect_spec.\n\n\n\n\n\n","title":"ChronoSim.verify_write_coverage"},{"category":"method","location":"reference/#ChronoSim.warnings-Tuple{LintReport}","page":"Reference","text":"warnings(r::LintReport) -> Vector{LintEdge}\n\nThe warning-level edges of r (the missed-trigger interferences).\n\n\n\n\n\n","title":"ChronoSim.warnings"},{"category":"method","location":"reference/#ChronoSim.whynot-Tuple{TrajectorySkeleton, Function, SimEvent}","page":"Reference","text":"whynot(skeleton, sim_factory, evt::SimEvent) -> WhynotReport\n\nExplain why evt did not fire during the recorded run, reported at the furthest lifecycle stage the event reached:\n\n:never_proposed — no generator ever proposed clock_key(evt). The report lists the declared trigger addresses that would have to change, the addresses the precondition actually reads at the final replayed state (the true trigger set), the reads no declared trigger covers (missing triggers), and recorded writes that nearly matched a trigger (same container, different index or leaf).\n:rejected — proposed but its precondition always evaluated false. The report replays to up to six rejection steps (first 5 and last 1) and, for @precondition events, names the failing conjunct via guard_clauses with the values and last writers of the evaluation's reads. Hand-written events get the whole-precondition verdict and reads; per-clause analysis requires @precondition.\n:enabled_never_fired — enabled but always outraced: interval count and total enabled duration, the committed distributions, and which events fired while it was enabled (the preemptors).\n:fired — the event did fire; the report says when.\n\nsim_factory follows the replay contract: sim_factory(policy) -> (sim, initializer) with the same constructor arguments as the recorded run. whynot replays the skeleton (once for stage :never_proposed, up to six prefixes for stage :rejected); a mismatch in the factory surfaces as ReplayDivergence.\n\nThe result is a WhynotReport; show prints a plain-text readout of at most ~30 lines.\n\n\n\n\n\n","title":"ChronoSim.whynot"},{"category":"method","location":"reference/#ChronoSim.whyrunning-Tuple{SimulationFSM, TrajectorySkeleton, Any}","page":"Reference","text":"whyrunning(sim, skeleton, stop_predicate; nsteps=50) -> WhyrunningReport\n\nExplain why a run has not stopped. Evaluates stop_predicate on sim's current state inside capture_state_reads and reports every address it read, its current value, and its last writer; then summarizes the last nsteps recorded steps — the dominant event types, the addresses they rewrite, and whether any recorded write in that window touched an address the predicate reads. The predicate is called as stop_predicate(physical) when that method exists, else as stop_predicate(physical, step, clock, when) (the run stop condition form).\n\nsim must be at the skeleton's final state (the recording sim after run returned, or the result of a full replay); an ArgumentError is thrown when sim.when disagrees with the skeleton's last step.\n\nStatic unreachability (\"no event can ever write what the predicate reads\") is answered by can_stop_change when the model's event-type vector is passed as events and those types carry @fire effect specs: the reachability field then reports :cannot_change/:can_change/:unknown. Without events (or when the types lack effect specs) the report keeps the fixed line: reachability analysis requires effect analysis (not yet run).\n\n\n\n\n\n","title":"ChronoSim.whyrunning"},{"category":"method","location":"reference/#ChronoSim.whystopped-Tuple{InvariantViolation}","page":"Reference","text":"whystopped(violation::InvariantViolation) -> WhystoppedReport\nwhystopped(skeleton::TrajectorySkeleton) -> WhystoppedReport\n\nRender why a run ended. The InvariantViolation method is the forensic readout of a violation caught from a CheckInvariants run: the invariant's name, the breaking event, each guilty address with its last writer at the violating step and the previous writer before that step, and the exact replay(...) invocation that reproduces the state one step before the violation. The TrajectorySkeleton method reads out a run that ended without an exception: the final step and time, and whether any clocks were still enabled (stopped by the stop condition) or none were (the sampler was exhausted).\n\n\n\n\n\n","title":"ChronoSim.whystopped"},{"category":"method","location":"reference/#ChronoSim.write_quint-Tuple{AbstractString, ChronoSim.QuintModel}","page":"Reference","text":"write_quint(path::AbstractString, qm::QuintModel) -> path\n\nWrite the compiled module text to path (conventionally <name>.qnt).\n\n\n\n\n\n","title":"ChronoSim.write_quint"},{"category":"method","location":"reference/#CompetingClocks.fire!-Tuple{SimEvent, Any, Any, Any}","page":"Reference","text":"fire!(event, physical, when, rng::AbstractRNG)\n\nWhen an event fires, it modifies state with this function. If you sample using the random number generator, that affects the likelihood of the outcome.\n\n\n\n\n\n","title":"CompetingClocks.fire!"},{"category":"method","location":"reference/#CompetingClocks.fire!-Tuple{SimulationFSM, Any, Any}","page":"Reference","text":"fire!(sim::SimulationFSM, time, event_key)\n\nLet the event act on the state.\n\n\n\n\n\n","title":"CompetingClocks.fire!"},{"category":"macro","location":"reference/#ChronoSim.@conditionsfor-Tuple{Any, Any}","page":"Reference","text":"@conditionsfor EventType begin\n    @reactto ... end\n    @reactto ... end\nend\n\nGenerates a generators(::Type{EventType}) function containing all the EventGenerators defined in the @reactto blocks.\n\n\n\n\n\n","title":"ChronoSim.@conditionsfor"},{"category":"macro","location":"reference/#ChronoSim.@domain-Tuple{Any}","page":"Reference","text":"@domain EvtType.field = <expr over `physical`>\n\nSupply the enumeration domain for an event field that a widened trigger cannot bind. Emits a derived_domain(::Type{EvtType}, ::Val{:field}, physical) method.\n\n\n\n\n\n","title":"ChronoSim.@domain"},{"category":"macro","location":"reference/#ChronoSim.@fire-Tuple{Any}","page":"Reference","text":"@fire function fire!(evt::EvtType, state, when, rng)\n    <body>\nend\n\nEmit the fire! method verbatim (analysis-only: runtime behavior is byte-identical to the unannotated method) and derive its static effect specification by a syntactic taint pass over the body — the write-side mirror of @precondition. The derived EffectSpec is exposed via effect_spec(EvtType) and checked at runtime by CheckEffects; the original body is baked as fire_ast(EvtType) for Phase 4.\n\nThe walker understands assignment to state access chains (x.field = rhs, c[k] = rhs, and the op-assign forms), the ObservedState mutation API (push!, filter!, delete!, union!, empty!, …), @obswrite/@obsread, registered @fragment helpers (inlined), and nested fire!(EvtType(args...), state, when, rng) calls to other @fired events (inlined). Loops and branches widen indices exactly as the read pass does; a write under a branch is recorded unconditionally (may-write).\n\nMacro-time errors: a call whose name ends in ! that receives state and is not a recognized mutator; a .= broadcast assignment to state; a body that writes no state at all.\n\n\n\n\n\n","title":"ChronoSim.@fire"},{"category":"macro","location":"reference/#ChronoSim.@fragment-Tuple{Any}","page":"Reference","text":"@fragment function helper(args...)\n    <body>\nend\n\nEmit the helper VERBATIM (behavior identical to unmarked) and register its body so a @precondition may CALL it: at derivation time the call is inlined (arguments substituted, locals α-renamed) instead of treated as opaque, letting the helper's state reads be seen. Only single-method, positional helpers are supported — type annotations are stripped (x::T -> x); varargs, keyword arguments, and default values error here.\n\n\n\n\n\n","title":"ChronoSim.@fragment"},{"category":"macro","location":"reference/#ChronoSim.@guard-Tuple{Any}","page":"Reference","text":"@guard function precondition(evt::EvtType, state)\n    <body>\nend\n\nEmit the precondition verbatim (analysis-only; runtime behavior is identical to the unannotated method) and derive its static read specification for the footprint lint, WITHOUT deriving generators — the hand-written @conditionsfor generators stay in charge. This is how a model with hand-written generators opts into lint's missed-trigger check. Also bakes precondition_ast so guard_clauses works on the annotated event.\n\nDifferences from @precondition: a body that reads no state is allowed (empty spec — e.g. precondition(evt, state) = true), and whole-container reads (length/keys/isempty) are recorded as container-level subtree reads instead of requiring a covering trigger. Passing state to an unregistered helper is still a macro-time error; register the helper with @fragment.\n\nUse exactly one of @guard or @precondition on a given event type.\n\n\n\n\n\n","title":"ChronoSim.@guard"},{"category":"macro","location":"reference/#ChronoSim.@invariant-Tuple{Any, Any}","page":"Reference","text":"@invariant \"name\" function (physical) ... end\n\nDeclare a named safety invariant for the enclosing model module: a pure boolean function of the physical state. The declaration registers the compiled function (for runtime checking by CheckInvariants) and the source body (for later compilation to a model-checker spec) under (module, name); redeclaring the same name in the same module replaces the previous definition.\n\nThe function must take exactly one argument (the physical state), read only that argument, and return Bool. It must not mutate state, draw random numbers, or close over mutable history — the checker may re-evaluate it at any time and expects the same answer for the same state.\n\n@invariant \"person location xor elevator\" function (physical)\n    all((p.location > 0 && p.elevator == 0) || (p.location == 0 && p.elevator > 0)\n        for p in physical.person)\nend\n\nPrefer one @invariant per logical clause: on violation, the failing name is the first diagnostic, so small named invariants localize a break the way a per-clause error message used to.\n\n\n\n\n\n","title":"ChronoSim.@invariant"},{"category":"macro","location":"reference/#ChronoSim.@precondition-Tuple{Any}","page":"Reference","text":"@precondition function precondition(evt::EvtType, state)\n    <body>\nend\n\nEmit the precondition verbatim plus a derived generators(::Type{EvtType}) method built by a syntactic taint pass over the body. Out-of-fragment constructs (helper calls with state arguments, uncovered whole-container dict reads, zero state reads) error at macro time with an actionable message.\n\n\n\n\n\n","title":"ChronoSim.@precondition"},{"category":"macro","location":"reference/#ChronoSim.@reactto-Tuple{Any}","page":"Reference","text":"@reactto changed(array[index].field) do physical\n    # generator body\nend\n\n@reactto fired(EventType(args...)) do physical\n    # generator body\nend\n\nCreates an EventGenerator that reacts to state changes or event firings. Used within @conditionsfor blocks.\n\n\n\n\n\n","title":"ChronoSim.@reactto"},{"category":"type","location":"reference/#ChronoSim.ObservedState.Address","page":"Reference","text":"Address{Key}()\n\nRepresents the address of this compound struct within the container that has this object. This object is mutable because addresses change as containers modify their contents.\n\nIf this object is in a vector, the Key=Int. If it's in a struct, Key=Member, which is a wrapper around the fieldname Symbol.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.Address"},{"category":"type","location":"reference/#ChronoSim.ObservedState.Addressed","page":"Reference","text":"Addressed\n\nThe abstract supertype of every element struct that the @keyedby macro creates. An Addressed value knows which container it currently sits in and under which index, which is how a field write such as piece.speed = 2.0 can report its full address.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.Addressed"},{"category":"type","location":"reference/#ChronoSim.ObservedState.FixedExtentError","page":"Reference","text":"FixedExtentError(op)\n\nSignals that the length-changing operation op was attempted on an ObservedArray/ObservedVector. These containers use the integer index as a place identity, so growing or shrinking the extent would silently migrate identity between addresses that the dependency network and generator templates have already recorded. See docs/src/state_contract.md (\"Fixed extent: position is identity\").\n\n\n\n\n\n","title":"ChronoSim.ObservedState.FixedExtentError"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedArray","page":"Reference","text":"ObservedArray{T,N,Index}\n\nA dense N-dimensional array whose element accesses are reported to the observed state, so that events depending on an element are re-examined when it changes. Declare state fields as ObservedArray{T,N,Member} and allocate with ObservedArray{T,Member}(undef, dims...). The extent is fixed for the life of the simulation: position is the identity of an element, so length-changing operations throw FixedExtentError. Use an ObservedDict for populations that grow or shrink.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedArray"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedDict","page":"Reference","text":"ObservedDict{K,V,Index}\n\nA dictionary whose accesses are reported to the observed state. The key is the identity of an element, so this is the container for populations whose members are created or destroyed during a run: insertion and deletion each affect exactly one address. Declare state fields as ObservedDict{K,V,Member}. Reads that test for an absent key (haskey, get with a default) are recorded too, so an event that checked for a missing key wakes up when the key is later inserted.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedDict"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedMatrix","page":"Reference","text":"ObservedMatrix{T,Index}\n\nA two-dimensional ObservedArray.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedMatrix"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedPhysical","page":"Reference","text":"ObservedPhysical\n\nThe abstract supertype of every state struct that the @observedphysical macro creates. It carries the collections into which reads and writes are reported, and the framework's capture_state_reads/capture_state_changes methods drain them.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedPhysical"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedSet","page":"Reference","text":"ObservedSet{T,Key} <: AbstractSet{T}\n\nA set that tracks mutations for the ChronoSim observed state system. Unlike ObservedVector and ObservedDict, ObservedSet is not a container -  elements within the set do not have addresses and cannot be individually tracked.\n\nType Parameters\n\nT: Element type of the set\nKey: The type of index this set has within its parent container  (e.g., Member for struct fields, Int for vector elements)\n\nExamples\n\n# In an observed struct\n@observedphysical GameState begin\n    buildings::ObservedSet{String,Member}\nend\n\n# In an observed vector  \nfloors = ObservedVector{ObservedSet{String,Int}}(undef, 10)\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedSet"},{"category":"type","location":"reference/#ChronoSim.ObservedState.ObservedVector","page":"Reference","text":"ObservedVector{T,Index}\n\nA one-dimensional ObservedArray.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.ObservedVector"},{"category":"type","location":"reference/#ChronoSim.ObservedState.Param","page":"Reference","text":"Param{T}\n\nA wrapper for state fields that are configuration rather than simulation state. Reads of a Param field are not tracked and create no dependencies, which is right for values that never change during a run, such as rate tables or precomputed geometry. Access is transparent: the field behaves as a T. The generated state constructor accepts an unwrapped T and wraps it.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.Param"},{"category":"method","location":"reference/#Base.show-Tuple{IO, ChronoSim.ObservedState.Address}","page":"Reference","text":"The show() function for Address{Key} doesn't print the container because the type can be quite large.\n\n\n\n\n\n","title":"Base.show"},{"category":"method","location":"reference/#ChronoSim.ObservedState.access_to_placekey-Tuple{Expr}","page":"Reference","text":"access_to_placekey(expr)\n\nThis function takes an expression for member access to a hierarchical container and converts it into a place key. For instance:\n\nAccessor expression Place key\nstate.agent[j].armor (:agent, j, :armor)\nsim.board[i, j].fval (:board, (i, j), :fval)\nstate.param (:param,)\nsim.adict[(name, kind)] (:adict, (name, kind))\nphysical.land[square].grass (:land, square, :grass)\n\nThe output is also an Expr-type object.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.access_to_placekey"},{"category":"method","location":"reference/#ChronoSim.ObservedState.notify_all-Tuple{Any}","page":"Reference","text":"notify_all(obj)\n\ne When a struct is deleted or has its index changed, it should create a notification for every member of the struct.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.notify_all"},{"category":"method","location":"reference/#ChronoSim.capture_state_changes-Tuple{Function, ChronoSim.ObservedState.ObservedPhysical}","page":"Reference","text":"capture_state_changes(f::Function, physical_state)\n\nThe callback function f will modify the physical state. This function records which parts of the state were modified. The callback should have no arguments and may return a result.\n\n\n\n\n\n","title":"ChronoSim.capture_state_changes"},{"category":"method","location":"reference/#ChronoSim.capture_state_reads-Tuple{Function, ChronoSim.ObservedState.ObservedPhysical}","page":"Reference","text":"capture_state_reads(f::Function, physical_state)\n\nThe callback function f will read the physical state. This function records which parts of the state were read. The callback should have no arguments and may return a result.\n\n\n\n\n\n","title":"ChronoSim.capture_state_reads"},{"category":"macro","location":"reference/#ChronoSim.ObservedState.@keyedby-Tuple{Any, Any, Any}","page":"Reference","text":"@keyedby StructName IndexType begin\n    field1::Type1\n    field2::Type2\n    ...\nend\n\nCreate a mutable struct with the given fields plus automatic _container::Any and _index::IndexType fields. The generated constructor only requires the user-defined fields.\n\nExample\n\n@keyedby MyElement Int64 begin\n    val::Int64\n    name::String\nend\n\nThis generates a struct equivalent to:\n\nmutable struct MyElement <: Addressed\n    val::Int64\n    name::String\n    _address::Address{Int64}\n    MyElement(val, name) = new(val, name)\nend\n\n\n\n\n\n","title":"ChronoSim.ObservedState.@keyedby"},{"category":"macro","location":"reference/#ChronoSim.ObservedState.@observedphysical-Tuple{Any, Any}","page":"Reference","text":"@observedphysical <typename> <definition block>\n\nThis macro defines a physical state that uses the ObservedState machinery to track changes to that physical state.\n\nFor example:\n\n@keyedby Int Piece begin\n    speed::Float64\n    kind::String\nend\n\n@keyedby NTuple{2,Int64} Square begin\n    grass::Float64\n    food::Float64\nend\n\n@observedphysical Board begin\n    board::ObservedArray{Square,2}\n    actor::ObservedDict{Int,Piece}\n    params::Dict{Symbol,Float64}\n    actors_max::Int64\nend\n\nThis macro creates a struct that contains tracking information about its Observed members, so in the example params would not be tracked. The Board type has a parent type ObservedPhysical which has the parent type ChronoSim.PhysicalState.\n\nExpanding the macro, we would see:\n\nmutable struct Board <: ObservedPhysical\n    board::ObservedArray{Square,2}\n    actor::ObservedDict{Int,Piece}\n    params::Dict{Symbol,Float64}\n    actors_max::Int64\n    obs_modified::Vector{Tuple}\n    obs_read::Vector{Tuple}\n    Board(board, actor, params, actors_max) = ...\nend\n\nHere, the list of read and modified state uses the abstract type Tuple because it unifies the way changed state is reported for the two observed containers: (:board, (2,2), :grass) and (:actor, 7, :speed). The ellipsis above for the constructor is a constructor that initializes obs_modified and obs_read to empty vectors.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.@observedphysical"},{"category":"macro","location":"reference/#ChronoSim.ObservedState.@obsread-Tuple{Any}","page":"Reference","text":"@obsread expr\n\nTrack reads and writes to ObservedPhysical state. Records the access in physical state and returns the value.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.@obsread"},{"category":"macro","location":"reference/#ChronoSim.ObservedState.@obswrite-Tuple{Any}","page":"Reference","text":"@obswrite expr\n\nTrack writes to ObservedPhysical state. Records the access in physical state and performs the assignment.\n\n\n\n\n\n","title":"ChronoSim.ObservedState.@obswrite"},{"category":"section","location":"customstate/#Custom-State-Types","page":"Custom State Types","text":"The physical state is an architectural component you can replace. The framework interacts with the state through exactly two functions, so if the observed containers and the @obsread/@obswrite macros both fail to fit your problem — for example, because your state lives in an external data structure or a memory-mapped file — you can supply your own state type by implementing those two functions for it.\n\nThe two functions are capture_state_changes and capture_state_reads. Each takes a function f to run (a firing function in the first case, a precondition or enabling function in the second) and your state. Each must run f, collect the addresses that were written or read while it ran, and return both the result and that collection. Here are the implementations that the built-in ObservedPhysical uses, which serve as the specification.\n\nfunction capture_state_changes(f::Function, physical::ObservedPhysical)\n    empty!(physical.obs_modified)\n    result = f()\n    # An ordered set makes the downstream processing deterministic,\n    # which keeps whole trajectories reproducible under one seed.\n    changes = OrderedSet(physical.obs_modified)\n    return (; result, changes)\nend\n\nfunction capture_state_reads(f::Function, physical::ObservedPhysical)\n    empty!(physical.obs_read)\n    result = f()\n    reads = OrderedSet(physical.obs_read)\n    return (; result, reads)\nend\n\nBoth functions return a named tuple. The first element passes through whatever f returned, and the second is a collection of address tuples. The addresses you report must satisfy the same contract the built-in containers satisfy: writes must cover everything that changed, reads must cover everything the result depended on, and the same access must produce the same address in every run. The State Contract page in the Development section states these obligations precisely, and it is the document to read before building a custom state, because the correctness of event scheduling rests entirely on them.\n\n","title":"Custom State Types"},{"category":"section","location":"runbook/#Runbook","page":"Runbook","text":"This runbook is the operational reference for ChronoSim's debugging and verification features. It is written to be followed mechanically, by a human or an AI agent, with no context beyond this repository. Each entry documents one feature and gives, in order:\n\nHow to invoke it — the exact call or command.\nWhat its output looks like — a captured example, copied verbatim from a real run.\nWhat each failure form means — how to read a non-success result.\nHow to turn it off — every debugging feature is opt-in; this says what \"off\" is.\n\nEntries are added as each debugging-and-verification feature lands. For the narrative behind each feature — when to reach for it and how to read its answer — see the overview and its linked guides.\n\n","title":"Runbook"},{"category":"section","location":"runbook/#Evaluate-a-recorded-trace","page":"Runbook","text":"Given a recorded trajectory (a list of (when, clock_key) pairs) and a model, trace_likelihood walks the trace against the model, computes the trajectory's log-likelihood, and reports whether the trace is feasible under the model. It never throws on an infeasible trace; it returns a TraceEvaluation that says what went wrong.","title":"Evaluate a recorded trace"},{"category":"section","location":"runbook/#How-to-invoke-it","page":"Runbook","text":"The evaluator needs a sim that records the enabled-clock set at each step. Build the evaluation sim with step_likelihood=true:\n\nusing ChronoSim\nusing Random: Xoshiro\n\n# 1. Record a trace with a forward run. The observer stores (when, clock_key).\ntrace = Tuple{Float64,Tuple}[]\nobserver = (physical, when, event, changed) -> begin\n    event isa ChronoSim.InitializeEvent && return nothing\n    push!(trace, (when, clock_key(event)))\nend\nfsim = SimulationFSM(\n    MyBoard(1), [MyEventA, MyEventB];\n    rng=Xoshiro(424242),\n    observer=observer,\n)\nChronoSim.run(fsim, my_init!, (p, i, e, w) -> i > 20)\n\n# 2. Evaluate the trace on a *fresh* sim built with step_likelihood=true.\nesim = SimulationFSM(\n    MyBoard(1), [MyEventA, MyEventB];\n    rng=Xoshiro(7),\n    step_likelihood=true,\n)\nev = trace_likelihood(esim, my_init!, trace)\n\nTo differentiate the trace log-likelihood with ForwardDiff, pass likelihood_eltype=eltype(θ) in addition to step_likelihood=true; see Differentiating a trace likelihood.\n\nThe initializer (my_init! above) is either an initialization function (physical, when, rng) -> nothing or a SimEvent whose fire! sets up the state. The trace is an AbstractVector of (when::Float64, clock_key::Tuple) pairs.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like","page":"Runbook","text":"trace_likelihood returns a TraceEvaluation. For a feasible trace, the text/plain display (what the REPL prints) is:\n\nTraceEvaluation\n  feasible         : true\n  loglikelihood    : -2.295205868713304\n  steps evaluated  : 20\n  first infeasible : none\n\nFor an infeasible trace — here, a trace whose step 17 names an event key (:FireA, 99) that is not enabled at that point — the display names the first failing step:\n\nTraceEvaluation\n  feasible         : false\n  loglikelihood    : -Inf\n  steps evaluated  : 16\n  first infeasible : step 17, event (:FireA, 99), reason not_enabled\n\nThe one-line form (used when a TraceEvaluation is printed inside another structure) is:\n\nTraceEvaluation(feasible=false, loglikelihood=-Inf, steps_evaluated=16)\n\nThe fields are:\n\nev.loglikelihood — the trajectory's log-likelihood, or -Inf if infeasible.\nev.feasible — true if every step named an enabled event at a strictly-increasing time.\nev.steps_evaluated — how many steps were scored before evaluation stopped; equals length(ev.steploglik).\nev.first_infeasible — nothing if feasible, else (step, event, reason) for the first failing step.\nev.steploglik — the per-step log-likelihood contributions.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means","page":"Runbook","text":"A trace is infeasible when a step cannot happen under the model. reason is:\n\n:not_enabled — the step named a clock_key that was not in the enabled set when its turn came. The event either was never enabled or had already been disabled/consumed. Evaluation stops; steps_evaluated is the number of steps that fired cleanly before it.\n:time_order — the step's when was not strictly greater than the previous event's time (sim.when). Times in a trace must strictly increase. An equal or earlier time is a :time_order failure.\n\n:not_enabled is checked before :time_order: if both fail, the missing enablement is the reported reason (the time comparison is only meaningful for a step that could fire at all).\n\nA separate, non-failure case: a trace entry with a non-finite when (e.g. Inf) or a nothing key ends evaluation early without marking the trace infeasible — feasible stays true, mirroring a forward run that exhausts its events. This is the trace's normal end-of-data sentinel, not an error.\n\nIf trace_likelihood throws an ArgumentError reading trace_likelihood needs a simulation built with step_likelihood=true, the evaluation sim was built without that flag. Add step_likelihood=true as shown above.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off","page":"Runbook","text":"There is nothing to turn off. Trace evaluation is a separate entry point (trace_likelihood) that you call explicitly; it does not run during a normal run and adds no cost to a production simulation. The only requirement is building the evaluation sim with step_likelihood=true.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Record-a-trajectory-skeleton","page":"Runbook","text":"The RecordSkeleton policy captures a replayable record of a run — the pre-initialization RNG state and, per fired event, its clock key, firing time, changed addresses, and enable/disable/proposal history. It is opt-in: a simulation constructed without the policy records nothing and pays nothing. Retrieve the result with recorded_skeleton and, optionally, persist it with save_skeleton / load_skeleton.","title":"Record a trajectory skeleton"},{"category":"section","location":"runbook/#How-to-invoke-it-2","page":"Runbook","text":"Pass policy=RecordSkeleton() to SimulationFSM and read the skeleton back after run returns (run is not exported; call it as ChronoSim.run):\n\nusing ChronoSim\n\nrec = RecordSkeleton(metadata=(model=\"sirvillage\", N=30, seed=2938423))\nsim = SimulationFSM(physical, events; seed=2938423, policy=rec)\nChronoSim.run(sim, InitEvent(), (p, i, e, w) -> w > 15.0)\nskel = recorded_skeleton(rec)\nsave_skeleton(\"smoke.skel\", skel)\n\nmetadata is stored opaquely in the skeleton and is never read by the framework; put model identification (module, constructor arguments, git SHA) there. The policy only observes — it never draws from the RNG and never mutates state — so a recorded run's trajectory is identical to the same-seed run unrecorded.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-2","page":"Runbook","text":"show(stdout, MIME\"text/plain\"(), skel) prints a five-line summary. Captured verbatim from the sirvillage smoke run above:\n\nTrajectorySkeleton\n  clock key  : Tuple{Symbol, Vararg{Int64}}\n  steps      : 16003\n  time span  : 0.0 -> 14.998863194116215\n  top events : Travel 15917 | Recover 36 | Infect 33 | Reset 15 | Mutate 2\n\nThe one-line form (used when a skeleton is printed inside another structure) is:\n\nTrajectorySkeleton(16003 steps, t=0.0..14.998863194116215)\n\nThe recorded fields are skel.rng_state (a copy of the RNG taken before the initializer ran), skel.metadata (the opaque value above), skel.init (the initialization record: when, changed, enabled, disabled, proposed), and skel.steps (one SkeletonStep per fired event, in firing order, each with clock, when, changed, enabled, disabled, proposed).","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-2","page":"Runbook","text":"ArgumentError: this RecordSkeleton has not observed a run — you called recorded_skeleton before run. Pass the policy to SimulationFSM and run the simulation first.\nload_skeleton throws a deserialization or TypeError — the file was written under a different Julia or package version (the Serialization format is version-bound), or is not a skeleton file. Re-record; do not archive .skel files across upgrades.\nSteps look truncated after a second run on the same sim — re-initializing discards the previous recording. Save the skeleton before re-running.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-2","page":"Runbook","text":"Construct SimulationFSM without the policy keyword. The default NoPolicy records nothing and adds zero time and zero allocation: every hook, including the once-per-run on_preinit that snapshots the RNG, compiles to return nothing.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Replay-a-recorded-run","page":"Runbook","text":"replay re-executes a TrajectorySkeleton bit-for-bit and returns the live simulation for inspection. It restores the skeleton's pre-init RNG state, re-runs through the same executor kernel run uses, and checks the fired (clock_key, when) against the recording at every step — at the exact code point where run evaluates its stop condition, so the random stream is reproduced identically. upto=k stops after step k (so you can inspect the state the original run had after its k-th event); probes observe each step without perturbing it. It is opt-in: forward runs are untouched.","title":"Replay a recorded run"},{"category":"section","location":"runbook/#How-to-invoke-it-3","page":"Runbook","text":"Record a run (see \"Record a trajectory skeleton\"), then hand replay a factory that rebuilds the simulation with the SAME constructor arguments and passes through the policy replay gives it. run and replay are not exported; call ChronoSim.run / replay (the latter is exported):\n\nusing ChronoSim\nusing Random: Xoshiro\n\n# 1. Record.\nrec = RecordSkeleton(metadata=(model=\"sirvillage\", N=30, seed=2938423))\nrng = Xoshiro(2938423)\nphysical = Village(30, 10, 1.0, rng)                 # ctor consumes rng\nsim = SimulationFSM(physical, EVENTS; rng=rng, policy=rec)\nChronoSim.run(sim, InitEvent(), (p, i, e, w) -> w > 1.0)\nskel = recorded_skeleton(rec)\n\n# 2. Replay to step 200 and inspect the live state.\nsim = replay(skel; upto=200) do policy\n    rng = Xoshiro(2938423)                           # SAME ctor args as recorded\n    physical = Village(30, 10, 1.0, rng)\n    (SimulationFSM(physical, EVENTS; rng=rng, policy=policy), InitEvent())\nend\nsim.physical.actors[7].state                         # poke at the state\n\nThe factory is called as factory(policy) and must return (sim, initializer): a fresh SimulationFSM built with the given policy passed through (SimulationFSM(...; policy=policy)) and the same initializer (a SimEvent or an init function) given to run. Any seed/rng the factory sets is overwritten — replay restores skel.rng_state, so the re-run consumes the identical random stream. Model identification (constructor arguments, git SHA) belongs in skel.metadata at record time; reproducing the constructor is the caller's responsibility.\n\nWatch an address across the whole run with a probe — a tuple of functions each called probe(sim, step, phase, event, when), with phase one of :init, :prefire, :postfire:\n\nvals = Tuple{Int,Any}[]\nwatch(sim, step, phase, event, when) =\n    phase === :postfire && push!(vals, (step, sim.physical.actors[7].state))\nreplay(factory, skel; probes=(watch,))\n\nProbePolicy is usable directly on a forward run too: SimulationFSM(...; policy=ProbePolicy((watch,))).","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-3","page":"Runbook","text":"replay returns the live SimulationFSM, positioned at the requested step. A :postfire probe that records each step's fired-event name over the first five steps of the sirvillage replay above prints:\n\n  (1, :Travel)\n  (2, :Travel)\n  (3, :Travel)\n  (4, :Travel)\n  (5, :Travel)\n\nA recorded 1,000-step sirvillage run replays in the same order of magnitude as the original forward run (the replay hot path adds only a tuple compare and one policy dispatch per step): original 0.115 s, replay 0.113 s for a 1,270-step run.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-3","page":"Runbook","text":"ReplayDivergence at step k — the rebuilt simulation fired a different (clock_key, when) than the skeleton recorded. Replay is exact (no tolerance), so any divergence means the re-run is not the recorded run: different constructor arguments, a different package/Julia version, or model code drawing randomness from outside sim.rng. The message names the expected and actual event; when only the time differs it prints time differs by …. An actual : (no event; sampler exhausted before this step) means the re-run ran out of events early — again a determinism break.\nArgumentError: sim_factory must pass the policy it is given — the factory dropped the policy argument. Pass policy=policy to SimulationFSM.\nArgumentError: upto=… is outside this skeleton's 0:N steps — upto was negative or larger than the recorded step count.\nArgumentError: sim_factory(policy) must return (sim, initializer) — the factory returned something other than the required 2-tuple.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-3","page":"Runbook","text":"There is nothing running to turn off. replay is a separate entry point you call explicitly; a forward run without a ProbePolicy executes the identical instructions it did before this feature existed.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Explain-a-precondition-clause-by-clause","page":"Runbook","text":"guard_clauses evaluates a derived event's @precondition body one conjunct at a time against live state, without eval, and returns a Vector{Tuple{String,Any}} of (source_text, value) pairs — the ingredient for saying which guard rejected a proposed event. It reads state and never mutates it. It works only on events defined with @precondition (the derivable fragment); hand-written @conditionsfor events are not registered and raise a GuardEvalError.","title":"Explain a precondition clause by clause"},{"category":"section","location":"runbook/#How-to-invoke-it-4","page":"Runbook","text":"Call it with an event instance and the physical state (typically a state you reached with replay(...; upto=k)):\n\nusing ChronoSim\n\nguard_clauses(OpenElevatorDoors(1), system)\n\nThere is also a type-first method, guard_clauses(EvtType, evt, physical; mod=parentmodule(EvtType)); pass mod= only if the event type's parent module is not where its @precondition was expanded.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-4","page":"Runbook","text":"Each entry is one top-level && conjunct of the precondition, in source order (a top-level || is a single clause). For a freshly built elevator system (stationary, no calls requested, no buttons pressed), OpenElevatorDoors(1) is rejected on its second clause:\n\n  (\"!(elevator.doors_open)\", true)\n  (\"call_exists || button_pressed\", false)\n\nFor a replayed sirvillage state, an Infect(1, 2) proposed between two actors who are not a valid infectious→susceptible co-located pair reports which guards fail:\n\n  (\"source_state == Infectious\", true)\n  (\"sink_state == Susceptible\", false)\n  (\"source_loc == sink_loc\", false)\n\nRead the real precondition's verdict as the first non-true value in source order: false ⇒ the precondition is rejected there; all true ⇒ accepted; an exception value before any false ⇒ the real precondition would itself throw on this state. A clause value that is an exception object (e.g. a KeyError) means that clause's read failed — and if an earlier clause is already false, the real short-circuiting precondition never reached it, so the exception is an artifact of independent evaluation, not a bug. Every clause is evaluated independently (from a fresh copy of the post-prelude environment), which is why you get a value for every clause even after one is false.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-4","page":"Runbook","text":"guard_clauses throws a GuardEvalError (never a silent degradation) whose kind names the problem:\n\n:no_precondition — the event has no @precondition (it uses hand-written @conditionsfor generators, or is unregistered). guard_clauses needs a @precondition event. For a hand-written event, use the precondition's boolean and capture_state_reads instead.\n:unsupported_call / :unsupported_node — the precondition uses a construct outside the interpreter's fragment (the error names it and its source text). The derivable fragment is the same one @precondition accepts; an opaque call like sqrt(2.0) (state-free, so the macro tolerates it) is the one construct the interpreter refuses.\n:mutating_call — the precondition calls get!, which would insert a default into live state; guard evaluation is read-only.\n:early_return — a return sits inside a branch or loop; the fragment must be a straight-line prelude followed by one returned expression.\n:prelude_threw — a statement before the returned expression threw (the error carries the offending source and the causing exception). The real precondition throws on this state too.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-4","page":"Runbook","text":"There is nothing to turn off. guard_clauses is a diagnostic verb you call explicitly; it never runs during run and adds no cost to a simulation.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Check-model-invariants-every-step","page":"Runbook","text":"@invariant declares named boolean safety properties of the physical state; the CheckInvariants policy evaluates all of a module's invariants after initialization and after every fired event, and throws a structured InvariantViolation on the first failure — naming the invariant, the firing event, and the addresses this fire wrote that the invariant reads (the guilty addresses). Checking is opt-in and debug/test-tier: zero cost when the policy is absent.","title":"Check model invariants every step"},{"category":"section","location":"runbook/#How-to-invoke-it-5","page":"Runbook","text":"Declare invariants at the model module's top level, then pass policy=CheckInvariants(MyModel) to SimulationFSM (run is not exported; call it as ChronoSim.run). Compose with a RecordSkeleton — recorder first — so the violation carries a replayable prefix:\n\nusing ChronoSim\n\n# In the model module, at top level:\n@invariant \"person location xor elevator\" function (physical)\n    all((p.location > 0 && p.elevator == 0) || (p.location == 0 && p.elevator > 0)\n        for p in physical.person)\nend\n\n# At run time:\nrec = RecordSkeleton()\nsim = SimulationFSM(physical, events; seed=42,\n    policy=PolicyStack(rec, CheckInvariants(MyModel)))\nChronoSim.run(sim, InitEvent(), (p, i, e, w) -> w > 120.0)\n\nPrefer one @invariant per logical clause: on violation the failing name is the first diagnostic. An invariant must be a pure boolean function of physical — one argument, no mutation, no randomness — because the checker re-evaluates it on the failure path to recover the read set.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-5","page":"Runbook","text":"showerror on the thrown InvariantViolation, captured verbatim from an elevator run under a test-local corruption event that sets a person's elevator while leaving their floor nonzero:\n\nInvariantViolation: invariant \"person location xor elevator\" is false\n  model    : ChronoSimExamples.ElevatorExample\n  declared : /Users/adolgert/dev/ChronoSimExamples.jl/src/elevator/elevator.jl:258\n  step     : 95 (fires since init)\n  event    : (:CorruptPerson,)\n  when     : 85.79201980830436\n  guilty   : 1 address(es) written by this fire AND read by the invariant\n    (person, 1, elevator)\n  reads    : 2 address(es) in the failing evaluation\n    (person, 1, location)\n    (person, 1, elevator)\n  replay   : replay(sim_factory, skeleton; upto=94)   # reproduces the state one step before the violation\nThe invariant held after the previous step; the writes above broke it.\n\nThe replay line is the exact replay call that reconstructs the state one step before the break; feed it the skeleton (recorded_skeleton(rec)) and a factory that rebuilds the sim with the same constructor arguments.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-5","page":"Runbook","text":"step : 0 — the initializer itself violated the invariant; fix init, not an event. replay reads n/a (re-run the initializer to reproduce the state).\nguilty : none identified — no address this fire wrote is read by the invariant. The invariant reads state the tracker cannot see (e.g. Param-wrapped fields), or the corruption predates this step; use the replay command and step forward.\nWARNING : ... not a pure function — re-evaluation under read capture returned true: the invariant gave different answers on the same state. Make it a pure function of physical.\nreplay : no skeleton recorded — no RecordSkeleton shared the policy stack. Compose PolicyStack(RecordSkeleton(), CheckInvariants(MyModel)) to capture a replayable prefix.\nArgumentError: no @invariant is registered for this module — the module has no declarations, or was not loaded before the policy was constructed.\n@invariant \"name\" ... returned <T>, not Bool — an invariant body returned a non-Bool; an invariant must be a boolean function of the physical state.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-5","page":"Runbook","text":"Construct SimulationFSM without the policy (the default NoPolicy checks nothing and costs nothing), or leave a PolicyStack empty. In the examples' entry points, run_elevator() / run_sirvillage() check only when passed policy=CheckInvariants(...); the test suites pass it, production callers do not.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Ask-why-an-event-did-not-fire","page":"Runbook","text":"whynot takes a recorded TrajectorySkeleton, a sim_factory (the replay contract), and an event instance, and explains the event's absence at the furthest lifecycle stage it reached: :never_proposed, :rejected, :enabled_never_fired, or :fired. It returns a WhynotReport whose show is a bounded (≤ 30 line) readout.","title":"Ask why an event did not fire"},{"category":"section","location":"runbook/#How-to-invoke-it-6","page":"Runbook","text":"using ChronoSim\n\n# 1. Record the run whose missing event you want to explain.\nrec = RecordSkeleton()\nsim = SimulationFSM(physical, EVENTS;\n    rng=Xoshiro(seed), policy=rec)\nChronoSim.run(sim, init_physical, stop)\nskel = recorded_skeleton(rec)\n\n# 2. A factory that rebuilds the same sim (rng is overwritten by replay).\nfactory = policy -> (SimulationFSM(physical_ctor_args..., EVENTS;\n    rng=Xoshiro(seed), policy=policy),\n    init_physical)\n\n# 3. Ask.\nrep = whynot(skel, factory, StopElevator(2))","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-6","page":"Runbook","text":"The :never_proposed readout — the historical StopElevator missing-trigger bug (the hand-written trigger set omits direction, so dispatching an elevator already at a boundary floor never proposes the stop):\n\nwhynot (:StopElevator, 2): NEVER PROPOSED over 4 recorded steps\n  declared triggers (hand_written):\n    (elevator, _index, floor) | (elevator, _index, doors_open)\n  fired-event triggers : none\n  precondition at final replayed state : true\n    !! the precondition holds now, yet the event was never proposed:\n    !! a trigger for one of the MISSING addresses below is required\n  precondition reads (true trigger set) :\n    (elevator, 2, direction) | (elevator, 2, floor) | (elevator, 2, buttons_pressed)\n    (elevator, 2, doors_open)\n  MISSING triggers (reads no declared trigger covers) :\n    (elevator, 2, direction) | (elevator, 2, buttons_pressed)\n  near-miss writes (same container, different index/leaf) : 2 total\n    step 4 (:DispatchElevator, 3, Up) wrote (elevator, 1, direction)  [container_near_miss]\n    step 4 (:DispatchElevator, 3, Up) wrote (elevator, 1, floor)  [index_near_miss]\n  note: trigger-set analysis is one precondition evaluation at the final replayed state; short-circuited reads may be missing. fired() triggers (if any) can cover reads without a place trigger. Full per-claus…\n\nAddress lists print packed (addr | addr | addr), capped per section with an explicit ... and N more overflow line, so the whole readout stays within 30 lines even for wide trigger sets. A !! N recorded write(s) exactly matched a declared trigger ... line, if present, is an anomaly: a write matched a declared trigger yet nothing proposed the event — report it as a framework bug.\n\nThe :rejected readout — OpenElevatorDoors proposed 57 times, always failing the same conjunct because the boarding person pressed no button and no call was requested:\n\nwhynot (:OpenElevatorDoors, 2): PROPOSED BUT REJECTED over 113 recorded steps\n  proposals : 57, all rejected (steps 2, 3, 4, 5, 6, 8 ...)\n  examined  : 6 replayed case(s); showing first and last\n  -- rejection at step 2, t=0.5439169374413173 --\n  failing clause : call_exists || button_pressed\n  clauses : !(elevator.doors_open) = true | call_exists || button_pressed = false\n  reads (whole precondition evaluation):\n    (elevator, 2, direction) = Stationary | writer: none\n    (elevator, 2, floor) = 1 | writer: none\n    (elevator, 2, buttons_pressed) = Set{Int64}() | writer: none\n    (elevator, 2, doors_open) = false | writer: none\n  -- rejection at step 112, t=59.211385790742284 --\n  ...","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-6","page":"Runbook","text":"stage = :fired — the premise was wrong; the event did fire. The readout lists when.\nReplayDivergence (raised by the internal replay) — the factory does not rebuild the recorded run (different constructor args, package versions, or randomness outside sim.rng). See the replay entry.\nGuardEvalError never escapes: for a hand-written (non-@precondition) event, stage :rejected falls back to whole-precondition analysis (clause_analysis = :whole_precondition) instead of per-clause values.\nA near-miss line with class :index_near_miss (same container/leaf, different index) or :container_near_miss (same container, different leaf) is the write that should have been a trigger — the missed-trigger diagnostic.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-6","page":"Runbook","text":"whynot is a diagnostic function; there is nothing to turn off — just do not call it. It runs entirely offline on a finished skeleton and never touches a live run.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Ask-why-the-run-has-not-stopped","page":"Runbook","text":"whyrunning evaluates a stop predicate on the current state inside read capture and reports every address it read, that address's value and last writer, plus a recurrence summary of the last nsteps recorded steps — the dominant event types, the addresses they rewrite, and whether any of them is an address the predicate reads. It returns a WhyrunningReport.","title":"Ask why the run has not stopped"},{"category":"section","location":"runbook/#How-to-invoke-it-7","page":"Runbook","text":"sim must be at the skeleton's final state — the recording sim after run, or replay(factory, skel):\n\nrep = whyrunning(sim, skel, physical -> physical.next_strain_id > 10)\n\nThe predicate is called as stop_predicate(physical) when that method exists, else as stop_predicate(physical, step, clock, when) (the run stop-condition form).","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-7","page":"Runbook","text":"A sirvillage run whose stop predicate reads next_strain_id — a counter that Mutate would advance, but Mutate was excluded, so nothing in the step window touches it:\n\nwhyrunning over window 2160:2209; stop predicate is false\n  predicate reads:\n    (next_strain_id,) = 4 | writer: init\n  recurrence over steps 2160:2209:\n    Travel  50 | writes (locations, _index, individual_cnt), (actors, _index, haunt) | predicate: untouched\n  predicate reads written in this window : none\n  reachability analysis requires effect analysis (not yet run)\n\nThe last line is a stub: the static \"no event can ever write what the predicate reads\" verdict needs effect-analysis reachability (can_stop_change) wired into whyrunning, which is not yet done. Until then the report says only that nothing wrote those addresses recently.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-7","page":"Runbook","text":"ArgumentError: sim.when = ... but the skeleton's last step is at ... — the sim is not at the skeleton's final state. Replay it fully first (replay(factory, skel)) or pass the recording sim straight from run.\nerror: the stop predicate returned <T>, not Bool — the predicate must return Bool.\npredicate reads written in this window : none — nothing the predicate looks at changed in the last nsteps steps: a strong hint the run is stuck on state the stop condition cannot see.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-7","page":"Runbook","text":"Diagnostic only; do not call it. It performs one predicate evaluation (the same cost the stop condition already pays each step) and single passes over the window.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Read-out-why-the-run-stopped","page":"Runbook","text":"whystopped has two methods. Given an InvariantViolation caught from a CheckInvariants run, it prints the forensic readout: the invariant, the breaking event, each guilty address with its last and prior writers, and the exact replay(...) command. Given a plain TrajectorySkeleton, it reads out a normal ending — the final step and whether clocks were still enabled (stopped by the stop condition) or none were (the sampler exhausted). Both return a WhystoppedReport.","title":"Read out why the run stopped"},{"category":"section","location":"runbook/#How-to-invoke-it-8","page":"Runbook","text":"Compose the recorder before the checker so the violation carries a replayable prefix (order matters):\n\nrec = RecordSkeleton()\nsim = SimulationFSM(physical, EVENTS;\n    policy=PolicyStack(rec, CheckInvariants(MyModel)),\n    rng=Xoshiro(seed))\nerr = try\n    ChronoSim.run(sim, init_physical, stop); nothing\ncatch e; e end\n\nrep = whystopped(err)                        # violation forensics\n# or, for a run that ended without an exception:\nrep = whystopped(recorded_skeleton(rec))     # end-of-run readout","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-8","page":"Runbook","text":"A seeded corruption that sets person[1].elevator while location is nonzero, breaking the xor invariant — whystopped names the guilty writer event:\n\nwhystopped: invariant \"person location xor elevator\" is false\n  step   : 1 (fires since init)\n  event  : (:CorruptPerson,)\n  when   : 0.00461990380084346\n  guilty : 1 address(es)\n    (person, 1, elevator)\n      last writer  : step 1 (:CorruptPerson,) t=0.00461990380084346\n      prior writer : init\n  replay : replay(sim_factory, skeleton; upto=0)\n\nThe end-of-run readout for a run stopped by its stop condition with clocks still live:\n\nwhystopped: the run ended without an exception\n  steps  : 2209\n  last event : (:Travel, 14)\n  when   : 1.9999360141028282\n  60 clock(s) were still enabled; the run ended by its stop condition\n    (:Recover, 1)\n    ...\n  replay : replay(sim_factory, skeleton)","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-8","page":"Runbook","text":"last writer : none — no skeleton was recorded in the violation (no RecordSkeleton in the stack). Compose the recorder to capture writers.\nguilty : 0 address(es) — inherited from the violation: no address this fire wrote is read by the invariant (untracked reads, or corruption that predates the step).\nverdict :no_events_enabled (end-of-run) — the sampler exhausted; nothing was enabled when the run ended. :stopped_while_events_enabled — the stop condition ended a run that still had live clocks.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-8","page":"Runbook","text":"Diagnostic only. The violation method reads the exception the checker already built; the skeleton method reads a finished skeleton. Neither touches a live run.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Check-that-events-only-write-what-they-declare-(@fire-CheckEffects)","page":"Runbook","text":"@fire derives each event's static write set (effect_spec(EvtType)) by a syntactic taint pass over its fire! body — the write-side mirror of @precondition. The CheckEffects policy then verifies, after initialization and after every fired event, that each captured changed address matches a declared write mask. It is opt-in, read-only, and consumes no randomness, so a trajectory with it on equals one with it off; it costs a production run nothing when absent.","title":"Check that events only write what they declare (@fire + CheckEffects)"},{"category":"section","location":"runbook/#How-to-invoke-it-9","page":"Runbook","text":"Annotate fire! with @fire (byte-identical runtime behavior) and pass the policy to the simulation:\n\n@fire function fire!(evt::Infect, physical, when, rng)\n    physical.actors[evt.sink].strain = physical.actors[evt.source].strain\n    physical.actors[evt.sink].state = Infectious\nend\n\nsim = SimulationFSM(physical, events; seed=42, policy=CheckEffects(events))\nChronoSim.run(sim, InitEvent(), stop)\nderivation_report(Infect)     # includes the WRITES section\n\nCheckEffects(events) also unions in the WriteSpecs of any isimmediate event types in events, since immediate-event writes merge into the same changed_places. Compose it with other policies via PolicyStack.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-9","page":"Runbook","text":"The WRITES section of derivation_report (captured verbatim), showing each write site's mask, index cleanliness (with the widened-write count), operation, and rhs classification, then the rhs mix and any walker notes:\n\nDerivation report for Infect\n  event fields: source, sink\n  triggers: none derived (hand-written generators)\n  WRITES (2 sites, 0 widened)\n    WRITE [actors, ℤ, strain]  CLEAN  binds: sink  op: assign  rhs: state_expr\n    WRITE [actors, ℤ, state]  CLEAN  binds: sink  op: assign  rhs: evt_pure\n  rhs mix: evt_pure 1, state_expr 1, stochastic 0, opaque 0\n\nOn a violation the oracle throws an EffectCoverageError (captured verbatim from a fire! that hid a write to locations behind an opaque helper):\n\nEffectCoverageError: event Infect wrote an address not\ncovered by any WriteSpec.\n  changed address: (locations, 3, cnt)\n  masked to      : (locations, _index, cnt)\n  classified     : missing_container — no WriteSpec names this top-level field (an undeclared effect)\n  declared writes (masked):\n    (actors, _index, strain)\n    (actors, _index, state)\nThis event performed a write its @fire analysis did not declare — either the write hides behind an opaque helper (register it with @fragment or use a recognized mutation form) or the walker misclassified the address shape.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-9","page":"Runbook","text":":missing_container — an undeclared effect: the changed address's top-level field is named by no WriteSpec. Usually a helper the walker could not see (register it with @fragment or use a recognized mutation form) or a state write on a container the body never assigns directly.\n:shape_mismatch — the container is declared but the leaf/index shape differs. Check for an observed-container-valued field assignment (out of fragment) or report a walker bug.\nA macro-time @fire error naming a ! call — the mutation form is not in the recognized table; register the helper with @fragment or rewrite the mutation with a recognized form. See the Static Effect Analysis guide.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-9","page":"Runbook","text":"Diagnostic only. Construct the SimulationFSM without the policy (or leave it out of the PolicyStack); @fire itself only adds analysis metadata and never changes runtime behavior.\n\n","title":"How to turn it off"},{"category":"section","location":"runbook/#Lint-a-model's-footprints-(interference,-races,-missed-triggers)","page":"Runbook","text":"What: lint(events; physical=...) intersects every event's static write masks (@fire) with every event's static guard-read masks (@precondition or @guard) and reports: missed-trigger warnings (a write can flip a guard whose event has no trigger on that address), write→write races (info), dead addresses (info). Write→rate edges are NOT analyzed in v1 and the report says so. The analysis is static and over-approximate: it sees address masks, not expression semantics. See the \"Linting a model's footprints\" guide.","title":"Lint a model's footprints (interference, races, missed triggers)"},{"category":"section","location":"runbook/#How-to-invoke-it-10","page":"Runbook","text":"using ChronoSim\nreport = ChronoSim.lint([Travel, Infect, Recover, Reset, Mutate, InitEvent];\n                        physical=Village(30, 10, 1.0, Xoshiro(2938423)))\nreport                                 # bounded summary (show)\nChronoSim.print_lint(stdout, report)   # every edge, greppable\n\n# In tests:\nChronoSim.assert_lint_clean(report; allow=[\n    ChronoSim.LintAllow(reader=:PickNewDestination, mask=\"[person, ℤ, waiting]\",\n                        reason=\"reachability: waiting flips only with location\"),\n])\n\nHand-written models must opt in: prefix each precondition with @guard (analysis-only; runtime behavior is byte-identical) and mark state-receiving helpers @fragment. Derived (@precondition) events need nothing.","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-10","page":"Runbook","text":"Captured verbatim from the SIRVillage model (lint([InitEvent, Travel, Infect, Recover, Reset, Mutate]; physical=Village(30,10,1.0,Xoshiro(2938423)))). This report was reviewed by a human once and is archived here as the reference output:\n\nLintReport: 6 events\n  write→guard: 18 edges over 2 addresses (0 warnings in 0 groups, 18 info)\n  write→write: 17 shared-address pairs (info)\n  write→rate edges: not analyzed (enable-time reads are runtime-only in v1; the depnet tracks them dynamically)\n  dead addresses: none\n  unanalyzed guards: InitEvent    unanalyzed effects: none\n  caps: none\n\nReading it: the two write→guard addresses are [actors, ℤ, state] (16 edges — Infect/Recover/Reset/InitEvent write the state field all four guards read) and [actors, ℤ, haunt] (2 edges — InitEvent/Travel write the haunt field Infect's guard reads); all 18 are info because every SIRVillage reader has a changed(actors[who].state) (or .haunt) trigger that covers them (no missed proposals). The 17 write→write pairs span actors (state/strain/haunt), locations, strains, and next_strain_id — actors.strain shows up only here, since no guard reads it. InitEvent appears under unanalyzed guards because it has no precondition (it is the bootstrap event), not because anything is wrong. The Mutate → Infect rate dependence (Mutate writes strains[*].infectivity, Infect's enable reads it) appears in NO edge — it is a write→rate edge, and the fixed rate line is the honest disclosure that v1 does not analyze it.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-10","page":"Runbook","text":"WARNING missed trigger — reader R has no trigger on the printed mask, so a writer can flip R's precondition without R ever being proposed. Either add the @reactto changed(...) trigger (the fix for a real bug — the historical doors_open/StopElevator gaps) or add a LintAllow entry with a written reason (an intended trigger narrowing).\nLintFailure from assert_lint_clean — an unallowed warning; the message shows the exact reader/mask/writer to allow if intended. A stale (unused) LintAllow prints a notice but does not fail.\ncaps: lines — enumeration or dead-address reflection was skipped or hit a cap; pass a live physical= instance or add @domain as named. Never silent.\ndead address — a physical field written by no event and read by no guard (rate reads are not analyzed, so a rate-only input like landspread's distance appears here by design).","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-10","page":"Runbook","text":"Diagnostic only — nothing runs unless you call lint. The LintHarvest policy is opt-in and used only by the static⊇dynamic CI test; leave it out of the PolicyStack for production replicates.","title":"How to turn it off"},{"category":"section","location":"runbook/#Compile-a-model-to-Quint-(compile_quint)","page":"Runbook","text":"Print a model's registered guards, effects, helpers, and invariants as a Quint module for model checking. Pure Julia — no toolchain needed to emit.","title":"Compile a model to Quint (compile_quint)"},{"category":"section","location":"runbook/#How-to-invoke-it-11","page":"Runbook","text":"using ChronoSim\nqm = compile_quint(MyModel, EVENTS, physical_snapshot;\n                   name=\"mymodel\", invariants=InvariantModule)\nwrite_quint(\"mymodel.qnt\", qm)\nshow(stdout, MIME\"text/plain\"(), qm.report)\n\nEVENTS is the same event-type vector passed to SimulationFSM; the physical snapshot supplies init literals and array extents. invariants names the module holding the @invariants (default: the events module). Opt-outs: skip_events drops an event (module marked PARTIAL); assume_true_guards compiles an event whose precondition is literally true (reliability StartDay, sirvillage Travel).","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-11","page":"Runbook","text":"quint compilation: elevator (9 events)\n  clean   : PickNewDestination CallElevator OpenElevatorDoors ...\n  widened : (none)\n  assumed : (none)\n  skipped : (none)\n  refused : (none)\nconstants promoted: floor_cnt\nrecords collapsed : ElevatorCall -> requested\nfields erased     : (none)\ninvariants        : 8 compiled, 0 refused\nwidenings total   : 0 (v1 refuses where the design allowed widening; `// WIDENED:` markers reserved)","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-11","page":"Runbook","text":"compile_quint gathers all problems and throws one QuintCompileError, each entry naming the event, construct, and source:\n\n:no_precondition / :no_fire — annotate @precondition / @fire, or opt out with assume_true_guards / skip_events.\n:float_read — a guard/invariant/index reads a Float64. The spec is the integer jump skeleton; model the gate as an integer/enum field or skip the event.\n:unclassified_loop — a loop matching none of the five idioms (filter-accumulate, existential flag, universal flag, count/sum, min-by / capped scan). Rewrite it as one of those, or as a reducer over a generator.\n:loop_read_write_overlap — a fire! loop reads a variable the same loop writes in a way independent per-variable folds cannot express (e.g. an element write reading a scalar the loop increments). Use a local counter (compiled as one ordered foldl), or make the read field-disjoint / loop-indexed.\n:unordered_fold — a min-by or capped scan over dict keys: no defined iteration order. Iterate an array index range instead.\n\nv1 refuses every case the design allowed to widen, so a compiled module is exact on the integer fragment; // WIDENED: markers and report.widenings are the reserved surface for a future opt-in widening mode (the reconciliation test pins both at 0).","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-11","page":"Runbook","text":"Nothing is default-on; compilation is an offline call you make explicitly.","title":"How to turn it off"},{"category":"section","location":"runbook/#Verify-invariants-with-Apalache-(quint-verify)","page":"Runbook","text":"Prove a compiled model's invariants hold over every reachable state to a bounded depth.","title":"Verify invariants with Apalache (quint verify)"},{"category":"section","location":"runbook/#How-to-invoke-it-12","page":"Runbook","text":"The compiled module names its invariants inv_<declared name> plus the grouping val inv (the conjunction of all of them — the usual target).\n\nexport JAVA_HOME=\".../temurin-17/Contents/Home\"; export PATH=\"$JAVA_HOME/bin:$PATH\"\nnpx quint verify --max-steps=5 --invariant=inv mymodel.qnt\n# localize a violation one invariant at a time:\nnpx quint verify --max-steps=5 --invariant=inv_no_ghost_calls mymodel.qnt\n\nThe first verify downloads Apalache 0.56.1 into ~/.quint (~1 min, one-time).","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-12","page":"Runbook","text":"State 0: state invariant 0 holds.\n...\nThe outcome is: NoError\n[ok] No violation found\n\nWall-clocks scale with depth and instance size: on the 3-person elevator the compiled inv proves at depth 5 in ≈ 25 s; an early hand translation's type invariant at depth 10 took ≈ 10 min and its safety invariant at depth 8 ≈ 1.5 min (quint_spike/RESULTS.md). Start small and grow the depth.","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-12","page":"Runbook","text":"The outcome is: Error with a counterexample trace means an invariant was violated at some reachable state. Analyze which side is wrong — a translation bug (the spec is stricter or looser than the model) or a genuine model finding — by re-deriving the compiled action by hand against the Julia source. NoError means proved to that depth (not a proof at greater depths).","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-12","page":"Runbook","text":"Don't call it — verification is a manual, offline step.","title":"How to turn it off"},{"category":"section","location":"runbook/#Validate-a-recorded-trace-(validate_trace)","page":"Runbook","text":"Check that a recorded trajectory is a legal path of the compiled spec: every state satisfies the invariants, and every transition is accepted by the recorded event's compiled action.","title":"Validate a recorded trace (validate_trace)"},{"category":"section","location":"runbook/#How-to-invoke-it-13","page":"Runbook","text":"rec = RecordSkeleton()\nsim, init = factory(rec)                 # factory(policy) -> (sim, initializer)\nChronoSim.run(sim, init, stop)\nskel = recorded_skeleton(rec)\n\nqm  = compile_quint(model, EVENTS, physical; invariants=InvModule)\nrep = validate_trace(qm, skel, factory)  # factory is exactly replay's factory\nshow(stdout, MIME\"text/plain\"(), rep)","title":"How to invoke it"},{"category":"section","location":"runbook/#What-its-output-looks-like-13","page":"Runbook","text":"A pass (captured from the elevator, 4 recorded steps):\n\ntrace validation: elevator\n  steps        : 4 checked / 4 total\n  invariants   : PASSED\n  transitions  : PASSED\n  log[invariants]  : /tmp/quint_trace_xyz/stage1_run.log\n  log[transitions]  : /tmp/quint_trace_xyz/stage2_verify.log\n  wall-clock   : 14.0 s\n\nA stage-2 failure (captured: the same trajectory checked against a module whose CallElevator guard was mis-emitted with mutate_for_test) — the ascending localization loop names the exact rejected transition:\n\ntrace validation: elevator\n  steps        : 4 checked / 4 total\n  invariants   : PASSED\n  transitions  : FAILED\n  first failure: step 3 event CallElevator at when=3.0204157240506944 (transitions stage)\n  log[invariants]  : .../stage1_run.log\n  log[localize]  : .../stage2_localize_4.log\n  log[transitions]  : .../stage2_verify.log\n  wall-clock   : 49.17 s","title":"What its output looks like"},{"category":"section","location":"runbook/#What-each-failure-form-means-13","page":"Runbook","text":"invariants: FAILED (stage 1, quint run) — a reconstructed state violates a compiled invariant: the simulator and the spec disagree on which states are legal. first_failure names the violating state's step and event (parsed from the deterministic counterexample's t_i).\ntransitions: FAILED (stage 2, quint verify, reported as Apalache Deadlock) — a recorded transition is rejected by the compiled guard/effect: the guard the simulator ran and the guard the spec models disagree. This is the thesis case; the mutation test (mutate_for_test) proves the loop has teeth, and the notReach_k localization loop pins the first rejected step.\n:skipped — the toolchain is absent (invariants needs Node; transitions needs a JVM), or the trace fires an event omitted by skip_events (a PARTIAL module cannot force that transition — skip_reason names the step). The report quotes the exact install commands from quint_spike/VERSIONS.md. Steps beyond maxsteps are reported, never silently dropped.\n:error — the checker crashed or printed unrecognizable output; the log tail is in skip_reason and the full log at the reported path. Distinct from :failed by construction.","title":"What each failure form means"},{"category":"section","location":"runbook/#How-to-turn-it-off-13","page":"Runbook","text":"Nothing is default-on. A ReplayDivergence raised during reconstruction is a pre-existing determinism bug (different constructor args or package versions), surfaced as-is, not swallowed.\n\n","title":"How to turn it off"},{"category":"section","location":"state_contract/#The-State-Addressing-Contract","page":"State Contract","text":"ChronoSim discovers which events to re-examine by observing reads and writes of the physical state. That discovery is only as trustworthy as the notifications the observed containers emit. This page states the contract the ObservedState containers promise, precisely enough to be tested mechanically. The snapshot-diff fuzz tester (test/test_container_fuzz.jl) is the runnable form of this page: it performs random mutation sequences against each container, computes the true change set by comparing before and after snapshots, and asserts the contract below.","title":"The State-Addressing Contract"},{"category":"section","location":"state_contract/#Vocabulary","page":"State Contract","text":"A place is a leaf location in the physical state that holds a primitive value: a struct field of Int, Float64, Bool, an enum, and so on. Places are what preconditions read and what fire! writes.\n\nAn address is the name of a place: the tuple of path components from the physical state to the leaf. Components are\n\nMember(fieldname) for struct fields (both fields of the physical state and fields of @keyedby element structs),\nInt for a one-dimensional ObservedVector index,\nNTuple{N,Int} for an N-dimensional ObservedArray index,\nthe raw key for an ObservedDict entry.\n\nExample: (Member(:actor), 7, Member(:speed)) names physical.actor[7].speed.\n\nThe denotation of an address at a moment in time is the value stored at that place, or ⊥ (undefined) if the address names no live place — an index beyond the array extent, a key absent from the dict.","title":"Vocabulary"},{"category":"section","location":"state_contract/#The-contract","page":"State Contract","text":"C1 — Write soundness. After any mutating operation on the physical state, the set of addresses appended to obs_modified is a superset of the set of addresses whose denotation changed, including changes to and from ⊥. Over-notification is permitted (it costs re-evaluation, not correctness); under-notification is a bug, because an event whose enabling condition depends on a silently-changed place will never be re-examined.\n\nC2 — Address integrity. At every moment a notification is emitted, the _address chain of the emitting element is rooted at the physical state and names the slot the element currently occupies. A notification emitted through a stale address is a C1 violation in disguise: it reports a change at a place other than the one that changed.\n\nC3 — Address determinism. The address of a place is a pure function of the path used to reach it. Two runs of the same program produce the same addresses; addresses can be serialized, compared, and forged (constructed by code that never touched the container, e.g. a generator template binding event fields into an address pattern). Nothing about an address depends on memory layout, hashing, or iteration order.\n\nRead symmetry. The same three statements hold for reads and obs_read: every operation whose result depends on the denotation of a place must notify a read of that place, or of a coarser address that covers it (see whole-container reads below). Reads feed the dependency network that decides which events to re-check, so a missed read disables future wake-ups — the same failure mode as a missed write, one step removed.","title":"The contract"},{"category":"section","location":"state_contract/#Granularity:-per-place,-whole-container,-and-bulk-reads","page":"State Contract","text":"A read notification with a concrete address, such as (Member(:calls), (3, Up), Member(:requested)), is per-place.\n\nSome operations depend on the shape of a container rather than any one entry: length, isempty, keys, values, pairs on an ObservedDict, and every operation on an ObservedSet. These emit a whole-container read or write: the container's own address with no trailing component, e.g. (Member(:calls),). A whole-container read means \"this computation depends on the entire container\"; any write inside the container must be treated as affecting it. This is sound and deliberately coarse.\n\nObservedSet is entirely whole-container: its elements are values, not addressable places, so membership tests, insertions, and removals all read or write the set's own address. Use a set when the fact of membership is the state, and events should react to any change of the set.","title":"Granularity: per-place, whole-container, and bulk reads"},{"category":"section","location":"state_contract/#Fixed-extent:-position-is-identity","page":"State Contract","text":"For ObservedArray and ObservedVector, an integer index is the identity of a place. actors[7] must mean the same individual for the life of the simulation, because addresses containing 7 are stored in the dependency network and in generator templates that outlive any single read.\n\nThis only holds if the array's extent is fixed. A popfirst! or deleteat! shifts every subsequent element to a new index: every address past the deletion point changes denotation, the vacated tail becomes ⊥, and identity silently migrates between addresses. Rather than notify O(n) places per operation and still leave identity unstable, ChronoSim restricts observed arrays to fixed extent:\n\nAllocate with ObservedArray{T,Member}(undef, dims...) and fill by setindex!.\nElement writes (arr[i] = x, arr[i].field = v) are the supported mutations.\nLength-changing operations — push!, pop!, pushfirst!, popfirst!, append!, resize! — throw a FixedExtentError.\n\nChoose containers by identity model: positional addressing (ObservedArray) when the population is fixed and dense — grid cells, a fleet of n machines; keyed addressing (ObservedDict) when entities are born, die, or carry natural identifiers — the key is the identity, and delete!/setindex! change exactly one place. The full slot-map container (stable handles over a dense store) is future work; the restriction above is the honest contract until it exists.","title":"Fixed extent: position is identity"},{"category":"section","location":"state_contract/#Documented-read-coarseness-and-holes","page":"State Contract","text":"The following behaviors are part of the contract in its current form. They are sound but coarse; the fuzzer measures their cost as imprecision, not failure.\n\nlength, isempty, keys, values, and pairs on an ObservedDict read the whole container.\nIterating an ObservedDict with primitive values reads each key it yields.\n\nAbsence is observable: haskey(dict, k), and the miss paths of get, get!, and pop!(dict, k, default), notify a read of the key k itself even though k names no live place. Addresses are forgeable (C3), so the dependency network can hold interest in a place before it exists; inserting k later notifies a write of the same address and wakes the dependent event.\n\nThe following are known unsound holes, tracked to be fixed (a test that demonstrates one is a passing regression test once fixed):\n\nIterating an ObservedArray or ObservedSet emits no reads. For fixed-extent arrays of compound elements this is benign — element field reads notify individually, and the extent cannot change — but iteration that inspects primitive element values directly is invisible. Preconditions should index rather than iterate, or the loop must be counted as reading every place it touched.","title":"Documented read coarseness and holes"},{"category":"section","location":"state_contract/#What-the-fuzzer-asserts","page":"State Contract","text":"For each container type, the fuzzer generates random operation sequences (reads, writes, and — for dicts and sets — insertions and deletions), snapshots the raw underlying storage before and after each operation, and computes the true denotation-change set Δ including ⊥ transitions. It then asserts:\n\nC1/soundness: notified_writes ⊇ Δ after every mutation.\nC2/integrity: every notified address, replayed against the physical state by pure address lookup, resolves to the element that emitted it (for compound elements) or to the mutated slot (for primitive elements).\nC3/determinism: the same operation sequence run twice from the same seed yields the identical notification sequence.\nPrecision (reported, not asserted): |notified_writes| / |Δ| per operation class — the over-notification factor.\n\n","title":"What the fuzzer asserts"},{"category":"section","location":"event_update/#How-Event-States-are-Updated","page":"Event Updates","text":"The main loop of the framework for simulation will fire an event, look at changed places, and then update the events in the system. This turns out to be complicated, so let's discuss it here.\n\nAn event has\n\nA key which identifies its relationship to physical state.\nA set of places upon which its precondition depends.\nA set of places upon which its rate depends.\nA rate (distribution in time) that is set in the Sampler by the enable() function.\n\nThe same event key, when applied to a different physical state, may depend on different sets of places. This is not true for a traditional GSPN or GSMP, but it's how we think about events. For instance, a Move(agentidx, rightdirection) will depend on space to the right of the agent, but which space must be empty changes as the agent moves.\n\nA simulation has:\n\nA set of enabled events.\nThe event that just fired.\nA set of states that were modified when the event fired.\n\nThe goal of the main loop of the simulation, once it has fired the event is to modify:\n\nThe set of enabled events, by disabling those with failed preconditions or enabling those generated.\nThe set of places upon which precondition or rates depends for affected events.\nThe rate of events for which their place dependencies changed in value or for which the set of places has changed.\n\nBefore and after this update, what can the states of an event be?\n\nDisabled -> enabled and enabled -> disabled.\nSets of places can change.\nMaybe just the rate is called again.\n\nThe notion of re-enabling is a little tough. I will define that any time an event with the same clock key is enabled before and after firing, and the rate-depending places have changed, it is re-enabled. That's a firm definition. Rate-depending places can be a different set of places or they can have been written to. Either way.\n\nWhat sets of events do we have?\n\nThe set of all events that depend on ANY changed places.\nThe set of those events whose preconditions still hold.\nThe set of those whose preconditions hold but they depend on different places.\nThe set of those whose rates depend on ANY changed places.\n\nLet's process the data this way, not an event at a time but a set of events at a time.\n\nA question: When rules generate new events, they don't need to create existing events. If we just discovered an event is disabled, that too can be excluded. My question is whether the set of all generated events intersected with the set of enabled events is contained in the set of affected events.\n\n(generated events) ∩ (enabled events) ⊆ (affected event preconditions)\n\nThe practical answer is that there aren't restrictions on event generation. The generator functions could generate lots of extra events, so there is no guarantee that those related to the recent modified places are all we will see.\n\n","title":"How Event States are Updated"},{"category":"section","location":"whatitis/#What-This-Framework-Does","page":"What It Does","text":"This page describes what actually happens when a ChronoSim simulation runs. It is worth reading once before you write a model, because every piece you write — the event structs, the preconditions, the rates, the firing functions — corresponds to one step of the loop described here.","title":"What This Framework Does"},{"category":"section","location":"whatitis/#One-pass-through-the-main-loop","page":"What It Does","text":"A ChronoSim simulation is a continuous-time simulation, which means events fire one at a time at increasing real-valued times, and the state changes only at those moments. Each pass through the main loop does the following.\n\nThe sampler chooses the next event. Every enabled event owns a clock whose ring time was drawn from the distribution its enable function supplied, and the sampler simply picks the clock that rings first. The chosen event is a small immutable value, such as Repair(machine=3).\nThe framework calls that event's fire! function, which modifies the state. Because the state is built from observed containers, the framework comes away with the exact list of addresses that were written, such as (:machines, 3, :status).\nThe framework then asks which events might care about those writes. Two groups answer. First, every currently enabled event whose precondition or rate previously read one of the changed addresses is re-examined. Second, the generators — whether written by hand with @conditionsfor or derived from a @precondition — propose brand-new candidate events that the changed addresses might have made possible.\nEvery event in those two groups has its precondition evaluated against the new state. An event that was enabled and is no longer justified is disabled and its clock discarded. An event that is newly justified is enabled, its enable function is called, and a fresh firing time is drawn. An event that remains enabled keeps its clock, unless the state it reads for its rate changed, in which case reenable lets it adjust its distribution without losing the time it has already waited.\nThe loop returns to step one.\n\nThe essential property of this loop is that nothing is scanned. The framework never walks a list of all conceivable events. It touches exactly the events that the changed addresses implicate, which is what lets a model consist of many small events without the bookkeeping cost growing with the size of the state.","title":"One pass through the main loop"},{"category":"section","location":"whatitis/#The-pieces-you-provide","page":"What It Does","text":"For each kind of event in your model, you define the following, and the list maps one-to-one onto the loop above.\n\nAn immutable struct subtyping SimEvent, whose fields identify which actor, place, or resource this instance of the event concerns. This is what the sampler chooses and what fire! receives.\nGenerators, which serve step three. With @precondition they are derived from the enabling rule; with @conditionsfor you state them yourself.\nA precondition function, which serves step four by deciding enabling.\nAn enable function returning a distribution and a reference time, which serves the sampler in step one, and optionally a reenable function for events whose rate must track a changing state.\nA fire! function, which serves step two.","title":"The pieces you provide"},{"category":"section","location":"whatitis/#The-components-underneath","page":"What It Does","text":"Four components implement the loop, and knowing their names helps when reading the rest of the documentation.\n\nObservedState provides the containers and macros (@observedphysical, @keyedby, ObservedArray, ObservedDict, ObservedSet) that give every part of the state an address and report every read and write. This is what makes steps two and three possible.\nThe generator machinery (@conditionsfor, @reactto, @precondition, @fragment, @domain) turns changed addresses into proposed candidate events.\nCompetingClocks.jl is the sampler. It holds one clock per enabled event, supports non-exponential distributions efficiently, and can report the likelihood of the trajectory it sampled, which is what enables statistical inference on top of a simulation.\nSimulationFSM is the machine that runs the loop: it fires the event, applies any immediate events, collects the changed addresses, reconciles the enabled set, and hands control back to the sampler.\n\n","title":"The components underneath"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This page builds a complete, running simulation of five machines that break down and get repaired. It is short, but nothing is left out: by the end you will have declared observed state, defined two events, run the simulation, and looked at the trajectory it produced. The Build an Elevator tutorial then does the same thing for a larger model with more kinds of events.\n\nFirst the imports. A model imports the framework, the state-declaration module, and the specific functions it will add methods to. Distributions supplies the waiting-time distributions, and Random supplies the seeded generator.\n\nusing ChronoSim\nusing ChronoSim.ObservedState\nimport ChronoSim: precondition, enable, fire!, generators\nusing Distributions\nusing Random","title":"Getting Started"},{"category":"section","location":"getting_started/#The-state","page":"Getting Started","text":"A machine is either working or broken. The state of the whole factory is a vector of machines. We declare the element type with @keyedby, naming the type, the index type it will have inside its container, and its fields. Then we declare the state itself with @observedphysical. These macros are what let the framework observe every read and write, which is how it knows which events to reconsider after each firing.\n\n@enum MachineStatus working broken\n\n@keyedby Machine Int64 begin\n    status::MachineStatus\nend\n\n@observedphysical Factory begin\n    machines::ObservedVector{Machine,Member}\nend\n\nThe vector of machines has a fixed length for the whole run, which is exactly what an ObservedVector requires. We allocate it, fill it, and wrap it in the state.\n\nfunction make_factory(machine_count)\n    machines = ObservedArray{Machine,Member}(undef, machine_count)\n    for i in eachindex(machines)\n        machines[i] = Machine(working)\n    end\n    return Factory(machines)\nend\nnothing #hide","title":"The state"},{"category":"section","location":"getting_started/#The-events","page":"Getting Started","text":"A machine that is working can break. The precondition states exactly that, and nothing else. Because we declare it with @precondition, the framework derives the event's generators from the body: any change to any machine's status field will propose a Break event for that machine.\n\nstruct Break <: SimEvent\n    machine::Int64\nend\n\n@precondition precondition(evt::Break, factory) = factory.machines[evt.machine].status == working\n\nThe enable function gives the waiting time until the event fires, counted from the moment the precondition became true. A Weibull distribution with shape greater than one means machines fail more often as they age since their last repair.\n\nenable(evt::Break, factory, when) = (Weibull(2.0, 5.0), when)\n\nFiring is the only place the state changes.\n\nfire!(evt::Break, factory, when, rng) = factory.machines[evt.machine].status = broken\n\nA machine that is broken can be repaired. The three definitions have the same shape.\n\nstruct Repair <: SimEvent\n    machine::Int64\nend\n\n@precondition precondition(evt::Repair, factory) = factory.machines[evt.machine].status == broken\n\nenable(evt::Repair, factory, when) = (Exponential(1.0), when)\n\nfire!(evt::Repair, factory, when, rng) = factory.machines[evt.machine].status = working","title":"The events"},{"category":"section","location":"getting_started/#Running","page":"Getting Started","text":"The simulation is assembled from the state and the list of event types. The observer is a function the framework calls after every firing; here it appends the event and its time to a record.\n\ntrajectory = Tuple{Tuple,Float64}[]\nobserve(factory, when, event, changed) = push!(trajectory, (clock_key(event), when))\n\nfactory = make_factory(5)\nsim = SimulationFSM(factory, [Break, Repair]; seed=979797, observer=observe)\nnothing #hide\n\nThe initializer runs at time zero. It must write the parts of the state that events depend on, not merely rely on the constructor having set them, because it is these initial writes that propose the first candidate events. Here, writing every machine's status proposes a Break for every machine, each precondition passes, and five failure clocks start running.\n\nstart_all_working(factory, when, rng) =\n    for i in eachindex(factory.machines)\n        factory.machines[i].status = working\n    end\nnothing #hide\n\nThe stop condition sees the event that is about to fire, before it fires, so stopping at a time boundary is exact.\n\nstopping(factory, step, event, when) = when > 20.0\n\nChronoSim.run(sim, start_all_working, stopping)\ntrajectory\n\nEach entry is the fired event's clock key and its firing time. Machines break, repairs follow, and the two event types alternate per machine, at irregular continuous times.","title":"Running"},{"category":"section","location":"getting_started/#What-the-derivation-did","page":"Getting Started","text":"We never said when to check whether a Break could happen; the @precondition macro worked that out from the enabling rule. You can ask it to show its work:\n\nusing ChronoSim: derivation_report\nderivation_report(Break)\n\nThe report says there is one trigger, listening on the pattern machines[i].status, and that a change there proposes Break(i) for the specific machine whose status changed. When your preconditions get more involved, this report is the first thing to look at, and the Generators page of the manual explains everything it can say.\n\n\n\nThis page was generated using Literate.jl.\n\n","title":"What the derivation did"},{"category":"section","location":"debugging_verification_overview/#Debugging-and-Verification","page":"Overview","text":"ChronoSim ships a layer of tools for understanding a run that finished wrong, proving a model right, and catching the bugs that a stochastic simulation hides best — the missed trigger that silently drops an event, the invariant that goes false a thousand steps before anything crashes. Every technique here shares three commitments. It is opt-in: nothing runs unless you ask for it. It is zero-cost when off: a production SimulationFSM built without a debugging policy compiles those hooks to return nothing, draws no extra randomness, and produces a byte-identical trajectory. And it produces plain-text, bounded readouts — a diagnostic is something you read, greppable and length-capped, not a debugger you drive. Turn a tool on to investigate; leave it off to ship.","title":"Debugging & Verification"},{"category":"section","location":"debugging_verification_overview/#Which-technique-for-which-symptom","page":"Overview","text":"Reach for this when… Technique Guide\nYou have an observed trajectory and want its likelihood, or to check it is even a legal path of your model Trace evaluation Evaluating a trace against a model\nYou want to stop a recorded run just before any step and inspect the exact state it had there — time-travel debugging Record & replay Recording and replaying a run\nYou suspect the state went bad long before anything crashed, and want a throw at the exact breaking event Invariants Declaring and checking invariants\nA run finished but looks wrong: an event never fired, the run won't stop, or an invariant broke Why-verbs Debugging a simulation\nYou want each event to declare what it writes, and a runtime check that it wrote nothing else Effect analysis Static effect analysis (@fire)\nYou want to catch a missed-trigger or interference bug statically, before any run Footprint lints Linting a model's footprints\nYou want to prove an invariant holds over every reachable state, or check a run against a formal spec Model checking Model checking a simulation\n\nThe Runbook is the mechanical companion to these guides: one terse entry per feature — exact invocation, captured output, every failure form, and how to turn it off — written to be followed with no context beyond this repository.","title":"Which technique for which symptom"},{"category":"section","location":"debugging_verification_overview/#How-they-fit-together","page":"Overview","text":"The techniques share artifacts, so investigating with one hands the next its input. A RecordSkeleton run produces the skeleton that the why-verbs, replay, and trace validation all read; composing that recorder under CheckInvariants makes a violation carry a replay command; the same @precondition and @fire annotations that drive effect analysis and the lints are what compile to a model-checkable spec. Record once, and every offline diagnostic runs on the same replayable artifact.\n\n","title":"How they fit together"},{"category":"section","location":"architecture_diagram/#ChronoSim.jl-Framework-Architecture","page":"Architecture","text":"","title":"ChronoSim.jl Framework Architecture"},{"category":"section","location":"architecture_diagram/#Core-Components-and-Data-Flow","page":"Architecture","text":"graph TD\n    subgraph Framework\n        TS[Tracked State]\n        EG[Event Generation]\n        TU[Transition Updates]\n        TJS[Trajectory Sampling]\n        \n        TU -->|fire!| TS\n        TS -->|changes| EG\n        EG -->|possible events| TU\n        TJS -->|next event| TU\n    end\n    \n    style TU fill:#f9f,stroke:#333,stroke-width:4px\n    style Framework fill:#f0f0f0,stroke:#333,stroke-width:2px","title":"Core Components and Data Flow"},{"category":"section","location":"architecture_diagram/#Component-Descriptions","page":"Architecture","text":"Transition Updates: The central orchestrator that manages event firing and state transitions\nTracked State: Monitors and tracks all state changes in the physical system\nEvent Generation: Creates new possible events based on state changes\nTrajectory Sampling: Selects the next event to fire based on the stochastic process","title":"Component Descriptions"},{"category":"section","location":"architecture_diagram/#Data-Flow","page":"Architecture","text":"fire!: Transition Updates executes state changes through the Tracked State\nchanges: Tracked State reports what changed to Event Generation\npossible events: Event Generation provides new/updated events to Transition Updates\nnext event: Trajectory Sampling selects which event fires next from the available events\n\n","title":"Data Flow"},{"category":"section","location":"#ChronoSim","page":"ChronoSim","text":"ChronoSim is a framework for continuous-time, stochastic, discrete-event simulation. You describe a system as a collection of events, where each event says when it makes sense to happen, how long it waits before happening, and what it changes when it does. The framework watches which parts of the state each event reads and writes, and it uses that information to keep exactly the right set of events enabled as the simulation runs.\n\nHere is the shape of a single event, taken from a model of machines that break and get repaired. A machine can only break while it is working, the time until it breaks follows a Weibull distribution whose age is read from the state, and breaking changes the machine's status.\n\nstruct Break <: SimEvent\n    machine::Int64\nend\n\n@precondition function precondition(evt::Break, state)\n    return state.machines[evt.machine].status == working\nend\n\nfunction enable(evt::Break, state, when)\n    age = state.machines[evt.machine].work_age\n    return (Weibull(2.0, 10.0), when - age)\nend\n\nfunction fire!(evt::Break, state, when, rng)\n    state.machines[evt.machine].status = broken\nend\n\nThat is the whole definition. The @precondition macro reads the enabling rule and derives, automatically, the bookkeeping that tells the framework when to reconsider this event: here, \"whenever any machine's status changes, check whether Break applies to that machine.\" You can also write that bookkeeping by hand when you want precise control, and the Generators page of the manual explains both paths.\n\nThere are many good simulation tools, and the page Why Not Hand Made discusses honestly when a hand-written loop or another framework serves better. What ChronoSim contributes is the combination of the following.\n\nExact sampling in continuous time. Events fire one at a time at real-valued times drawn from the distributions you specify, including non-exponential ones, in the tradition of the generalized semi-Markov process and next-reaction samplers. Sampling is handled by CompetingClocks.jl, which also makes trajectory likelihoods available for model fitting.\nAutomatic dependency tracking. The state is observed, so the framework records every read a precondition makes and every write a firing makes. It re-examines an event only when something it actually depends on has changed, which is what makes many small, interacting events affordable.\nModels compose through the state. If one set of events moves people and another set spreads disease among them, the two sets coordinate through the shared state without either one naming the other.\nThe enabling logic is checkable. Because every event carries an explicit precondition, the framework can verify invariants that hand-woven simulations leave implicit, and the model corresponds closely to a formal specification style such as TLA+.","title":"ChronoSim"},{"category":"section","location":"#Where-to-go-next","page":"ChronoSim","text":"If you are new, read the Introduction for the core concepts, then work through Getting Started, which builds a complete running simulation in about sixty lines. The Build an Elevator tutorial constructs a larger model event by event and exercises every feature you would need to write a system of your own. The Manual explains the simulation state and events and generators precisely, and the Reference lists every exported function and macro. The Development section documents the internal contracts for contributors.\n\n","title":"Where to go next"},{"category":"section","location":"model_checking/#Model-checking-a-simulation","page":"Model Checking a Simulation","text":"The same registered ASTs that run a ChronoSim model — @precondition guards, @fire effects, @fragment helpers, @invariants — already carry its full discrete semantics. compile_quint prints them as a Quint module, and validate_trace checks a recorded trajectory against that module. This closes the loop in both directions: the spec checks your runs exhaustively, and your runs check the compiler.\n\nCompiling needs nothing but Julia. Checking the emitted module needs the Quint toolchain (Node) and, for symbolic proofs, a JVM for Apalache — both discovered by find_quint_toolchain and, when absent, reported as a loud :skipped verdict rather than a silent pass.","title":"Model checking a simulation"},{"category":"section","location":"model_checking/#1.-What-you-get","page":"Model Checking a Simulation","text":"An integer/Bool/enum/set model of the discrete jump skeleton of your GSMP. Continuous quantities — firing times (when), rates, ages — are erased: they decide when events fire, never whether a guard holds, and bounded model checking over reals is out of scope.\nTwo directions of trust. quint verify (Apalache) proves your @invariants hold over every reachable state to a bounded depth. validate_trace proves a recorded run is a legal path of the compiled spec — if the compiler and the simulator disagree, one of them has a bug.","title":"1. What you get"},{"category":"section","location":"model_checking/#2.-Compile","page":"Model Checking a Simulation","text":"using ChronoSim\nqm = compile_quint(ElevatorDerivedExample, EVENTS, ElevatorSystem(3, 2, 5);\n                   name=\"elevator\", invariants=ElevatorExample)\nwrite_quint(\"elevator.qnt\", qm)\nshow(stdout, MIME\"text/plain\"(), qm.report)\n\nRead the emitted .qnt side by side with the Julia source: each @precondition becomes an all { … } of guard conjuncts before the primed variable assignments (guard-before-effect is load-bearing — a disabled action never evaluates its effect); each @fire becomes those primed updates; each @fragment becomes a pure def; precondition-recursion becomes a shared pure def precond_<Event>(…). The compilation report is a bounded, greppable block:\n\nquint compilation: ElevatorDerivedExample (9 events)\n  clean   : PickNewDestination CallElevator OpenElevatorDoors EnterElevator ExitElevator CloseElevatorDoors MoveElevator StopElevator DispatchElevator\n  widened : (none)\n  assumed : (none)\n  skipped : (none)\n  refused : (none)\nconstants promoted: floor_cnt\nrecords collapsed : ElevatorCall -> requested\nfields erased     : (none)\ninvariants        : 8 compiled, 0 refused\nwidenings total   : 0 (v1 refuses where the design allowed widening; `// WIDENED:` markers reserved)\n\nEvery deviation from a 1:1 transliteration is loud. A field a guard never reads is promoted to a pure val (shrinking the state space); a single-field @keyedby element collapses to its bare value; a Float64 field is erased. Where the design allowed a widened (over-approximate) emission — a dict-key argmin with an unspecified tie-break, a non-uniform draw's erased weights — v1 refuses instead (:unordered_fold, :unsupported_call): stricter than required, and never a silent over-approximation. The // WIDENED: marker plus report.widenings is the reserved surface for a future opt-in widening mode; the reconciliation test pins marker count == report count (both 0 today).","title":"2. Compile"},{"category":"section","location":"model_checking/#3.-Check","page":"Model Checking a Simulation","text":"The compiler emits one val inv_<name> per @invariant (the declared name, sanitized, kept in a trailing comment) plus the grouping val inv — the conjunction of every compiled invariant and the usual checker target.\n\n# fast falsification (Node only, seconds)\nquint run --max-steps=200 --max-samples=2000 --seed=42 --invariant=inv elevator.qnt\n\n# bounded proof (Apalache; needs JAVA_HOME)\nquint verify --max-steps=5 --invariant=inv elevator.qnt\n# or one invariant at a time, to localize:\nquint verify --max-steps=5 --invariant=inv_no_ghost_calls elevator.qnt\n\nquint run samples random traces and is the right first tool — it falsifies in seconds. quint verify explores every trace to the given depth and proves the invariant (or returns a counterexample). Depth costs time: on the 3-person instance the compiled inv at depth 5 proves in ~25 s; an early hand translation took ~10 min for its type invariant at depth 10 and ~1.5 min for its safety invariant at depth 8 (the calibration table lives in quint_spike/RESULTS.md). Choose small instances and grow the depth until the wall-clock hurts.","title":"3. Check"},{"category":"section","location":"model_checking/#4.-Validate-a-trace","page":"Model Checking a Simulation","text":"Record a run with RecordSkeleton, then hand the skeleton and the same factory replay uses to validate_trace:\n\nrec = RecordSkeleton()\nsim, init = factory(rec)                 # factory(policy) -> (sim, initializer)\nChronoSim.run(sim, init, stop)\nskel = recorded_skeleton(rec)\n\nqm  = compile_quint(model, EVENTS, physical; invariants=InvModule)\nrep = validate_trace(qm, skel, factory)\nshow(stdout, MIME\"text/plain\"(), rep)\n\ntrace validation: elevator\n  steps        : 4 checked / 4 total\n  invariants   : PASSED\n  transitions  : PASSED\n  log[invariants]  : /tmp/quint_trace_xyz/stage1_run.log\n  log[transitions]  : /tmp/quint_trace_xyz/stage2_verify.log\n  wall-clock   : 14.0 s\n\nStage 1 (invariants, quint run, Node) checks that every reconstructed state satisfies every compiled invariant. Stage 2 (transitions, quint verify, Apalache) checks that every recorded transition is accepted by the recorded event's compiled action from the preceding state.\n\nThen the demo with teeth. Recompile with one guard operator mis-emitted and watch stage 2 fail — and the ascending localization loop name the exact rejected transition (captured output):\n\nqmm = compile_quint(model, EVENTS, physical; invariants=InvModule,\n    mutate_for_test=(event=:CallElevator, from=:!=, to=:(==), occurrence=2))\nvalidate_trace(qmm, skel, factory)\n\ntrace validation: elevator\n  steps        : 4 checked / 4 total\n  invariants   : PASSED\n  transitions  : FAILED\n  first failure: step 3 event CallElevator at when=3.0204157240506944 (transitions stage)\n  log[invariants]  : .../stage1_run.log\n  log[localize]  : .../stage2_localize_4.log\n  log[transitions]  : .../stage2_verify.log\n  wall-clock   : 49.17 s","title":"4. Validate a trace"},{"category":"section","location":"model_checking/#5.-Read-each-failure","page":"Model Checking a Simulation","text":"Compile refusal (float read). QuintCompileError(:float_read) names the event, the chain, and its file:line. The float never gated discrete behavior: model the gate as an integer/enum field, or skip_events=[:Event] and record the skip.\nInvariant violation (Apalache). A counterexample trace. First decide which side is wrong: a translation bug (the spec is stricter/looser than the model) or a genuine model finding. Re-derive the compiled action by hand against the Julia source before blaming the model.\nStage-1 trace failure. A recorded state violates a compiled invariant — the simulator and the spec disagree about what states are legal. first_failure names the violating state's step and event, parsed from the deterministic counterexample.\nStage-2 trace failure. A recorded transition is rejected by the compiled guard/effect (Apalache reports Deadlock) — the thesis case: the guard the simulator ran and the guard the spec models disagree. The ascending localization loop (notReach_k targets, shallow verifies) pins the first rejected step into first_failure.\n:skipped. The toolchain is absent; the report quotes the exact install commands from quint_spike/VERSIONS.md.\n:error. The checker itself crashed or printed unparseable output — the log tail is in skip_reason, the full log at the reported path. Never conflated with :failed.","title":"5. Read each failure"},{"category":"section","location":"model_checking/#6.-Limits","page":"Model Checking a Simulation","text":"The compiled spec is the integer fragment of the model. Floats are erased; when, rates, and ages are out of scope by construction (a @precondition takes only (evt, state), so a firing time cannot appear in a guard). v1 refuses every construct the design allowed to widen (dict-key argmin tie-breaks, non-uniform draw supports): a compiled module is therefore exact on the integer fragment, never a silent superset. The // WIDENED: marker mechanism and report.widenings remain as the documented surface for a future opt-in widening mode — a proof over a widened spec would still be sound for safety, just no longer exact. Future work: ITF-JSON trace export, that widening mode, and lifting the integer restriction where a bounded real abstraction is tractable.","title":"6. Limits"},{"category":"section","location":"model_checking/#Related","page":"Model Checking a Simulation","text":"Runbook: Compile a model to Quint, Verify invariants with Apalache, Validate a recorded trace.\nDeclaring and checking invariants — the @invariants this compiles and proves.\nRecording and replaying a run — the skeleton validate_trace checks against the spec.\nDebugging & Verification — the overview and symptom-to-technique table.\n\n","title":"Related"}]
}
